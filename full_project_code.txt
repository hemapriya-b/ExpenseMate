=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\database\init_db.py
=============================
from app import db, app
from models import User, Room, Expense, RoomMember, PersonalExpense
import datetime

def init_database():
    with app.app_context():
        # Create all tables
        db.create_all()
        
        # Create sample admin user
        if not User.query.first():
            admin = User(
                username='admin',
                email='admin@expensio.com',
                password_hash='hashed_password_here'  # You'll hash this properly
            )
            db.session.add(admin)
            db.session.commit()
            print("✅ Database initialized successfully!")
            print("👤 Sample user created: admin / password")

if __name__ == '__main__':
    init_database()


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\static\css\auth.css
=============================
/* ===== AUTH PAGES SPECIAL DESIGN ===== */

/* Login Specific Styles */
.login-container {
    min-height: calc(100vh - 200px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
}

.login-box {
    width: 100%;
    max-width: 420px;
    animation: fadeInUp 0.6s ease;
}

.login-header {
    text-align: center;
    margin-bottom: 2.5rem;
}

.login-header h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
    background: var(--secondary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.login-header p {
    color: var(--text-muted);
    font-size: 1.1rem;
}

.login-form .form-group {
    margin-bottom: 1.5rem;
    position: relative;
}

.form-icon {
    position: absolute;
    left: 15px;
    top: 42px;
    color: var(--accent-purple);
    font-size: 1.1rem;
}

.remember-forgot {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
    font-size: 0.9rem;
}

.remember-me {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--text-muted);
}

.remember-me input {
    width: 18px;
    height: 18px;
    accent-color: var(--accent-purple);
}

.forgot-password {
    color: var(--accent-blue);
    text-decoration: none;
    font-size: 0.9rem;
}

.forgot-password:hover {
    text-decoration: underline;
}

.btn-login {
    width: 100%;
    padding: 14px;
    background: var(--secondary-gradient);
    color: white;
    border: none;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
}

.btn-login:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
}

.divider {
    display: flex;
    align-items: center;
    margin: 2rem 0;
    color: var(--text-muted);
}

.divider::before,
.divider::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--card-border);
}

.divider span {
    padding: 0 1rem;
    font-size: 0.9rem;
}

.social-login {
    display: flex;
    gap: 1rem;
    margin-top: 1.5rem;
}

.social-btn {
    flex: 1;
    padding: 12px;
    border: 1px solid var(--card-border);
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
    color: var(--text-light);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    cursor: pointer;
    transition: var(--transition);
    font-size: 0.9rem;
}

.social-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: var(--accent-purple);
}

/* Signup Specific Styles */
.signup-container {
    min-height: calc(100vh - 100px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
}

.signup-box {
    width: 100%;
    max-width: 480px;
    animation: fadeInUp 0.6s ease;
}

.signup-header {
    text-align: center;
    margin-bottom: 2rem;
}

.signup-header h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
    background: var(--secondary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.signup-header p {
    color: var(--text-muted);
    font-size: 1.1rem;
}

.signup-form {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
}

.form-group.full-width {
    grid-column: 1 / -1;
}

.password-strength {
    margin-top: 0.5rem;
    font-size: 0.85rem;
}

.strength-bar {
    height: 4px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 2px;
    margin: 0.25rem 0;
    overflow: hidden;
}

.strength-fill {
    height: 100%;
    width: 0%;
    border-radius: 2px;
    transition: width 0.3s ease;
}

.strength-weak .strength-fill {
    background: #ff6b8b;
    width: 33%;
}

.strength-medium .strength-fill {
    background: #ffb74d;
    width: 66%;
}

.strength-strong .strength-fill {
    background: #00ff9d;
    width: 100%;
}

.strength-text {
    color: var(--text-muted);
    font-size: 0.8rem;
}

.terms-conditions {
    grid-column: 1 / -1;
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.5;
}

.terms-conditions label {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    cursor: pointer;
}

.terms-conditions input {
    margin-top: 3px;
    accent-color: var(--accent-purple);
}

.terms-conditions a {
    color: var(--accent-blue);
    text-decoration: none;
}

.terms-conditions a:hover {
    text-decoration: underline;
}

.btn-signup {
    grid-column: 1 / -1;
    width: 100%;
    padding: 14px;
    background: var(--secondary-gradient);
    color: white;
    border: none;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    cursor: pointer;
    transition: var(--transition);
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
}

.btn-signup:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
}

/* Password toggle */
.password-toggle {
    position: absolute;
    right: 15px;
    top: 42px;
    background: none;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    font-size: 1rem;
    padding: 0;
}

.password-toggle:hover {
    color: var(--accent-blue);
}

/* Form validation styles */
.form-control.valid {
    border-color: var(--success);
    background: rgba(0, 255, 157, 0.05);
}

.form-control.invalid {
    border-color: var(--danger);
    background: rgba(255, 107, 139, 0.05);
}

.validation-message {
    font-size: 0.8rem;
    margin-top: 0.25rem;
    display: flex;
    align-items: center;
    gap: 0.25rem;
}

.valid-message {
    color: var(--success);
}

.invalid-message {
    color: var(--danger);
}

/* App Info Section */
.app-info {
    text-align: center;
    margin-top: 3rem;
    padding: 2rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 15px;
    border: 1px solid var(--card-border);
}

.app-info h3 {
    margin-bottom: 1rem;
    color: var(--accent-blue);
}

.features-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    margin-top: 1rem;
}

.feature-tag {
    padding: 6px 12px;
    background: rgba(138, 43, 226, 0.1);
    border-radius: 20px;
    font-size: 0.85rem;
    color: var(--accent-purple);
}

/* Responsive for Auth */
@media (max-width: 768px) {
    .signup-form {
        grid-template-columns: 1fr;
    }
    
    .form-group.full-width {
        grid-column: 1;
    }
    
    .terms-conditions {
        grid-column: 1;
    }
    
    .btn-signup {
        grid-column: 1;
    }
}

@media (max-width: 480px) {
    .login-box,
    .signup-box {
        padding: 1.5rem;
    }
    
    .social-login {
        flex-direction: column;
    }
    
    .remember-forgot {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }
    
    .features-list {
        flex-direction: column;
        align-items: center;
    }
}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\static\css\style.css
=============================
/* ===== AESTHETIC CLUB GRADIENT THEME ===== */
:root {
    /* Color Variables */
    --primary-dark: #0f0c29;
    --primary-gradient: linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%);
    --secondary-gradient: linear-gradient(135deg, #8A2BE2 0%, #4B0082 100%);
    --accent-purple: #8A2BE2;
    --accent-blue: #00D4FF;
    --text-light: #E6E6FA;
    --text-muted: #B0B0D0;
    --card-bg: rgba(255, 255, 255, 0.05);
    --card-border: rgba(138, 43, 226, 0.3);
    --success: #00FF9D;
    --warning: #FFB74D;
    --danger: #FF6B8B;
    --shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ===== RESET & BASE STYLES ===== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: var(--primary-gradient);
    color: var(--text-light);
    min-height: 100vh;
    overflow-x: hidden;
    line-height: 1.6;
}

/* Background Animation */
body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        radial-gradient(circle at 20% 80%, rgba(138, 43, 226, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(0, 212, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 40% 40%, rgba(75, 0, 130, 0.1) 0%, transparent 50%);
    z-index: -1;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
}

/* ===== TYPOGRAPHY ===== */
h1, h2, h3, h4, h5, h6 {
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 1rem;
}

h1 { font-size: 2.5rem; }
h2 { font-size: 2rem; }
h3 { font-size: 1.5rem; }

p {
    margin-bottom: 1rem;
    color: var(--text-muted);
}

a {
    color: var(--accent-blue);
    text-decoration: none;
    transition: var(--transition);
}

a:hover {
    color: var(--accent-purple);
}

/* ===== LAYOUT COMPONENTS ===== */

/* Glassmorphism Container */
.glass-container {
    background: var(--card-bg);
    backdrop-filter: blur(10px);
    border: 1px solid var(--card-border);
    border-radius: 20px;
    box-shadow: var(--shadow);
}

/* Cards */
.card {
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: 15px;
    padding: 1.5rem;
    transition: var(--transition);
    margin-bottom: 1rem;
}

.card:hover {
    transform: translateY(-5px);
    border-color: var(--accent-purple);
    box-shadow: 0 12px 40px rgba(138, 43, 226, 0.2);
}

/* ===== NAVIGATION ===== */
.navbar {
    background: rgba(15, 12, 41, 0.9);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--card-border);
    padding: 1rem 2rem;
    position: sticky;
    top: 0;
    z-index: 1000;
}

.navbar .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.navbar-brand {
    font-size: 1.8rem;
    font-weight: 700;
    background: var(--secondary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.nav-menu {
    display: flex;
    align-items: center;
    gap: 1.5rem;
}

.nav-link {
    color: var(--text-muted);
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: var(--transition);
    padding: 0.5rem 1rem;
    border-radius: 10px;
    font-weight: 500;
}

.nav-link:hover {
    color: var(--accent-blue);
    background: rgba(0, 212, 255, 0.1);
}

.nav-link.active {
    color: var(--accent-blue);
    background: rgba(0, 212, 255, 0.1);
}

/* User Dropdown */
.user-dropdown {
    position: relative;
}

.user-btn {
    background: var(--secondary-gradient);
    color: white;
    border: none;
    padding: 0.75rem 1.5rem;
    border-radius: 12px;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 500;
    transition: var(--transition);
}

.user-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
}

.dropdown-menu {
    position: absolute;
    top: 100%;
    right: 0;
    background: var(--primary-dark);
    border: 1px solid var(--card-border);
    border-radius: 10px;
    padding: 0.5rem;
    min-width: 200px;
    display: none;
    z-index: 1000;
    margin-top: 0.5rem;
}

.user-dropdown:hover .dropdown-menu {
    display: block;
}

.dropdown-item {
    display: block;
    padding: 0.75rem 1rem;
    color: var(--text-light);
    text-decoration: none;
    border-radius: 8px;
    transition: var(--transition);
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.dropdown-item:hover {
    background: rgba(138, 43, 226, 0.1);
    color: var(--accent-purple);
}

/* ===== BUTTONS ===== */
.btn {
    padding: 12px 28px;
    border-radius: 12px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: var(--transition);
    border: none;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-size: 0.95rem;
}

.btn-primary {
    background: var(--secondary-gradient);
    color: white;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
}

.btn-outline {
    background: transparent;
    border: 2px solid var(--accent-purple);
    color: var(--accent-purple);
}

.btn-outline:hover {
    background: var(--accent-purple);
    color: white;
}

.btn-sm {
    padding: 8px 16px;
    font-size: 0.85rem;
}

.btn-lg {
    padding: 16px 32px;
    font-size: 1.1rem;
}

/* ===== FORM ELEMENTS ===== */
.form-group {
    margin-bottom: 1.5rem;
}

.form-label {
    display: block;
    margin-bottom: 0.5rem;
    color: var(--text-light);
    font-weight: 500;
    font-size: 0.95rem;
}

.form-control {
    width: 100%;
    padding: 12px 16px;
    background: rgba(255, 255, 255, 0.07);
    border: 1px solid var(--card-border);
    border-radius: 10px;
    color: var(--text-light);
    font-size: 1rem;
    transition: var(--transition);
}

.form-control:focus {
    outline: none;
    border-color: var(--accent-blue);
    box-shadow: 0 0 0 3px rgba(0, 212, 255, 0.1);
    background: rgba(255, 255, 255, 0.1);
}

.form-control::placeholder {
    color: var(--text-muted);
    opacity: 0.7;
}

/* Form Icons */
.form-icon {
    position: absolute;
    left: 15px;
    top: 42px;
    color: var(--accent-purple);
    font-size: 1.1rem;
}

/* ===== AUTH PAGES ===== */
.auth-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
}

.auth-box {
    width: 100%;
    max-width: 420px;
    padding: 2.5rem;
    border-radius: 25px;
    position: relative;
    overflow: hidden;
}

.auth-box::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: conic-gradient(
        from 0deg,
        transparent,
        rgba(138, 43, 226, 0.1),
        rgba(0, 212, 255, 0.1),
        transparent
    );
    animation: rotate 10s linear infinite;
    z-index: -1;
}

.auth-header {
    text-align: center;
    margin-bottom: 2rem;
}

.auth-header h1 {
    font-size: 2.2rem;
    margin-bottom: 0.5rem;
    background: var(--secondary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.auth-header p {
    color: var(--text-muted);
}

.auth-footer {
    text-align: center;
    margin-top: 2rem;
    color: var(--text-muted);
}

.auth-footer a {
    color: var(--accent-blue);
    text-decoration: none;
    font-weight: 600;
}

.auth-footer a:hover {
    text-decoration: underline;
}

/* ===== DASHBOARD ===== */
.dashboard-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem;
}

.dashboard-grid {
    display: grid;
    grid-template-columns: 1fr 350px;
    gap: 2rem;
    margin-top: 2rem;
}

/* Summary Cards */
.summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
}

.summary-card {
    padding: 2rem;
    border-radius: 15px;
    text-align: center;
    position: relative;
    overflow: hidden;
    transition: var(--transition);
}

.summary-card:nth-child(1) {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.summary-card:nth-child(2) {
    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

.summary-card:nth-child(3) {
    background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}

.summary-card i {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    opacity: 0.9;
}

.summary-card h3 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
}

.summary-card p {
    font-size: 0.9rem;
    opacity: 0.9;
    margin-bottom: 0;
}

/* Quick Actions */
.quick-actions {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin-bottom: 3rem;
}

.action-btn {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 2rem 1rem;
    border-radius: 15px;
    background: var(--card-bg);
    border: 2px solid var(--card-border);
    transition: var(--transition);
    text-decoration: none;
    color: var(--text-light);
}

.action-btn:hover {
    transform: translateY(-5px);
    border-color: var(--accent-purple);
    background: rgba(138, 43, 226, 0.1);
}

.action-btn i {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: var(--accent-blue);
}

.action-btn span {
    font-weight: 600;
    font-size: 1.1rem;
}

/* Rooms Section */
.rooms-section {
    margin-bottom: 3rem;
}

.section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
}

.section-title {
    font-size: 1.8rem;
    font-weight: 600;
}

.view-all {
    color: var(--accent-blue);
    text-decoration: none;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.view-all:hover {
    text-decoration: underline;
}

.rooms-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
}

.room-card {
    position: relative;
    overflow: hidden;
}

.room-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transition: 0.6s;
}

.room-card:hover::before {
    left: 100%;
}

.room-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
}

.room-name {
    font-size: 1.3rem;
    font-weight: 600;
    color: var(--accent-blue);
}

.room-code {
    background: rgba(138, 43, 226, 0.2);
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.9rem;
    font-family: monospace;
}

.room-stats {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
    font-size: 0.9rem;
}

.room-stat {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.room-stat i {
    color: var(--accent-purple);
}

/* Recent Activity */
.activity-list {
    background: var(--card-bg);
    border-radius: 15px;
    padding: 1.5rem;
    border: 1px solid var(--card-border);
}

.activity-item {
    display: flex;
    align-items: center;
    padding: 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.activity-item:last-child {
    border-bottom: none;
}

.activity-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(138, 43, 226, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 1rem;
}

.activity-details {
    flex: 1;
}

.activity-title {
    font-weight: 500;
    margin-bottom: 0.25rem;
}

.activity-time {
    font-size: 0.85rem;
    color: var(--text-muted);
}

.activity-amount {
    font-weight: 600;
    color: var(--success);
}

/* Budget Tracker */
.budget-tracker {
    background: var(--card-bg);
    border-radius: 15px;
    padding: 2rem;
    border: 1px solid var(--card-border);
    margin-top: 2rem;
}

.budget-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
}

.budget-title {
    font-size: 1.3rem;
    font-weight: 600;
}

.budget-amount {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--accent-blue);
}

.progress-bar {
    height: 12px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    overflow: hidden;
    margin-bottom: 1rem;
}

.progress-fill {
    height: 100%;
    background: var(--secondary-gradient);
    border-radius: 10px;
    transition: width 0.5s ease;
}

.budget-info {
    display: flex;
    justify-content: space-between;
    font-size: 0.9rem;
    color: var(--text-muted);
}

/* ===== CHARTS ===== */
.chart-container {
    background: var(--card-bg);
    border-radius: 15px;
    padding: 2rem;
    border: 1px solid var(--card-border);
    margin-bottom: 2rem;
}

.charts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
}

/* ===== FLASH MESSAGES ===== */
.flash-container {
    position: fixed;
    top: 80px;
    right: 20px;
    z-index: 1000;
    max-width: 400px;
}

.flash-message {
    padding: 1rem 1.5rem;
    margin-bottom: 1rem;
    border-radius: 10px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    animation: slideIn 0.3s ease;
    border-left: 4px solid;
    backdrop-filter: blur(10px);
}

.flash-success {
    background: rgba(0, 255, 157, 0.1);
    border-color: var(--success);
    color: var(--success);
}

.flash-error {
    background: rgba(255, 107, 139, 0.1);
    border-color: var(--danger);
    color: var(--danger);
}

.flash-info {
    background: rgba(0, 212, 255, 0.1);
    border-color: var(--accent-blue);
    color: var(--accent-blue);
}

.flash-warning {
    background: rgba(255, 183, 77, 0.1);
    border-color: var(--warning);
    color: var(--warning);
}

.flash-close {
    background: none;
    border: none;
    color: inherit;
    font-size: 1.5rem;
    cursor: pointer;
    padding: 0;
    margin-left: 1rem;
    line-height: 1;
}

/* ===== FOOTER ===== */
.footer {
    text-align: center;
    padding: 2rem;
    margin-top: 3rem;
    border-top: 1px solid var(--card-border);
    color: var(--text-muted);
}

.footer-links {
    margin-top: 1rem;
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
}

.footer-links a {
    color: var(--text-muted);
    text-decoration: none;
}

.footer-links a:hover {
    color: var(--accent-blue);
}

/* ===== UTILITY CLASSES ===== */
.mt-1 { margin-top: 0.5rem; }
.mt-2 { margin-top: 1rem; }
.mt-3 { margin-top: 1.5rem; }
.mt-4 { margin-top: 2rem; }

.mb-1 { margin-bottom: 0.5rem; }
.mb-2 { margin-bottom: 1rem; }
.mb-3 { margin-bottom: 1.5rem; }
.mb-4 { margin-bottom: 2rem; }

.text-center { text-align: center; }
.text-right { text-align: right; }

.d-flex { display: flex; }
.align-center { align-items: center; }
.justify-between { justify-content: space-between; }
.gap-1 { gap: 0.5rem; }
.gap-2 { gap: 1rem; }

.w-100 { width: 100%; }

/* ===== ANIMATIONS ===== */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

@keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
}

@keyframes glow {
    0%, 100% { box-shadow: 0 0 20px rgba(138, 43, 226, 0.3); }
    50% { box-shadow: 0 0 40px rgba(138, 43, 226, 0.6); }
}

@keyframes rotate {
    100% { transform: rotate(360deg); }
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
}

.floating {
    animation: float 3s ease-in-out infinite;
}

.glow {
    animation: glow 2s ease-in-out infinite;
}

.loading {
    animation: pulse 1.5s infinite;
}

/* ===== EMPTY STATES ===== */
.empty-state {
    text-align: center;
    padding: 3rem 2rem;
    color: var(--text-muted);
}

.empty-state i {
    font-size: 3rem;
    margin-bottom: 1rem;
    opacity: 0.5;
}

.empty-state h3 {
    margin-bottom: 0.5rem;
    color: var(--text-light);
}

/* ===== RESPONSIVE DESIGN ===== */
@media (max-width: 1024px) {
    .dashboard-grid {
        grid-template-columns: 1fr;
    }
    
    .charts-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 768px) {
    .navbar {
        padding: 1rem;
    }
    
    .nav-menu {
        gap: 1rem;
    }
    
    .dashboard-container {
        padding: 1rem;
    }
    
    .summary-grid {
        grid-template-columns: 1fr;
    }
    
    .quick-actions {
        grid-template-columns: repeat(2, 1fr);
    }
    
    .rooms-grid {
        grid-template-columns: 1fr;
    }
    
    .section-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
    }
    
    .flash-container {
        left: 1rem;
        right: 1rem;
        max-width: none;
    }
}

@media (max-width: 480px) {
    .quick-actions {
        grid-template-columns: 1fr;
    }
    
    .auth-box {
        padding: 1.5rem;
    }
    
    .user-btn span {
        display: none;
    }
    
    .nav-link span {
        display: none;
    }
    
    .nav-link {
        padding: 0.5rem;
    }
}

/* ===== PRINT STYLES ===== */
@media print {
    .navbar,
    .footer,
    .btn,
    .action-btn {
        display: none !important;
    }
    
    body {
        background: white !important;
        color: black !important;
    }
    
    .card {
        border: 1px solid #ddd !important;
        box-shadow: none !important;
    }
}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\static\js\charts.js
=============================
// Charts.js - Additional chart functionality for Expensio

// Custom chart plugins
const chartPlugins = {
    id: 'customBackground',
    beforeDraw: (chart) => {
        const ctx = chart.ctx;
        ctx.save();
        ctx.globalCompositeOperation = 'destination-over';
        ctx.fillStyle = 'rgba(255, 255, 255, 0.02)';
        ctx.fillRect(0, 0, chart.width, chart.height);
        ctx.restore();
    }
};

// Register the plugin
Chart.register(chartPlugins);

// Chart utility functions
const ChartUtils = {
    // Format currency
    formatCurrency: (value) => {
        return '₹' + value.toLocaleString('en-IN');
    },
    
    // Generate gradient
    createGradient: (ctx, color1, color2) => {
        const gradient = ctx.createLinearGradient(0, 0, 0, 400);
        gradient.addColorStop(0, color1);
        gradient.addColorStop(1, color2);
        return gradient;
    },
    
    // Animate chart update
    animateChartUpdate: (chart, duration = 500) => {
        chart.options.animation.duration = duration;
        chart.update();
    },
    
    // Export chart as image
    exportChart: (chartId, fileName = 'chart.png') => {
        const chart = document.getElementById(chartId);
        const link = document.createElement('a');
        link.download = fileName;
        link.href = chart.toDataURL('image/png');
        link.click();
        return true;
    },
    
    // Get chart data as JSON
    getChartData: (chartId) => {
        const chart = Chart.getChart(chartId);
        if (chart) {
            return {
                labels: chart.data.labels,
                datasets: chart.data.datasets
            };
        }
        return null;
    }
};

// Additional chart types configuration
const chartConfigs = {
    pie: {
        cutout: '50%',
        borderRadius: 10,
        spacing: 5
    },
    
    bar: {
        borderRadius: 8,
        borderSkipped: false
    },
    
    line: {
        tension: 0.4,
        fill: true,
        pointRadius: 4,
        pointHoverRadius: 6
    }
};

// Initialize all charts on page
function initializeAllCharts() {
    // This function can be called when you have multiple charts
    const charts = document.querySelectorAll('canvas[data-chart-type]');
    
    charts.forEach(canvas => {
        const chartType = canvas.getAttribute('data-chart-type');
        const chartData = JSON.parse(canvas.getAttribute('data-chart-data') || '{}');
        
        switch(chartType) {
            case 'pie':
                createCustomPieChart(canvas.id, chartData);
                break;
            case 'bar':
                createCustomBarChart(canvas.id, chartData);
                break;
            case 'line':
                createCustomLineChart(canvas.id, chartData);
                break;
        }
    });
}

// Create custom pie chart with enhanced options
function createCustomPieChart(canvasId, data) {
    const ctx = document.getElementById(canvasId).getContext('2d');
    
    return new Chart(ctx, {
        type: 'pie',
        data: {
            labels: data.labels || [],
            datasets: [{
                data: data.values || [],
                backgroundColor: data.colors || [],
                borderColor: 'rgba(255, 255, 255, 0.1)',
                borderWidth: 2,
                hoverOffset: 20,
                borderRadius: chartConfigs.pie.borderRadius,
                spacing: chartConfigs.pie.spacing
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            cutout: chartConfigs.pie.cutout,
            plugins: {
                legend: {
                    position: 'right',
                    labels: {
                        padding: 20,
                        usePointStyle: true,
                        font: {
                            size: 12
                        }
                    }
                },
                tooltip: {
                    callbacks: {
                        label: function(context) {
                            const label = context.label || '';
                            const value = context.parsed;
                            const total = context.dataset.data.reduce((a, b) => a + b, 0);
                            const percentage = Math.round((value / total) * 100);
                            return `${label}: ₹${value.toLocaleString()} (${percentage}%)`;
                        }
                    }
                }
            },
            animation: {
                animateScale: true,
                animateRotate: true,
                duration: 1500,
                easing: 'easeOutQuart'
            }
        }
    });
}

// Create custom bar chart with enhanced options
function createCustomBarChart(canvasId, data) {
    const ctx = document.getElementById(canvasId).getContext('2d');
    
    // Create gradient for bars
    const gradient = ChartUtils.createGradient(ctx, data.color || '#8A2BE2', '#4B0082');
    
    return new Chart(ctx, {
        type: 'bar',
        data: {
            labels: data.labels || [],
            datasets: [{
                label: data.label || 'Data',
                data: data.values || [],
                backgroundColor: gradient,
                borderColor: data.color || '#8A2BE2',
                borderWidth: 2,
                borderRadius: chartConfigs.bar.borderRadius,
                borderSkipped: chartConfigs.bar.borderSkipped
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: {
                    display: false
                }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    grid: {
                        color: 'rgba(255, 255, 255, 0.1)'
                    },
                    ticks: {
                        callback: ChartUtils.formatCurrency
                    }
                },
                x: {
                    grid: {
                        display: false
                    }
                }
            },
            animation: {
                duration: 1500,
                easing: 'easeOutQuart'
            }
        }
    });
}

// Create custom line chart
function createCustomLineChart(canvasId, data) {
    const ctx = document.getElementById(canvasId).getContext('2d');
    
    // Create gradient for area under line
    const gradient = ChartUtils.createGradient(ctx, 
        (data.color || '#00D4FF') + '40', 
        (data.color || '#00D4FF') + '10'
    );
    
    return new Chart(ctx, {
        type: 'line',
        data: {
            labels: data.labels || [],
            datasets: [{
                label: data.label || 'Trend',
                data: data.values || [],
                backgroundColor: gradient,
                borderColor: data.color || '#00D4FF',
                borderWidth: 3,
                tension: chartConfigs.line.tension,
                fill: chartConfigs.line.fill,
                pointRadius: chartConfigs.line.pointRadius,
                pointHoverRadius: chartConfigs.line.pointHoverRadius,
                pointBackgroundColor: '#FFFFFF',
                pointBorderColor: data.color || '#00D4FF',
                pointBorderWidth: 2
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: {
                    display: false
                }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    grid: {
                        color: 'rgba(255, 255, 255, 0.1)'
                    },
                    ticks: {
                        callback: ChartUtils.formatCurrency
                    }
                },
                x: {
                    grid: {
                        display: false
                    }
                }
            },
            animation: {
                duration: 2000,
                easing: 'easeOutQuart'
            }
        }
    });
}

// Export the utilities
window.ChartUtils = ChartUtils;
window.initializeAllCharts = initializeAllCharts;


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\static\js\main.js
=============================


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\auth\login.html
=============================
{% extends "layout.html" %}

{% block title %}Login - Expensio{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{{ url_for('static', filename='css/auth.css') }}">
<style>
    /* Login Specific Styles */
    .login-container {
        min-height: calc(100vh - 200px);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
    }
    
    .login-box {
        width: 100%;
        max-width: 420px;
        animation: fadeInUp 0.6s ease;
    }
    
    .login-header {
        text-align: center;
        margin-bottom: 2.5rem;
    }
    
    .login-header h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .login-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
    }
    
    .login-form .form-group {
        margin-bottom: 1.5rem;
        position: relative;
    }
    
    .form-label {
        display: block;
        margin-bottom: 0.5rem;
        color: var(--text-light);
        font-weight: 500;
    }
    
    .form-icon {
        position: absolute;
        left: 15px;
        top: 42px;
        color: var(--accent-purple);
        font-size: 1.1rem;
    }
    
    .form-control {
        width: 100%;
        padding: 12px 16px 12px 45px;
        background: rgba(255, 255, 255, 0.07);
        border: 1px solid var(--card-border);
        border-radius: 12px;
        color: var(--text-light);
        font-size: 1rem;
        transition: var(--transition);
    }
    
    .form-control:focus {
        outline: none;
        border-color: var(--accent-blue);
        box-shadow: 0 0 0 3px rgba(0, 212, 255, 0.1);
        background: rgba(255, 255, 255, 0.1);
    }
    
    .remember-forgot {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        font-size: 0.9rem;
    }
    
    .remember-me {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--text-muted);
    }
    
    .remember-me input {
        width: 18px;
        height: 18px;
        accent-color: var(--accent-purple);
    }
    
    .forgot-password {
        color: var(--accent-blue);
        text-decoration: none;
    }
    
    .forgot-password:hover {
        text-decoration: underline;
    }
    
    .btn-login {
        width: 100%;
        padding: 14px;
        background: var(--secondary-gradient);
        color: white;
        border: none;
        border-radius: 12px;
        font-size: 1rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        cursor: pointer;
        transition: var(--transition);
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 0.5rem;
    }
    
    .btn-login:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
    }
    
    .btn-login:active {
        transform: translateY(0);
    }
    
    .login-footer {
        text-align: center;
        margin-top: 2rem;
        color: var(--text-muted);
    }
    
    .login-footer a {
        color: var(--accent-blue);
        text-decoration: none;
        font-weight: 600;
    }
    
    .login-footer a:hover {
        text-decoration: underline;
    }
    
    .divider {
        display: flex;
        align-items: center;
        margin: 2rem 0;
        color: var(--text-muted);
    }
    
    .divider::before,
    .divider::after {
        content: '';
        flex: 1;
        height: 1px;
        background: var(--card-border);
    }
    
    .divider span {
        padding: 0 1rem;
        font-size: 0.9rem;
    }
    
    .social-login {
        display: flex;
        gap: 1rem;
        margin-top: 1.5rem;
    }
    
    .social-btn {
        flex: 1;
        padding: 12px;
        border: 1px solid var(--card-border);
        border-radius: 12px;
        background: rgba(255, 255, 255, 0.05);
        color: var(--text-light);
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        cursor: pointer;
        transition: var(--transition);
    }
    
    .social-btn:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: var(--accent-purple);
    }
    
    .app-info {
        text-align: center;
        margin-top: 3rem;
        padding: 2rem;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 15px;
        border: 1px solid var(--card-border);
    }
    
    .app-info h3 {
        margin-bottom: 1rem;
        color: var(--accent-blue);
    }
    
    .features-list {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
        margin-top: 1rem;
    }
    
    .feature-tag {
        padding: 6px 12px;
        background: rgba(138, 43, 226, 0.1);
        border-radius: 20px;
        font-size: 0.85rem;
        color: var(--accent-purple);
    }
    
    /* Animations */
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    @keyframes pulse {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.7; }
    }
    
    .loading {
        animation: pulse 1.5s infinite;
    }
    
    /* Responsive */
    @media (max-width: 480px) {
        .login-box {
            padding: 1.5rem;
        }
        
        .social-login {
            flex-direction: column;
        }
        
        .remember-forgot {
            flex-direction: column;
            align-items: flex-start;
            gap: 0.5rem;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="auth-container login-container">
    <div class="glass-container auth-box login-box">
        <!-- Header -->
        <div class="login-header">
            <h1><i class="fas fa-money-bill-wave"></i> EXPENSIO</h1>
            <p>Smart Planning & Budgeting</p>
        </div>
        
        <!-- Flash Messages -->
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="flash-message flash-{{ category }} mb-3">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        
        <!-- Login Form -->
        <form method="POST" action="{{ url_for('login') }}" class="login-form">
            <div class="form-group">
                <label class="form-label" for="username">
                    <i class="fas fa-user form-icon"></i>
                    Username
                </label>
                <input type="text" 
                       id="username" 
                       name="username" 
                       class="form-control" 
                       placeholder="Enter your username"
                       required
                       autofocus>
            </div>
            
            <div class="form-group">
                <label class="form-label" for="password">
                    <i class="fas fa-lock form-icon"></i>
                    Password
                </label>
                <input type="password" 
                       id="password" 
                       name="password" 
                       class="form-control" 
                       placeholder="Enter your password"
                       required>
            </div>
            
            <div class="remember-forgot">
                <label class="remember-me">
                    <input type="checkbox" name="remember">
                    Remember me
                </label>
                <a href="#" class="forgot-password">Forgot password?</a>
            </div>
            
            <button type="submit" class="btn-login" id="loginBtn">
                <i class="fas fa-sign-in-alt"></i>
                <span>Login</span>
            </button>
            
            <div class="divider">
                <span>OR</span>
            </div>
            
            <div class="social-login">
                <button type="button" class="social-btn" id="googleLogin">
                    <i class="fab fa-google"></i>
                    Google
                </button>
                <button type="button" class="social-btn" id="githubLogin">
                    <i class="fab fa-github"></i>
                    GitHub
                </button>
            </div>
        </form>
        
        <div class="login-footer">
            <p>Don't have an account? 
                <a href="{{ url_for('signup') }}">Sign up now</a>
            </p>
        </div>
    </div>
</div>

<!-- App Info Section -->
<div class="app-info">
    <h3>Track. Split. Save. 💰</h3>
    <p>Expensio helps you manage group expenses and personal budgets effortlessly.</p>
    
    <div class="features-list">
        <span class="feature-tag">Group Expense Splitting</span>
        <span class="feature-tag">Personal Budget Tracking</span>
        <span class="feature-tag">Visual Charts & Reports</span>
        <span class="feature-tag">CSV Export</span>
        <span class="feature-tag">Real-time Balances</span>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const loginForm = document.querySelector('.login-form');
        const loginBtn = document.getElementById('loginBtn');
        
        // Form validation
        loginForm.addEventListener('submit', function(e) {
            const username = document.getElementById('username').value.trim();
            const password = document.getElementById('password').value;
            
            if (!username || !password) {
                e.preventDefault();
                showError('Please fill in all fields');
                return;
            }
            
            // Show loading state
            loginBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Logging in...';
            loginBtn.disabled = true;
            loginBtn.classList.add('loading');
        });
        
        // Social login buttons
        document.getElementById('googleLogin').addEventListener('click', function() {
            alert('Google login will be implemented in future updates!');
        });
        
        document.getElementById('githubLogin').addEventListener('click', function() {
            alert('GitHub login will be implemented in future updates!');
        });
        
        // Forgot password
        document.querySelector('.forgot-password').addEventListener('click', function(e) {
            e.preventDefault();
            const email = prompt('Enter your email to reset password:');
            if (email) {
                alert(`Password reset link will be sent to ${email} (Demo)`);
            }
        });
        
        // Input focus effects
        const inputs = document.querySelectorAll('.form-control');
        inputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.style.transform = 'translateY(-2px)';
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.style.transform = 'translateY(0)';
            });
        });
        
        // Show error function
        function showError(message) {
            // Remove existing error
            const existingError = document.querySelector('.flash-error');
            if (existingError) existingError.remove();
            
            // Create error element
            const errorDiv = document.createElement('div');
            errorDiv.className = 'flash-message flash-error mb-3';
            errorDiv.innerHTML = `
                <i class="fas fa-exclamation-circle"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            // Insert after form header
            const form = document.querySelector('.login-form');
            form.insertBefore(errorDiv, form.firstChild);
            
            // Add close functionality
            errorDiv.querySelector('.flash-close').addEventListener('click', function() {
                errorDiv.style.opacity = '0';
                errorDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => errorDiv.remove(), 300);
            });
            
            // Auto remove after 5 seconds
            setTimeout(() => {
                if (errorDiv.parentElement) {
                    errorDiv.style.opacity = '0';
                    errorDiv.style.transform = 'translateY(-10px)';
                    setTimeout(() => errorDiv.remove(), 300);
                }
            }, 5000);
        }
        
        // Add floating animation to app info
        const appInfo = document.querySelector('.app-info');
        setTimeout(() => {
            appInfo.classList.add('floating');
        }, 1000);
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\auth\signup.html
=============================
{% extends "layout.html" %}

{% block title %}Sign Up - Expensio{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{{ url_for('static', filename='css/auth.css') }}">
<style>
    /* Signup Specific Styles */
    .signup-container {
        min-height: calc(100vh - 100px);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
    }
    
    .signup-box {
        width: 100%;
        max-width: 480px;
        animation: fadeInUp 0.6s ease;
    }
    
    .signup-header {
        text-align: center;
        margin-bottom: 2rem;
    }
    
    .signup-header h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .signup-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
    }
    
    .signup-form {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.5rem;
    }
    
    .form-group {
        position: relative;
    }
    
    .form-group.full-width {
        grid-column: 1 / -1;
    }
    
    .form-label {
        display: block;
        margin-bottom: 0.5rem;
        color: var(--text-light);
        font-weight: 500;
        font-size: 0.95rem;
    }
    
    .form-icon {
        position: absolute;
        left: 15px;
        top: 42px;
        color: var(--accent-purple);
        font-size: 1.1rem;
    }
    
    .form-control {
        width: 100%;
        padding: 12px 16px 12px 45px;
        background: rgba(255, 255, 255, 0.07);
        border: 1px solid var(--card-border);
        border-radius: 12px;
        color: var(--text-light);
        font-size: 1rem;
        transition: var(--transition);
    }
    
    .form-control:focus {
        outline: none;
        border-color: var(--accent-blue);
        box-shadow: 0 0 0 3px rgba(0, 212, 255, 0.1);
        background: rgba(255, 255, 255, 0.1);
    }
    
    .password-strength {
        margin-top: 0.5rem;
        font-size: 0.85rem;
    }
    
    .strength-bar {
        height: 4px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 2px;
        margin: 0.25rem 0;
        overflow: hidden;
    }
    
    .strength-fill {
        height: 100%;
        width: 0%;
        border-radius: 2px;
        transition: width 0.3s ease;
    }
    
    .strength-weak .strength-fill {
        background: #ff6b8b;
        width: 33%;
    }
    
    .strength-medium .strength-fill {
        background: #ffb74d;
        width: 66%;
    }
    
    .strength-strong .strength-fill {
        background: #00ff9d;
        width: 100%;
    }
    
    .strength-text {
        color: var(--text-muted);
        font-size: 0.8rem;
    }
    
    .terms-conditions {
        grid-column: 1 / -1;
        font-size: 0.9rem;
        color: var(--text-muted);
        line-height: 1.5;
    }
    
    .terms-conditions a {
        color: var(--accent-blue);
        text-decoration: none;
    }
    
    .terms-conditions a:hover {
        text-decoration: underline;
    }
    
    .btn-signup {
        grid-column: 1 / -1;
        width: 100%;
        padding: 14px;
        background: var(--secondary-gradient);
        color: white;
        border: none;
        border-radius: 12px;
        font-size: 1rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        cursor: pointer;
        transition: var(--transition);
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 0.5rem;
    }
    
    .btn-signup:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
    }
    
    .signup-footer {
        text-align: center;
        margin-top: 2rem;
        color: var(--text-muted);
    }
    
    .signup-footer a {
        color: var(--accent-blue);
        text-decoration: none;
        font-weight: 600;
    }
    
    .signup-footer a:hover {
        text-decoration: underline;
    }
    
    /* Password toggle */
    .password-toggle {
        position: absolute;
        right: 15px;
        top: 42px;
        background: none;
        border: none;
        color: var(--text-muted);
        cursor: pointer;
        font-size: 1rem;
        padding: 0;
    }
    
    .password-toggle:hover {
        color: var(--accent-blue);
    }
    
    /* Form validation styles */
    .form-control.valid {
        border-color: var(--success);
        background: rgba(0, 255, 157, 0.05);
    }
    
    .form-control.invalid {
        border-color: var(--danger);
        background: rgba(255, 107, 139, 0.05);
    }
    
    .validation-message {
        font-size: 0.8rem;
        margin-top: 0.25rem;
        display: flex;
        align-items: center;
        gap: 0.25rem;
    }
    
    .valid-message {
        color: var(--success);
    }
    
    .invalid-message {
        color: var(--danger);
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .signup-form {
            grid-template-columns: 1fr;
        }
        
        .form-group.full-width {
            grid-column: 1;
        }
        
        .terms-conditions {
            grid-column: 1;
        }
        
        .btn-signup {
            grid-column: 1;
        }
    }
    
    @media (max-width: 480px) {
        .signup-box {
            padding: 1.5rem;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="auth-container signup-container">
    <div class="glass-container auth-box signup-box">
        <!-- Header -->
        <div class="signup-header">
            <h1>Join Expensio</h1>
            <p>Start managing your finances smartly</p>
        </div>
        
        <!-- Flash Messages -->
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="flash-message flash-{{ category }} mb-3">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
        
        <!-- Signup Form -->
        <form method="POST" action="{{ url_for('signup') }}" class="signup-form" id="signupForm">
            <!-- Username -->
            <div class="form-group">
                <label class="form-label" for="username">
                    <i class="fas fa-user form-icon"></i>
                    Username
                </label>
                <input type="text" 
                       id="username" 
                       name="username" 
                       class="form-control" 
                       placeholder="Choose a username"
                       required
                       autocomplete="username"
                       minlength="3"
                       maxlength="20">
                <div class="validation-message" id="usernameValidation"></div>
            </div>
            
            <!-- Email -->
            <div class="form-group">
                <label class="form-label" for="email">
                    <i class="fas fa-envelope form-icon"></i>
                    Email
                </label>
                <input type="email" 
                       id="email" 
                       name="email" 
                       class="form-control" 
                       placeholder="your@email.com"
                       required
                       autocomplete="email">
                <div class="validation-message" id="emailValidation"></div>
            </div>
            
            <!-- Password -->
            <div class="form-group">
                <label class="form-label" for="password">
                    <i class="fas fa-lock form-icon"></i>
                    Password
                </label>
                <input type="password" 
                       id="password" 
                       name="password" 
                       class="form-control" 
                       placeholder="Create a password"
                       required
                       autocomplete="new-password"
                       minlength="6">
                <button type="button" class="password-toggle" id="togglePassword">
                    <i class="fas fa-eye"></i>
                </button>
                
                <div class="password-strength" id="passwordStrength">
                    <div class="strength-bar">
                        <div class="strength-fill"></div>
                    </div>
                    <div class="strength-text">Password strength</div>
                </div>
            </div>
            
            <!-- Confirm Password -->
            <div class="form-group">
                <label class="form-label" for="confirm_password">
                    <i class="fas fa-lock form-icon"></i>
                    Confirm Password
                </label>
                <input type="password" 
                       id="confirm_password" 
                       name="confirm_password" 
                       class="form-control" 
                       placeholder="Confirm your password"
                       required
                       autocomplete="new-password">
                <button type="button" class="password-toggle" id="toggleConfirmPassword">
                    <i class="fas fa-eye"></i>
                </button>
                <div class="validation-message" id="confirmValidation"></div>
            </div>
            
            <!-- Terms & Conditions -->
            <div class="terms-conditions">
                <label style="display: flex; align-items: flex-start; gap: 0.5rem; cursor: pointer;">
                    <input type="checkbox" name="terms" id="terms" required style="margin-top: 3px;">
                    <span>
                        I agree to the <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a>. 
                        I understand that Expensio will process my data in accordance with these policies.
                    </span>
                </label>
            </div>
            
            <!-- Submit Button -->
            <button type="submit" class="btn-signup" id="signupBtn">
                <i class="fas fa-user-plus"></i>
                <span>Create Account</span>
            </button>
        </form>
        
        <div class="signup-footer">
            <p>Already have an account? 
                <a href="{{ url_for('login') }}">Login here</a>
            </p>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        const signupForm = document.getElementById('signupForm');
        const signupBtn = document.getElementById('signupBtn');
        const passwordInput = document.getElementById('password');
        const confirmInput = document.getElementById('confirm_password');
        const togglePassword = document.getElementById('togglePassword');
        const toggleConfirmPassword = document.getElementById('toggleConfirmPassword');
        const passwordStrength = document.getElementById('passwordStrength');
        
        let isPasswordValid = false;
        let isConfirmValid = false;
        let isUsernameValid = false;
        let isEmailValid = false;
        let isTermsAccepted = false;
        
        // Password toggle functionality
        togglePassword.addEventListener('click', function() {
            const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
            passwordInput.setAttribute('type', type);
            this.innerHTML = type === 'password' ? '<i class="fas fa-eye"></i>' : '<i class="fas fa-eye-slash"></i>';
        });
        
        toggleConfirmPassword.addEventListener('click', function() {
            const type = confirmInput.getAttribute('type') === 'password' ? 'text' : 'password';
            confirmInput.setAttribute('type', type);
            this.innerHTML = type === 'password' ? '<i class="fas fa-eye"></i>' : '<i class="fas fa-eye-slash"></i>';
        });
        
        // Password strength checker
        passwordInput.addEventListener('input', function() {
            const password = this.value;
            let strength = 0;
            
            // Length check
            if (password.length >= 8) strength++;
            
            // Lowercase check
            if (/[a-z]/.test(password)) strength++;
            
            // Uppercase check
            if (/[A-Z]/.test(password)) strength++;
            
            // Number check
            if (/[0-9]/.test(password)) strength++;
            
            // Special character check
            if (/[^A-Za-z0-9]/.test(password)) strength++;
            
            // Update strength display
            passwordStrength.className = 'password-strength';
            const strengthText = passwordStrength.querySelector('.strength-text');
            
            if (password.length === 0) {
                strengthText.textContent = 'Password strength';
            } else if (strength <= 2) {
                passwordStrength.classList.add('strength-weak');
                strengthText.textContent = 'Weak password';
                strengthText.style.color = '#ff6b8b';
            } else if (strength <= 4) {
                passwordStrength.classList.add('strength-medium');
                strengthText.textContent = 'Medium strength';
                strengthText.style.color = '#ffb74d';
            } else {
                passwordStrength.classList.add('strength-strong');
                strengthText.textContent = 'Strong password';
                strengthText.style.color = '#00ff9d';
            }
            
            // Validate password
            validatePassword();
        });
        
        // Real-time validation
        document.getElementById('username').addEventListener('input', validateUsername);
        document.getElementById('email').addEventListener('input', validateEmail);
        confirmInput.addEventListener('input', validateConfirmPassword);
        document.getElementById('terms').addEventListener('change', validateTerms);
        
        // Validation functions
        function validateUsername() {
            const usernameInput = document.getElementById('username');
            const validationDiv = document.getElementById('usernameValidation');
            const username = usernameInput.value.trim();
            
            if (username.length < 3) {
                usernameInput.classList.remove('valid');
                usernameInput.classList.add('invalid');
                validationDiv.innerHTML = '<i class="fas fa-times-circle"></i> Username must be at least 3 characters';
                validationDiv.className = 'validation-message invalid-message';
                isUsernameValid = false;
            } else if (!/^[a-zA-Z0-9_]+$/.test(username)) {
                usernameInput.classList.remove('valid');
                usernameInput.classList.add('invalid');
                validationDiv.innerHTML = '<i class="fas fa-times-circle"></i> Only letters, numbers, and underscores allowed';
                validationDiv.className = 'validation-message invalid-message';
                isUsernameValid = false;
            } else {
                usernameInput.classList.remove('invalid');
                usernameInput.classList.add('valid');
                validationDiv.innerHTML = '<i class="fas fa-check-circle"></i> Username available';
                validationDiv.className = 'validation-message valid-message';
                isUsernameValid = true;
            }
            
            updateSubmitButton();
        }
        
        function validateEmail() {
            const emailInput = document.getElementById('email');
            const validationDiv = document.getElementById('emailValidation');
            const email = emailInput.value.trim();
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            
            if (!emailRegex.test(email)) {
                emailInput.classList.remove('valid');
                emailInput.classList.add('invalid');
                validationDiv.innerHTML = '<i class="fas fa-times-circle"></i> Please enter a valid email address';
                validationDiv.className = 'validation-message invalid-message';
                isEmailValid = false;
            } else {
                emailInput.classList.remove('invalid');
                emailInput.classList.add('valid');
                validationDiv.innerHTML = '<i class="fas fa-check-circle"></i> Valid email format';
                validationDiv.className = 'validation-message valid-message';
                isEmailValid = true;
            }
            
            updateSubmitButton();
        }
        
        function validatePassword() {
            const password = passwordInput.value;
            isPasswordValid = password.length >= 6;
            updateSubmitButton();
            
            // Also validate confirm password if it has value
            if (confirmInput.value) {
                validateConfirmPassword();
            }
        }
        
        function validateConfirmPassword() {
            const validationDiv = document.getElementById('confirmValidation');
            const password = passwordInput.value;
            const confirm = confirmInput.value;
            
            if (confirm === '') {
                confirmInput.classList.remove('valid', 'invalid');
                validationDiv.innerHTML = '';
                isConfirmValid = false;
            } else if (password === confirm) {
                confirmInput.classList.remove('invalid');
                confirmInput.classList.add('valid');
                validationDiv.innerHTML = '<i class="fas fa-check-circle"></i> Passwords match';
                validationDiv.className = 'validation-message valid-message';
                isConfirmValid = true;
            } else {
                confirmInput.classList.remove('valid');
                confirmInput.classList.add('invalid');
                validationDiv.innerHTML = '<i class="fas fa-times-circle"></i> Passwords do not match';
                validationDiv.className = 'validation-message invalid-message';
                isConfirmValid = false;
            }
            
            updateSubmitButton();
        }
        
        function validateTerms() {
            const termsCheckbox = document.getElementById('terms');
            isTermsAccepted = termsCheckbox.checked;
            updateSubmitButton();
        }
        
        function updateSubmitButton() {
            const isValid = isUsernameValid && isEmailValid && isPasswordValid && isConfirmValid && isTermsAccepted;
            
            if (isValid) {
                signupBtn.disabled = false;
                signupBtn.style.opacity = '1';
                signupBtn.style.cursor = 'pointer';
            } else {
                signupBtn.disabled = true;
                signupBtn.style.opacity = '0.7';
                signupBtn.style.cursor = 'not-allowed';
            }
        }
        
        // Form submission
        signupForm.addEventListener('submit', function(e) {
            // Final validation
            validateUsername();
            validateEmail();
            validatePassword();
            validateConfirmPassword();
            validateTerms();
            
            if (!isUsernameValid || !isEmailValid || !isPasswordValid || !isConfirmValid || !isTermsAccepted) {
                e.preventDefault();
                showError('Please fix all validation errors before submitting.');
                return;
            }
            
            // Show loading state
            signupBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Creating account...';
            signupBtn.disabled = true;
        });
        
        // Show error function
        function showError(message) {
            // Remove existing error
            const existingError = document.querySelector('.flash-error');
            if (existingError) existingError.remove();
            
            // Create error element
            const errorDiv = document.createElement('div');
            errorDiv.className = 'flash-message flash-error mb-3';
            errorDiv.innerHTML = `
                <i class="fas fa-exclamation-circle"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            // Insert after form header
            const form = document.querySelector('.signup-form');
            const header = document.querySelector('.signup-header');
            header.parentNode.insertBefore(errorDiv, form);
            
            // Add close functionality
            errorDiv.querySelector('.flash-close').addEventListener('click', function() {
                errorDiv.style.opacity = '0';
                errorDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => errorDiv.remove(), 300);
            });
            
            // Auto remove after 5 seconds
            setTimeout(() => {
                if (errorDiv.parentElement) {
                    errorDiv.style.opacity = '0';
                    errorDiv.style.transform = 'translateY(-10px)';
                    setTimeout(() => errorDiv.remove(), 300);
                }
            }, 5000);
        }
        
        // Initialize validation
        validateUsername();
        validateEmail();
        validatePassword();
        validateTerms();
        
        // Add some visual effects
        const inputs = document.querySelectorAll('.form-control');
        inputs.forEach((input, index) => {
            input.style.animationDelay = `${index * 0.1}s`;
            input.style.animation = 'fadeInUp 0.5s ease forwards';
            input.style.opacity = '0';
            
            setTimeout(() => {
                input.style.opacity = '1';
            }, 300 + (index * 100));
        });
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\expenses\add.html
=============================
{% extends "layout.html" %}

{% block title %}Add Expense - Expensio{% endblock %}

{% block extra_css %}
<style>
    /* Add Expense Specific Styles */
    .add-expense-container {
        max-width: 800px;
        margin: 2rem auto;
        padding: 0 1.5rem;
        animation: fadeInUp 0.5s ease;
    }
    
    .page-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    
    .page-header h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .page-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
        max-width: 600px;
        margin: 0 auto;
    }
    
    .room-info-card {
        background: rgba(138, 43, 226, 0.1);
        border-radius: 15px;
        padding: 1.5rem;
        margin-bottom: 2rem;
        border: 1px solid var(--card-border);
        display: flex;
        align-items: center;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 1rem;
    }
    
    .room-info-left {
        display: flex;
        align-items: center;
        gap: 1rem;
    }
    
    .room-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }
    
    .room-details h3 {
        margin: 0 0 0.25rem 0;
        color: var(--accent-blue);
    }
    
    .room-details p {
        margin: 0;
        color: var(--text-muted);
        font-size: 0.95rem;
    }
    
    .room-code {
        background: rgba(138, 43, 226, 0.2);
        padding: 6px 16px;
        border-radius: 20px;
        font-family: monospace;
        font-size: 1rem;
        color: var(--accent-purple);
        font-weight: 600;
    }
    
    /* Expense Form */
    .expense-form {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2.5rem;
        border: 1px solid var(--card-border);
        box-shadow: var(--shadow);
    }
    
    /* Form Sections */
    .form-section {
        margin-bottom: 2.5rem;
        padding-bottom: 2rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .form-section:last-child {
        border-bottom: none;
        margin-bottom: 0;
        padding-bottom: 0;
    }
    
    .section-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }
    
    .section-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }
    
    .section-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-blue);
    }
    
    .section-description {
        color: var(--text-muted);
        margin-bottom: 1.5rem;
        font-size: 0.95rem;
        line-height: 1.6;
    }
    
    /* Form Grid */
    .form-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.5rem;
    }
    
    @media (max-width: 768px) {
        .form-grid {
            grid-template-columns: 1fr;
        }
    }
    
    /* Input Groups */
    .input-group {
        position: relative;
    }
    
    .input-group label {
        display: block;
        margin-bottom: 0.5rem;
        color: var(--text-light);
        font-weight: 500;
        font-size: 0.95rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .input-group .required {
        color: var(--danger);
        font-size: 0.8em;
    }
    
    .input-group .form-control {
        width: 100%;
        padding: 12px 16px;
        background: rgba(255, 255, 255, 0.07);
        border: 1px solid var(--card-border);
        border-radius: 12px;
        color: var(--text-light);
        font-size: 1rem;
        transition: var(--transition);
    }
    
    .input-group .form-control:focus {
        outline: none;
        border-color: var(--accent-blue);
        box-shadow: 0 0 0 3px rgba(0, 212, 255, 0.1);
        background: rgba(255, 255, 255, 0.1);
    }
    
    .input-group textarea.form-control {
        min-height: 100px;
        resize: vertical;
        font-family: inherit;
        line-height: 1.5;
    }
    
    /* Amount Input */
    .amount-input {
        position: relative;
    }
    
    .amount-input .currency {
        position: absolute;
        left: 12px;
        top: 50%;
        transform: translateY(-50%);
        color: var(--accent-purple);
        font-weight: 600;
        font-size: 1.1rem;
    }
    
    .amount-input .form-control {
        padding-left: 40px;
        font-size: 1.2rem;
        font-weight: 500;
    }
    
    /* Category Selection */
    .category-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
        gap: 1rem;
        margin-top: 1rem;
    }
    
    .category-option {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 1.5rem 1rem;
        border-radius: 12px;
        background: rgba(255, 255, 255, 0.05);
        border: 2px solid transparent;
        cursor: pointer;
        transition: var(--transition);
        text-align: center;
    }
    
    .category-option:hover {
        background: rgba(138, 43, 226, 0.1);
        transform: translateY(-3px);
    }
    
    .category-option.selected {
        border-color: var(--accent-purple);
        background: rgba(138, 43, 226, 0.15);
    }
    
    .category-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        margin-bottom: 0.75rem;
    }
    
    .category-food .category-icon {
        background: rgba(0, 255, 157, 0.1);
        color: var(--success);
    }
    
    .category-travel .category-icon {
        background: rgba(0, 212, 255, 0.1);
        color: var(--accent-blue);
    }
    
    .category-shopping .category-icon {
        background: rgba(138, 43, 226, 0.1);
        color: var(--accent-purple);
    }
    
    .category-entertainment .category-icon {
        background: rgba(255, 183, 77, 0.1);
        color: var(--warning);
    }
    
    .category-other .category-icon {
        background: rgba(255, 107, 139, 0.1);
        color: var(--danger);
    }
    
    .category-name {
        font-weight: 500;
        font-size: 0.95rem;
        margin-bottom: 0.25rem;
    }
    
    /* Date Input */
    .date-input {
        position: relative;
    }
    
    .date-input .calendar-icon {
        position: absolute;
        right: 12px;
        top: 50%;
        transform: translateY(-50%);
        color: var(--accent-purple);
        pointer-events: none;
    }
    
    /* Split Options */
    .split-options {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        margin-top: 1rem;
    }
    
    .split-option {
        display: flex;
        align-items: center;
        padding: 1.5rem;
        border-radius: 12px;
        background: rgba(255, 255, 255, 0.05);
        border: 2px solid transparent;
        cursor: pointer;
        transition: var(--transition);
    }
    
    .split-option:hover {
        background: rgba(138, 43, 226, 0.1);
    }
    
    .split-option.selected {
        border-color: var(--accent-purple);
        background: rgba(138, 43, 226, 0.15);
    }
    
    .split-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        margin-right: 1rem;
        flex-shrink: 0;
    }
    
    .split-details {
        flex: 1;
    }
    
    .split-title {
        font-weight: 600;
        margin-bottom: 0.25rem;
        color: var(--text-light);
    }
    
    .split-description {
        font-size: 0.9rem;
        color: var(--text-muted);
        line-height: 1.4;
    }
    
    /* Receipt Upload */
    .receipt-upload {
        border: 2px dashed var(--card-border);
        border-radius: 15px;
        padding: 3rem 2rem;
        text-align: center;
        cursor: pointer;
        transition: var(--transition);
        margin-top: 1rem;
    }
    
    .receipt-upload:hover {
        border-color: var(--accent-purple);
        background: rgba(138, 43, 226, 0.05);
    }
    
    .receipt-upload i {
        font-size: 3rem;
        color: var(--accent-purple);
        margin-bottom: 1rem;
    }
    
    .receipt-upload h4 {
        margin-bottom: 0.5rem;
        color: var(--text-light);
    }
    
    .receipt-upload p {
        color: var(--text-muted);
        margin-bottom: 1rem;
    }
    
    .file-info {
        display: none;
        margin-top: 1rem;
        padding: 1rem;
        background: rgba(0, 212, 255, 0.1);
        border-radius: 10px;
        border: 1px solid rgba(0, 212, 255, 0.2);
    }
    
    .file-info.show {
        display: block;
    }
    
    /* Expense Preview */
    .expense-preview {
        background: rgba(0, 212, 255, 0.05);
        border-radius: 15px;
        padding: 2rem;
        margin-top: 2rem;
        border: 1px solid rgba(0, 212, 255, 0.2);
    }
    
    .preview-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }
    
    .preview-header i {
        color: var(--accent-blue);
        font-size: 1.5rem;
    }
    
    .preview-header h3 {
        margin: 0;
        color: var(--accent-blue);
    }
    
    .preview-content {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
    }
    
    .preview-item {
        text-align: center;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
    }
    
    .preview-label {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin-bottom: 0.5rem;
    }
    
    .preview-value {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--text-light);
    }
    
    /* Form Actions */
    .form-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 3rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        flex-wrap: wrap;
        gap: 1rem;
    }
    
    .form-actions .btn {
        min-width: 150px;
    }
    
    /* Quick Tips */
    .quick-tips {
        background: rgba(255, 183, 77, 0.05);
        border-radius: 15px;
        padding: 1.5rem;
        margin-top: 2rem;
        border: 1px solid rgba(255, 183, 77, 0.2);
    }
    
    .tips-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1rem;
        color: var(--warning);
    }
    
    .tips-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    
    .tips-list li {
        padding: 0.5rem 0;
        color: var(--text-muted);
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
    }
    
    .tips-list li:before {
        content: '💡';
        flex-shrink: 0;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .add-expense-container {
            padding: 0 1rem;
        }
        
        .expense-form {
            padding: 1.5rem;
        }
        
        .room-info-card {
            flex-direction: column;
            align-items: flex-start;
            text-align: left;
        }
        
        .form-actions {
            flex-direction: column;
        }
        
        .form-actions .btn {
            width: 100%;
        }
        
        .category-grid {
            grid-template-columns: repeat(2, 1fr);
        }
        
        .split-options {
            grid-template-columns: 1fr;
        }
    }
    
    @media (max-width: 480px) {
        .category-grid {
            grid-template-columns: 1fr;
        }
        
        .page-header h1 {
            font-size: 2rem;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="add-expense-container">
    <!-- Page Header -->
    <div class="page-header">
        <h1>Add New Expense</h1>
        <p>Track a new expense for your room. Fill in the details below to split costs fairly among members.</p>
    </div>
    
    <!-- Room Info Card -->
    <div class="room-info-card">
        <div class="room-info-left">
            <div class="room-icon">
                <i class="fas fa-door-open"></i>
            </div>
            <div class="room-details">
                <h3>{{ room.name }}</h3>
                <p>Add expense to this room</p>
            </div>
        </div>
        <div class="room-code">
            <i class="fas fa-key"></i> {{ room.room_code }}
        </div>
    </div>
    
    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                <div class="flash-message flash-{{ category }} mb-4">
                    <i class="fas fa-{% if category == 'success' %}check-circle{% elif category == 'error' %}exclamation-circle{% else %}info-circle{% endif %}"></i>
                    {{ message }}
                    <button class="flash-close">&times;</button>
                </div>
            {% endfor %}
        {% endif %}
    {% endwith %}
    
    <!-- Expense Form -->
    <form method="POST" action="{{ url_for('add_expense', room_id=room.id) }}" class="expense-form" id="expenseForm" enctype="multipart/form-data">
        <!-- Expense Details Section -->
        <div class="form-section">
            <div class="section-header">
                <div class="section-icon">
                    <i class="fas fa-receipt"></i>
                </div>
                <h2 class="section-title">Expense Details</h2>
            </div>
            
            <p class="section-description">
                Enter the basic information about your expense. Be descriptive so everyone understands what it's for.
            </p>
            
            <div class="form-grid">
                <!-- Description -->
                <div class="input-group">
                    <label for="description">
                        <i class="fas fa-align-left"></i> Description <span class="required">*</span>
                    </label>
                    <input type="text" 
                           id="description" 
                           name="description" 
                           class="form-control" 
                           placeholder="e.g., Dinner at restaurant, Movie tickets, Groceries"
                           required
                           maxlength="200"
                           autofocus>
                    <div class="char-counter" style="text-align: right; font-size: 0.85rem; color: var(--text-muted); margin-top: 0.25rem;">
                        <span id="descCounter">0</span>/200
                    </div>
                </div>
                
                <!-- Amount -->
                <div class="input-group">
                    <label for="amount">
                        <i class="fas fa-rupee-sign"></i> Amount <span class="required">*</span>
                    </label>
                    <div class="amount-input">
                        <span class="currency">₹</span>
                        <input type="number" 
                               id="amount" 
                               name="amount" 
                               class="form-control" 
                               placeholder="0.00"
                               required
                               min="0.01"
                               step="0.01"
                               value="0.00">
                    </div>
                </div>
            </div>
            
            <!-- Category Selection -->
            <div class="input-group">
                <label>
                    <i class="fas fa-tags"></i> Category <span class="required">*</span>
                </label>
                <div class="category-grid" id="categoryGrid">
                    <div class="category-option category-food" data-category="Food">
                        <div class="category-icon">
                            <i class="fas fa-utensils"></i>
                        </div>
                        <div class="category-name">Food</div>
                        <div class="category-examples" style="font-size: 0.8rem; color: var(--text-muted);">
                            Restaurants, Groceries
                        </div>
                    </div>
                    
                    <div class="category-option category-travel" data-category="Travel">
                        <div class="category-icon">
                            <i class="fas fa-car"></i>
                        </div>
                        <div class="category-name">Travel</div>
                        <div class="category-examples" style="font-size: 0.8rem; color: var(--text-muted);">
                            Transport, Fuel, Flights
                        </div>
                    </div>
                    
                    <div class="category-option category-shopping" data-category="Shopping">
                        <div class="category-icon">
                            <i class="fas fa-shopping-bag"></i>
                        </div>
                        <div class="category-name">Shopping</div>
                        <div class="category-examples" style="font-size: 0.8rem; color: var(--text-muted);">
                            Clothes, Electronics
                        </div>
                    </div>
                    
                    <div class="category-option category-entertainment" data-category="Entertainment">
                        <div class="category-icon">
                            <i class="fas fa-film"></i>
                        </div>
                        <div class="category-name">Entertainment</div>
                        <div class="category-examples" style="font-size: 0.8rem; color: var(--text-muted);">
                            Movies, Concerts, Games
                        </div>
                    </div>
                    
                    <div class="category-option category-other selected" data-category="Other">
                        <div class="category-icon">
                            <i class="fas fa-receipt"></i>
                        </div>
                        <div class="category-name">Other</div>
                        <div class="category-examples" style="font-size: 0.8rem; color: var(--text-muted);">
                            Miscellaneous
                        </div>
                    </div>
                </div>
                <input type="hidden" id="category" name="category" value="Other">
            </div>
            
            <!-- Notes -->
            <div class="input-group">
                <label for="notes">
                    <i class="fas fa-sticky-note"></i> Additional Notes (Optional)
                </label>
                <textarea id="notes" 
                          name="notes" 
                          class="form-control" 
                          placeholder="Add any extra details about this expense..."
                          maxlength="500"></textarea>
                <div class="char-counter" style="text-align: right; font-size: 0.85rem; color: var(--text-muted); margin-top: 0.25rem;">
                    <span id="notesCounter">0</span>/500
                </div>
            </div>
        </div>
        
        <!-- Split & Payment Section -->
        <div class="form-section">
            <div class="section-header">
                <div class="section-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h2 class="section-title">Split & Payment</h2>
            </div>
            
            <p class="section-description">
                Specify how this expense should be split among room members.
            </p>
            
            <!-- Split Options -->
            <div class="input-group">
                <label>
                    <i class="fas fa-share-alt"></i> Split Method <span class="required">*</span>
                </label>
                <div class="split-options" id="splitOptions">
                    <div class="split-option selected" data-split="equal">
                        <div class="split-icon">
                            <i class="fas fa-balance-scale"></i>
                        </div>
                        <div class="split-details">
                            <div class="split-title">Equal Split</div>
                            <div class="split-description">
                                Divide equally among all room members
                            </div>
                        </div>
                    </div>
                    
                    <div class="split-option" data-split="percentage">
                        <div class="split-icon">
                            <i class="fas fa-percentage"></i>
                        </div>
                        <div class="split-details">
                            <div class="split-title">Percentage Split</div>
                            <div class="split-description">
                                Custom percentages for each member
                            </div>
                        </div>
                    </div>
                    
                    <div class="split-option" data-split="custom">
                        <div class="split-icon">
                            <i class="fas fa-edit"></i>
                        </div>
                        <div class="split-details">
                            <div class="split-title">Custom Amounts</div>
                            <div class="split-description">
                                Specify exact amounts for each member
                            </div>
                        </div>
                    </div>
                </div>
                <input type="hidden" id="splitMethod" name="split_method" value="equal">
            </div>
            
            <!-- Paid By -->
            <div class="form-grid">
                <div class="input-group">
                    <label for="paidBy">
                        <i class="fas fa-credit-card"></i> Paid By <span class="required">*</span>
                    </label>
                    <select id="paidBy" name="paid_by" class="form-control" required>
                        <option value="{{ current_user.id }}" selected>You ({{ current_user.username }})</option>
                        <!-- Other members would be populated dynamically -->
                        <option value="other1">Alex Johnson</option>
                        <option value="other2">Sarah Williams</option>
                    </select>
                </div>
                
                <!-- Date -->
                <div class="input-group">
                    <label for="date">
                        <i class="fas fa-calendar"></i> Date <span class="required">*</span>
                    </label>
                    <div class="date-input">
                        <input type="date" 
                               id="date" 
                               name="date" 
                               class="form-control" 
                               value="{{ now.strftime('%Y-%m-%d') }}"
                               required>
                        <i class="fas fa-calendar-alt calendar-icon"></i>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Receipt Upload (Optional) -->
        <div class="form-section">
            <div class="section-header">
                <div class="section-icon">
                    <i class="fas fa-paperclip"></i>
                </div>
                <h2 class="section-title">Receipt (Optional)</h2>
            </div>
            
            <p class="section-description">
                Upload a photo or scan of your receipt for record keeping.
            </p>
            
            <div class="receipt-upload" id="receiptUpload">
                <i class="fas fa-cloud-upload-alt"></i>
                <h4>Upload Receipt</h4>
                <p>Drag & drop or click to browse</p>
                <div class="btn btn-outline">
                    <i class="fas fa-folder-open"></i> Choose File
                </div>
                <input type="file" 
                       id="receipt" 
                       name="receipt" 
                       accept="image/*,.pdf"
                       style="display: none;">
                
                <div class="file-info" id="fileInfo">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <i class="fas fa-file-invoice" style="font-size: 2rem; color: var(--accent-blue);"></i>
                        <div style="flex: 1;">
                            <div style="font-weight: 600; margin-bottom: 0.25rem;" id="fileName">receipt.jpg</div>
                            <div style="font-size: 0.9rem; color: var(--text-muted);" id="fileSize">2.5 MB</div>
                        </div>
                        <button type="button" class="action-icon" id="removeFile" style="background: rgba(255, 107, 139, 0.1); color: var(--danger);">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Quick Tips -->
        <div class="quick-tips">
            <div class="tips-header">
                <i class="fas fa-lightbulb"></i>
                <h3 style="margin: 0; font-size: 1.1rem;">Quick Tips</h3>
            </div>
            <ul class="tips-list">
                <li>Be specific in your description for better tracking</li>
                <li>Upload receipts to avoid confusion later</li>
                <li>Split equally for shared expenses like dinners</li>
                <li>Use custom split for personal items within group orders</li>
                <li>Review the preview before submitting</li>
            </ul>
        </div>
        
        <!-- Expense Preview -->
        <div class="expense-preview" id="expensePreview">
            <div class="preview-header">
                <i class="fas fa-eye"></i>
                <h3>Expense Preview</h3>
            </div>
            <div class="preview-content">
                <div class="preview-item">
                    <div class="preview-label">Amount Per Person</div>
                    <div class="preview-value" id="previewPerPerson">₹0.00</div>
                </div>
                <div class="preview-item">
                    <div class="preview-label">Category</div>
                    <div class="preview-value" id="previewCategory">Other</div>
                </div>
                <div class="preview-item">
                    <div class="preview-label">Split Method</div>
                    <div class="preview-value" id="previewSplit">Equal</div>
                </div>
                <div class="preview-item">
                    <div class="preview-label">Total Members</div>
                    <div class="preview-value">4</div>
                </div>
            </div>
        </div>
        
        <!-- Form Actions -->
        <div class="form-actions">
            <a href="{{ url_for('view_room', room_id=room.id) }}" class="btn btn-outline">
                <i class="fas fa-arrow-left"></i> Back to Room
            </a>
            <div style="display: flex; gap: 1rem;">
                <button type="button" class="btn btn-outline" id="saveDraftBtn">
                    <i class="fas fa-save"></i> Save Draft
                </button>
                <button type="submit" class="btn btn-primary" id="submitExpenseBtn">
                    <i class="fas fa-check-circle"></i> Add Expense
                </button>
            </div>
        </div>
    </form>
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Elements
        const expenseForm = document.getElementById('expenseForm');
        const descriptionInput = document.getElementById('description');
        const amountInput = document.getElementById('amount');
        const categoryGrid = document.getElementById('categoryGrid');
        const categoryInput = document.getElementById('category');
        const notesInput = document.getElementById('notes');
        const splitOptions = document.getElementById('splitOptions');
        const splitMethodInput = document.getElementById('splitMethod');
        const receiptUpload = document.getElementById('receiptUpload');
        const receiptInput = document.getElementById('receipt');
        const fileInfo = document.getElementById('fileInfo');
        const removeFileBtn = document.getElementById('removeFile');
        const submitExpenseBtn = document.getElementById('submitExpenseBtn');
        const saveDraftBtn = document.getElementById('saveDraftBtn');
        
        // Preview elements
        const previewPerPerson = document.getElementById('previewPerPerson');
        const previewCategory = document.getElementById('previewCategory');
        const previewSplit = document.getElementById('previewSplit');
        
        // Character counters
        const descCounter = document.getElementById('descCounter');
        const notesCounter = document.getElementById('notesCounter');
        
        // Category selection
        categoryGrid.querySelectorAll('.category-option').forEach(option => {
            option.addEventListener('click', function() {
                // Remove selected class from all options
                categoryGrid.querySelectorAll('.category-option').forEach(opt => {
                    opt.classList.remove('selected');
                });
                
                // Add selected class to clicked option
                this.classList.add('selected');
                
                // Update hidden input
                const category = this.getAttribute('data-category');
                categoryInput.value = category;
                previewCategory.textContent = category;
                
                // Add animation
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 200);
            });
        });
        
        // Split method selection
        splitOptions.querySelectorAll('.split-option').forEach(option => {
            option.addEventListener('click', function() {
                // Remove selected class from all options
                splitOptions.querySelectorAll('.split-option').forEach(opt => {
                    opt.classList.remove('selected');
                });
                
                // Add selected class to clicked option
                this.classList.add('selected');
                
                // Update hidden input
                const splitMethod = this.getAttribute('data-split');
                splitMethodInput.value = splitMethod;
                
                // Update preview
                previewSplit.textContent = splitMethod.charAt(0).toUpperCase() + splitMethod.slice(1);
                
                // Add animation
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 200);
            });
        });
        
        // Receipt upload
        receiptUpload.addEventListener('click', function() {
            receiptInput.click();
        });
        
        receiptInput.addEventListener('change', function() {
            if (this.files.length > 0) {
                const file = this.files[0];
                const fileName = file.name;
                const fileSize = (file.size / (1024 * 1024)).toFixed(2) + ' MB';
                
                // Update file info
                document.getElementById('fileName').textContent = fileName;
                document.getElementById('fileSize').textContent = fileSize;
                
                // Show file info
                fileInfo.classList.add('show');
                
                // Change upload area style
                receiptUpload.style.borderStyle = 'solid';
                receiptUpload.style.borderColor = 'var(--accent-blue)';
                receiptUpload.style.background = 'rgba(0, 212, 255, 0.05)';
                
                // Update icon and text
                const icon = receiptUpload.querySelector('i');
                const title = receiptUpload.querySelector('h4');
                const text = receiptUpload.querySelector('p');
                const btn = receiptUpload.querySelector('.btn');
                
                icon.className = 'fas fa-file-invoice';
                icon.style.color = 'var(--accent-blue)';
                title.textContent = 'Receipt Uploaded';
                text.textContent = 'Click to change file';
                btn.innerHTML = '<i class="fas fa-sync-alt"></i> Change File';
            }
        });
        
        // Remove file
        removeFileBtn.addEventListener('click', function(e) {
            e.stopPropagation();
            receiptInput.value = '';
            fileInfo.classList.remove('show');
            
            // Reset upload area
            receiptUpload.style.borderStyle = 'dashed';
            receiptUpload.style.borderColor = '';
            receiptUpload.style.background = '';
            
            const icon = receiptUpload.querySelector('i');
            const title = receiptUpload.querySelector('h4');
            const text = receiptUpload.querySelector('p');
            const btn = receiptUpload.querySelector('.btn');
            
            icon.className = 'fas fa-cloud-upload-alt';
            icon.style.color = '';
            title.textContent = 'Upload Receipt';
            text.textContent = 'Drag & drop or click to browse';
            btn.innerHTML = '<i class="fas fa-folder-open"></i> Choose File';
        });
        
        // Update character counters
        function updateCharacterCounters() {
            descCounter.textContent = descriptionInput.value.length;
            notesCounter.textContent = notesInput.value.length;
        }
        
        // Update preview in real-time
        function updatePreview() {
            const amount = parseFloat(amountInput.value) || 0;
            const members = 4; // This would come from backend in real app
            
            // Calculate per person amount
            let perPerson = amount / members;
            if (isNaN(perPerson)) perPerson = 0;
            
            previewPerPerson.textContent = `₹${perPerson.toFixed(2)}`;
            
            // Update category preview
            previewCategory.textContent = categoryInput.value;
            
            // Update split preview
            const splitMethod = splitMethodInput.value;
            previewSplit.textContent = splitMethod.charAt(0).toUpperCase() + splitMethod.slice(1);
        }
        
        // Format amount input
        amountInput.addEventListener('blur', function() {
            const value = parseFloat(this.value);
            if (!isNaN(value) && value >= 0) {
                this.value = value.toFixed(2);
            } else {
                this.value = '0.00';
            }
            updatePreview();
        });
        
        // Event listeners for real-time updates
        descriptionInput.addEventListener('input', updateCharacterCounters);
        amountInput.addEventListener('input', updatePreview);
        notesInput.addEventListener('input', updateCharacterCounters);
        
        // Set today's date as default
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('date').value = today;
        
        // Save draft functionality
        saveDraftBtn.addEventListener('click', function() {
            const expenseData = {
                description: descriptionInput.value,
                amount: amountInput.value,
                category: categoryInput.value,
                notes: notesInput.value,
                splitMethod: splitMethodInput.value,
                date: document.getElementById('date').value
            };
            
            localStorage.setItem('expensio_draft', JSON.stringify(expenseData));
            
            showFlash('Expense saved as draft!', 'success');
            
            // Animate button
            this.innerHTML = '<i class="fas fa-check"></i> Saved!';
            this.style.background = 'var(--success)';
            this.style.color = 'white';
            this.style.borderColor = 'var(--success)';
            
            setTimeout(() => {
                this.innerHTML = '<i class="fas fa-save"></i> Save Draft';
                this.style.background = '';
                this.style.color = '';
                this.style.borderColor = '';
            }, 2000);
        });
        
        // Load draft if exists
        const savedDraft = localStorage.getItem('expensio_draft');
        if (savedDraft) {
            try {
                const draft = JSON.parse(savedDraft);
                descriptionInput.value = draft.description || '';
                amountInput.value = draft.amount || '0.00';
                categoryInput.value = draft.category || 'Other';
                notesInput.value = draft.notes || '';
                splitMethodInput.value = draft.splitMethod || 'equal';
                if (draft.date) document.getElementById('date').value = draft.date;
                
                // Update UI to reflect loaded values
                categoryGrid.querySelectorAll('.category-option').forEach(opt => {
                    opt.classList.toggle('selected', opt.getAttribute('data-category') === draft.category);
                });
                
                splitOptions.querySelectorAll('.split-option').forEach(opt => {
                    opt.classList.toggle('selected', opt.getAttribute('data-split') === draft.splitMethod);
                });
                
                updateCharacterCounters();
                updatePreview();
                
                showFlash('Draft expense loaded!', 'info');
            } catch (e) {
                console.error('Error loading draft:', e);
            }
        }
        
        // Form validation
        expenseForm.addEventListener('submit', function(e) {
            const description = descriptionInput.value.trim();
            const amount = parseFloat(amountInput.value);
            
            if (!description) {
                e.preventDefault();
                showError('Please enter an expense description');
                descriptionInput.focus();
                return;
            }
            
            if (!amount || amount <= 0) {
                e.preventDefault();
                showError('Please enter a valid amount greater than 0');
                amountInput.focus();
                return;
            }
            
            // Clear draft
            localStorage.removeItem('expensio_draft');
            
            // Show loading state
            submitExpenseBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Adding Expense...';
            submitExpenseBtn.disabled = true;
        });
        
        // Initialize
        updateCharacterCounters();
        updatePreview();
        
        // Add some visual effects
        const formSections = document.querySelectorAll('.form-section');
        formSections.forEach((section, index) => {
            section.style.animationDelay = `${index * 0.1}s`;
            section.style.animation = 'fadeInUp 0.5s ease forwards';
            section.style.opacity = '0';
            
            setTimeout(() => {
                section.style.opacity = '1';
            }, 300 + (index * 100));
        });
        
        // Show error function
        function showError(message) {
            // Remove existing error
            const existingError = document.querySelector('.flash-error');
            if (existingError) existingError.remove();
            
            // Create error element
            const errorDiv = document.createElement('div');
            errorDiv.className = 'flash-message flash-error mb-4';
            errorDiv.innerHTML = `
                <i class="fas fa-exclamation-circle"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            // Insert after room info card
            const roomCard = document.querySelector('.room-info-card');
            roomCard.parentNode.insertBefore(errorDiv, roomCard.nextSibling);
            
            // Add close functionality
            errorDiv.querySelector('.flash-close').addEventListener('click', function() {
                errorDiv.style.opacity = '0';
                errorDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => errorDiv.remove(), 300);
            });
            
            // Auto remove after 5 seconds
            setTimeout(() => {
                if (errorDiv.parentElement) {
                    errorDiv.style.opacity = '0';
                    errorDiv.style.transform = 'translateY(-10px)';
                    setTimeout(() => errorDiv.remove(), 300);
                }
            }, 5000);
            
            // Scroll to error
            errorDiv.scrollIntoView({ behavior: 'smooth', block: 'center' });
        }
        
        // Show flash message
        function showFlash(message, type = 'info') {
            // Remove existing flash
            const existingFlash = document.querySelector('.flash-message');
            if (existingFlash && existingFlash.className.includes('flash-error')) {
                existingFlash.remove();
            }
            
            const flashDiv = document.createElement('div');
            flashDiv.className = `flash-message flash-${type} mb-4`;
            flashDiv.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : 'info-circle'}"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            const roomCard = document.querySelector('.room-info-card');
            roomCard.parentNode.insertBefore(flashDiv, roomCard.nextSibling);
            
            flashDiv.querySelector('.flash-close').addEventListener('click', function() {
                flashDiv.style.opacity = '0';
                flashDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => flashDiv.remove(), 300);
            });
            
            setTimeout(() => {
                if (flashDiv.parentElement) {
                    flashDiv.style.opacity = '0';
                    flashDiv.style.transform = 'translateY(-10px)';
                    setTimeout(() => flashDiv.remove(), 300);
                }
            }, 5000);
        }
        
        // Auto-calculate and update preview when amount changes
        amountInput.addEventListener('input', function() {
            setTimeout(updatePreview, 100);
        });
        
        // Quick category shortcuts
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey) {
                switch(e.key) {
                    case '1':
                        selectCategory('Food');
                        break;
                    case '2':
                        selectCategory('Travel');
                        break;
                    case '3':
                        selectCategory('Shopping');
                        break;
                    case '4':
                        selectCategory('Entertainment');
                        break;
                    case '5':
                        selectCategory('Other');
                        break;
                }
            }
        });
        
        function selectCategory(category) {
            categoryGrid.querySelectorAll('.category-option').forEach(opt => {
                if (opt.getAttribute('data-category') === category) {
                    opt.click();
                    showFlash(`Category set to ${category}`, 'info');
                }
            });
        }
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\expenses\personal.html
=============================
{% extends "layout.html" %}

{% block title %}Personal Expenses - Expensio{% endblock %}

{% block extra_css %}
<style>
    /* Personal Expenses Specific Styles */
    .personal-expenses-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1.5rem;
        animation: fadeIn 0.5s ease;
    }
    
    .page-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    
    .page-header h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .page-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
        max-width: 600px;
        margin: 0 auto;
    }
    
    /* Stats Overview */
    .stats-overview {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-bottom: 3rem;
    }
    
    .stat-card {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
        text-align: center;
    }
    
    .stat-card:hover {
        transform: translateY(-5px);
        border-color: var(--accent-purple);
        box-shadow: 0 15px 35px rgba(138, 43, 226, 0.2);
    }
    
    .stat-icon {
        width: 70px;
        height: 70px;
        border-radius: 20px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        font-size: 2rem;
    }
    
    .stat-value {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        color: var(--accent-blue);
    }
    
    .stat-label {
        color: var(--text-muted);
        font-size: 1rem;
        font-weight: 500;
    }
    
    .stat-trend {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        margin-top: 1rem;
        font-size: 0.9rem;
    }
    
    .trend-up {
        color: var(--success);
    }
    
    .trend-down {
        color: var(--danger);
    }
    
    /* Budget Progress */
    .budget-progress-card {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 3rem;
        border: 1px solid var(--card-border);
    }
    
    .budget-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
    }
    
    .budget-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-blue);
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .budget-actions {
        display: flex;
        gap: 1rem;
    }
    
    .progress-container {
        margin: 2rem 0;
    }
    
    .progress-labels {
        display: flex;
        justify-content: space-between;
        margin-bottom: 0.5rem;
        font-size: 0.95rem;
    }
    
    .progress-label {
        color: var(--text-light);
        font-weight: 500;
    }
    
    .progress-percentage {
        color: var(--accent-blue);
        font-weight: 600;
    }
    
    .progress-bar {
        height: 12px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 6px;
        overflow: hidden;
        position: relative;
    }
    
    .progress-fill {
        height: 100%;
        background: var(--secondary-gradient);
        border-radius: 6px;
        transition: width 1s ease;
        position: relative;
        overflow: hidden;
    }
    
    .progress-fill::after {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        animation: shimmer 2s infinite;
    }
    
    @keyframes shimmer {
        100% { left: 100%; }
    }
    
    .budget-details {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
        margin-top: 2rem;
    }
    
    .budget-detail {
        text-align: center;
        padding: 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
    }
    
    .detail-label {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin-bottom: 0.5rem;
    }
    
    .detail-value {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--text-light);
    }
    
    /* Expenses Table */
    .expenses-section {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 3rem;
        border: 1px solid var(--card-border);
    }
    
    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        padding-bottom: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .section-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-blue);
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .table-controls {
        display: flex;
        gap: 1rem;
        align-items: center;
    }
    
    .filter-select {
        background: rgba(255, 255, 255, 0.07);
        border: 1px solid var(--card-border);
        border-radius: 10px;
        padding: 8px 16px;
        color: var(--text-light);
        font-size: 0.95rem;
        min-width: 150px;
    }
    
    .expenses-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
    }
    
    .table-header {
        background: rgba(255, 255, 255, 0.05);
    }
    
    .table-header th {
        padding: 1rem;
        text-align: left;
        font-weight: 600;
        color: var(--text-light);
        border-bottom: 1px solid var(--card-border);
    }
    
    .table-row {
        transition: var(--transition);
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }
    
    .table-row:hover {
        background: rgba(138, 43, 226, 0.05);
    }
    
    .table-row td {
        padding: 1rem;
        vertical-align: middle;
    }
    
    .expense-description {
        display: flex;
        align-items: center;
        gap: 1rem;
    }
    
    .expense-icon {
        width: 40px;
        height: 40px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
    }
    
    .icon-food { background: rgba(0, 255, 157, 0.1); color: var(--success); }
    .icon-travel { background: rgba(0, 212, 255, 0.1); color: var(--accent-blue); }
    .icon-shopping { background: rgba(138, 43, 226, 0.1); color: var(--accent-purple); }
    .icon-entertainment { background: rgba(255, 183, 77, 0.1); color: var(--warning); }
    .icon-other { background: rgba(255, 107, 139, 0.1); color: var(--danger); }
    
    .expense-info {
        flex: 1;
    }
    
    .expense-name {
        font-weight: 500;
        margin-bottom: 0.25rem;
        color: var(--text-light);
    }
    
    .expense-category {
        font-size: 0.85rem;
        color: var(--text-muted);
    }
    
    .expense-amount {
        font-weight: 600;
        color: var(--accent-blue);
    }
    
    .expense-date {
        color: var(--text-muted);
        font-size: 0.9rem;
    }
    
    .expense-actions {
        display: flex;
        gap: 0.5rem;
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    
    .table-row:hover .expense-actions {
        opacity: 1;
    }
    
    .action-btn {
        width: 36px;
        height: 36px;
        border-radius: 8px;
        background: rgba(255, 255, 255, 0.07);
        border: none;
        color: var(--text-muted);
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: var(--transition);
    }
    
    .action-btn:hover {
        background: var(--accent-purple);
        color: white;
    }
    
    /* Add Expense Button */
    .add-expense-btn {
        position: fixed;
        bottom: 2rem;
        right: 2rem;
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: var(--secondary-gradient);
        color: white;
        border: none;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        box-shadow: 0 10px 30px rgba(138, 43, 226, 0.4);
        transition: var(--transition);
        z-index: 1000;
    }
    
    .add-expense-btn:hover {
        transform: scale(1.1);
        box-shadow: 0 15px 40px rgba(138, 43, 226, 0.6);
    }
    
    /* Empty State */
    .empty-state {
        text-align: center;
        padding: 4rem 2rem;
        color: var(--text-muted);
    }
    
    .empty-state i {
        font-size: 4rem;
        margin-bottom: 1rem;
        opacity: 0.5;
    }
    
    .empty-state h3 {
        margin-bottom: 1rem;
        color: var(--text-light);
    }
    
    /* Monthly Summary */
    .monthly-summary {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 3rem;
        border: 1px solid var(--card-border);
    }
    
    .summary-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 1.5rem;
        margin-top: 1.5rem;
    }
    
    .summary-item {
        text-align: center;
        padding: 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
    }
    
    .summary-value {
        font-size: 1.8rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        color: var(--accent-blue);
    }
    
    .summary-label {
        font-size: 0.9rem;
        color: var(--text-muted);
    }
    
    /* Export Section */
    .export-section {
        background: rgba(138, 43, 226, 0.05);
        border-radius: 20px;
        padding: 2rem;
        text-align: center;
        border: 1px solid rgba(138, 43, 226, 0.2);
    }
    
    .export-header {
        margin-bottom: 1.5rem;
    }
    
    .export-header h2 {
        color: var(--accent-purple);
        margin-bottom: 0.5rem;
    }
    
    .export-header p {
        color: var(--text-muted);
        max-width: 600px;
        margin: 0 auto;
    }
    
    .export-options {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .personal-expenses-container {
            padding: 0 1rem;
        }
        
        .stats-overview {
            grid-template-columns: 1fr;
        }
        
        .section-header {
            flex-direction: column;
            align-items: flex-start;
            gap: 1rem;
        }
        
        .table-controls {
            width: 100%;
            justify-content: space-between;
        }
        
        .expenses-table {
            display: block;
            overflow-x: auto;
        }
        
        .add-expense-btn {
            bottom: 1rem;
            right: 1rem;
            width: 50px;
            height: 50px;
            font-size: 1.2rem;
        }
        
        .budget-details {
            grid-template-columns: 1fr;
        }
        
        .summary-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }
    
    @media (max-width: 480px) {
        .summary-grid {
            grid-template-columns: 1fr;
        }
        
        .export-options {
            flex-direction: column;
        }
        
        .export-options .btn {
            width: 100%;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="personal-expenses-container">
    <!-- Page Header -->
    <div class="page-header">
        <h1>Personal Expense Tracker</h1>
        <p>Track your individual spending, set budgets, and gain insights into your personal finances.</p>
    </div>
    
    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                <div class="flash-message flash-{{ category }} mb-4">
                    <i class="fas fa-{% if category == 'success' %}check-circle{% elif category == 'error' %}exclamation-circle{% else %}info-circle{% endif %}"></i>
                    {{ message }}
                    <button class="flash-close">&times;</button>
                </div>
            {% endfor %}
        {% endif %}
    {% endwith %}
    
    <!-- Stats Overview -->
    <div class="stats-overview">
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-money-bill-wave"></i>
            </div>
            <div class="stat-value">₹{{ "%.2f"|format(total) }}</div>
            <div class="stat-label">Total Spent</div>
            <div class="stat-trend trend-up">
                <i class="fas fa-arrow-up"></i>
                <span>From all time</span>
            </div>
        </div>
        
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-calendar-alt"></i>
            </div>
            <div class="stat-value">{{ expenses|length }}</div>
            <div class="stat-label">Total Expenses</div>
            <div class="stat-trend trend-down">
                <i class="fas fa-arrow-down"></i>
                <span>{{ (expenses|length / 30)|round|int }} avg/month</span>
            </div>
        </div>
        
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-tags"></i>
            </div>
            <div class="stat-value" id="topCategory">Food</div>
            <div class="stat-label">Top Category</div>
            <div class="stat-trend">
                <i class="fas fa-chart-pie"></i>
                <span>35% of total</span>
            </div>
        </div>
        
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-wallet"></i>
            </div>
            <div class="stat-value" id="dailyAverage">₹{{ "%.2f"|format(total/30 if total > 0 else 0) }}</div>
            <div class="stat-label">Daily Average</div>
            <div class="stat-trend trend-up">
                <i class="fas fa-info-circle"></i>
                <span>Based on 30 days</span>
            </div>
        </div>
    </div>
    
    <!-- Budget Progress -->
    <div class="budget-progress-card">
        <div class="budget-header">
            <h2 class="budget-title">
                <i class="fas fa-bullseye"></i> Monthly Budget
            </h2>
            <div class="budget-actions">
                <button class="btn btn-outline btn-sm" onclick="editBudget()">
                    <i class="fas fa-edit"></i> Edit
                </button>
                <button class="btn btn-outline btn-sm" onclick="resetBudget()">
                    <i class="fas fa-redo"></i> Reset
                </button>
            </div>
        </div>
        
        <div class="progress-container">
            <div class="progress-labels">
                <div class="progress-label">Budget Progress</div>
                <div class="progress-percentage" id="budgetPercentage">65%</div>
            </div>
            <div class="progress-bar">
                <div class="progress-fill" id="budgetProgress" style="width: 65%;"></div>
            </div>
        </div>
        
        <div class="budget-details">
            <div class="budget-detail">
                <div class="detail-label">Budget Limit</div>
                <div class="detail-value">₹10,000</div>
            </div>
            <div class="budget-detail">
                <div class="detail-label">Spent</div>
                <div class="detail-value">₹6,500</div>
            </div>
            <div class="budget-detail">
                <div class="detail-label">Remaining</div>
                <div class="detail-value" style="color: var(--success);">₹3,500</div>
            </div>
            <div class="budget-detail">
                <div class="detail-label">Days Left</div>
                <div class="detail-value">15</div>
            </div>
        </div>
    </div>
    
    <!-- Monthly Summary -->
    <div class="monthly-summary">
        <div class="section-header">
            <h2 class="section-title">
                <i class="fas fa-chart-line"></i> This Month's Summary
            </h2>
            <select class="filter-select" onchange="filterByMonth(this.value)">
                <option value="current">Current Month</option>
                <option value="last">Last Month</option>
                <option value="last3">Last 3 Months</option>
                <option value="all">All Time</option>
            </select>
        </div>
        
        <div class="summary-grid">
            <div class="summary-item">
                <div class="summary-value">₹6,500</div>
                <div class="summary-label">Total Spent</div>
            </div>
            <div class="summary-item">
                <div class="summary-value">12</div>
                <div class="summary-label">Transactions</div>
            </div>
            <div class="summary-item">
                <div class="summary-value">₹217</div>
                <div class="summary-label">Daily Average</div>
            </div>
            <div class="summary-item">
                <div class="summary-value">Food</div>
                <div class="summary-label">Top Category</div>
            </div>
            <div class="summary-item">
                <div class="summary-value">₹542</div>
                <div class="summary-label">Avg/Transaction</div>
            </div>
            <div class="summary-item">
                <div class="summary-value">35%</div>
                <div class="summary-label">Budget Used</div>
            </div>
        </div>
    </div>
    
    <!-- Expenses Table -->
    <div class="expenses-section">
        <div class="section-header">
            <h2 class="section-title">
                <i class="fas fa-list"></i> Recent Expenses
            </h2>
            <div class="table-controls">
                <select class="filter-select" onchange="filterExpenses(this.value)">
                    <option value="all">All Categories</option>
                    <option value="food">Food</option>
                    <option value="travel">Travel</option>
                    <option value="shopping">Shopping</option>
                    <option value="entertainment">Entertainment</option>
                    <option value="other">Other</option>
                </select>
                <button class="btn btn-outline btn-sm" onclick="exportTable()">
                    <i class="fas fa-download"></i> Export
                </button>
            </div>
        </div>
        
        {% if expenses %}
        <div class="table-responsive">
            <table class="expenses-table">
                <thead class="table-header">
                    <tr>
                        <th>Description</th>
                        <th>Category</th>
                        <th>Amount</th>
                        <th>Date</th>
                        <th style="width: 100px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for expense in expenses %}
                    <tr class="table-row" data-category="{{ expense.category|lower }}">
                        <td>
                            <div class="expense-description">
                                <div class="expense-icon icon-{{ expense.category|lower }}">
                                    {% if expense.category == 'Food' %}
                                    <i class="fas fa-utensils"></i>
                                    {% elif expense.category == 'Travel' %}
                                    <i class="fas fa-car"></i>
                                    {% elif expense.category == 'Shopping' %}
                                    <i class="fas fa-shopping-bag"></i>
                                    {% elif expense.category == 'Entertainment' %}
                                    <i class="fas fa-film"></i>
                                    {% else %}
                                    <i class="fas fa-receipt"></i>
                                    {% endif %}
                                </div>
                                <div class="expense-info">
                                    <div class="expense-name">{{ expense.description }}</div>
                                    {% if expense.notes %}
                                    <div class="expense-notes" style="font-size: 0.85rem; color: var(--text-muted);">
                                        {{ expense.notes[:50] }}{% if expense.notes|length > 50 %}...{% endif %}
                                    </div>
                                    {% endif %}
                                </div>
                            </div>
                        </td>
                        <td>
                            <span class="expense-category">{{ expense.category }}</span>
                        </td>
                        <td>
                            <div class="expense-amount">₹{{ "%.2f"|format(expense.amount) }}</div>
                        </td>
                        <td>
                            <div class="expense-date">{{ expense.date.strftime('%b %d, %Y') }}</div>
                        </td>
                        <td>
                            <div class="expense-actions">
                                <button class="action-btn" title="Edit" onclick="editExpense({{ expense.id }})">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <button class="action-btn" title="Delete" onclick="deleteExpense({{ expense.id }})">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        
        <!-- Pagination -->
        <div class="pagination" style="display: flex; justify-content: center; margin-top: 2rem; gap: 0.5rem;">
            <button class="btn btn-outline btn-sm">
                <i class="fas fa-chevron-left"></i> Previous
            </button>
            <button class="btn btn-outline btn-sm active">1</button>
            <button class="btn btn-outline btn-sm">2</button>
            <button class="btn btn-outline btn-sm">3</button>
            <button class="btn btn-outline btn-sm">
                Next <i class="fas fa-chevron-right"></i>
            </button>
        </div>
        
        {% else %}
        <!-- Empty State -->
        <div class="empty-state">
            <i class="fas fa-wallet"></i>
            <h3>No Personal Expenses Yet</h3>
            <p>Start tracking your personal spending by adding your first expense.</p>
            <button class="btn btn-primary mt-2" onclick="showAddExpenseModal()">
                <i class="fas fa-plus-circle"></i> Add Your First Expense
            </button>
        </div>
        {% endif %}
    </div>
    
    <!-- Export Section -->
    <div class="export-section">
        <div class="export-header">
            <h2>Export Your Expenses</h2>
            <p>Download your personal expense data for analysis or record keeping.</p>
        </div>
        <div class="export-options">
            <a href="{{ url_for('export_personal') }}" class="btn btn-primary">
                <i class="fas fa-file-csv"></i> Export as CSV
            </a>
            <button class="btn btn-outline" onclick="printExpenses()">
                <i class="fas fa-print"></i> Print Summary
            </button>
            <a href="{{ url_for('charts') }}" class="btn btn-outline">
                <i class="fas fa-chart-pie"></i> View Charts
            </a>
        </div>
    </div>
    
    <!-- Add Expense Floating Button -->
    <button class="add-expense-btn" onclick="showAddExpenseModal()" title="Add New Expense">
        <i class="fas fa-plus"></i>
    </button>
</div>

<!-- Add Expense Modal (Will be shown by JavaScript) -->
<div id="addExpenseModal" class="modal-overlay" style="display: none;">
    <div class="modal-content">
        <div class="modal-header">
            <h3><i class="fas fa-plus-circle"></i> Add Personal Expense</h3>
            <button class="modal-close" onclick="closeModal()">&times;</button>
        </div>
        <div class="modal-body">
            <form id="addExpenseForm" method="POST" action="{{ url_for('personal_expense') }}">
                <div class="input-group mb-3">
                    <label for="modalDescription"><i class="fas fa-align-left"></i> Description *</label>
                    <input type="text" id="modalDescription" name="description" class="form-control" 
                           placeholder="What did you spend on?" required>
                </div>
                
                <div class="input-group mb-3">
                    <label for="modalAmount"><i class="fas fa-rupee-sign"></i> Amount *</label>
                    <div class="amount-input">
                        <span class="currency">₹</span>
                        <input type="number" id="modalAmount" name="amount" class="form-control" 
                               placeholder="0.00" min="0.01" step="0.01" required>
                    </div>
                </div>
                
                <div class="input-group mb-3">
                    <label for="modalCategory"><i class="fas fa-tags"></i> Category *</label>
                    <select id="modalCategory" name="category" class="form-control" required>
                        <option value="Food">Food & Dining</option>
                        <option value="Travel">Travel & Transport</option>
                        <option value="Shopping">Shopping</option>
                        <option value="Entertainment">Entertainment</option>
                        <option value="Bills">Bills & Utilities</option>
                        <option value="Other">Other</option>
                    </select>
                </div>
                
                <div class="input-group mb-3">
                    <label for="modalDate"><i class="fas fa-calendar"></i> Date *</label>
                    <input type="date" id="modalDate" name="date" class="form-control" 
                           value="{{ datetime.utcnow().strftime('%Y-%m-%d') }}" required>
                </div>
                
                <div class="input-group mb-3">
                    <label for="modalNotes"><i class="fas fa-sticky-note"></i> Notes (Optional)</label>
                    <textarea id="modalNotes" name="notes" class="form-control" 
                              placeholder="Any additional details..." rows="3"></textarea>
                </div>
                
                <div class="form-actions">
                    <button type="button" class="btn btn-outline" onclick="closeModal()">Cancel</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-check-circle"></i> Add Expense
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Initialize budget progress animation
        const progressBar = document.getElementById('budgetProgress');
        const percentage = document.getElementById('budgetPercentage');
        
        // Animate progress bar
        setTimeout(() => {
            const width = progressBar.style.width;
            progressBar.style.width = '0%';
            setTimeout(() => {
                progressBar.style.width = width;
            }, 300);
        }, 500);
        
        // Calculate top category
        calculateTopCategory();
        
        // Add animations to stat cards
        const statCards = document.querySelectorAll('.stat-card');
        statCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.2}s`;
            card.style.animation = 'fadeInUp 0.6s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 200));
        });
        
        // Initialize today's date in modal
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('modalDate').value = today;
    });
    
    // Calculate top spending category
    function calculateTopCategory() {
        // This would normally come from backend
        // For demo, we'll calculate from table data
        const categories = {};
        const rows = document.querySelectorAll('.table-row[data-category]');
        
        rows.forEach(row => {
            const category = row.getAttribute('data-category');
            const amount = parseFloat(row.querySelector('.expense-amount').textContent.replace('₹', ''));
            categories[category] = (categories[category] || 0) + amount;
        });
        
        let topCategory = 'Other';
        let maxAmount = 0;
        
        for (const [category, amount] of Object.entries(categories)) {
            if (amount > maxAmount) {
                maxAmount = amount;
                topCategory = category.charAt(0).toUpperCase() + category.slice(1);
            }
        }
        
        document.getElementById('topCategory').textContent = topCategory;
    }
    
    // Filter expenses by category
    function filterExpenses(category) {
        const rows = document.querySelectorAll('.table-row');
        
        rows.forEach(row => {
            if (category === 'all' || row.getAttribute('data-category') === category) {
                row.style.display = 'table-row';
                row.style.animation = 'fadeIn 0.3s ease';
            } else {
                row.style.display = 'none';
            }
        });
        
        // Show notification
        if (category === 'all') {
            showFlash('Showing all expenses', 'info');
        } else {
            showFlash(`Filtered by ${category} category`, 'info');
        }
    }
    
    // Filter by month
    function filterByMonth(period) {
        // This would normally make an API call
        // For demo, we'll just show a message
        const periods = {
            'current': 'Current Month',
            'last': 'Last Month',
            'last3': 'Last 3 Months',
            'all': 'All Time'
        };
        
        showFlash(`Showing data for ${periods[period]}`, 'info');
    }
    
    // Edit budget
    function editBudget() {
        const newBudget = prompt('Enter new monthly budget amount:', '10000');
        if (newBudget && !isNaN(newBudget) && parseFloat(newBudget) > 0) {
            const amount = parseFloat(newBudget).toFixed(2);
            showFlash(`Budget updated to ₹${amount}`, 'success');
            
            // Update UI
            document.querySelector('.budget-detail .detail-value').textContent = `₹${amount}`;
        }
    }
    
    // Reset budget
    function resetBudget() {
        if (confirm('Are you sure you want to reset your budget progress?')) {
            showFlash('Budget progress reset successfully', 'success');
            
            // Reset progress bar
            const progressBar = document.getElementById('budgetProgress');
            const percentage = document.getElementById('budgetPercentage');
            
            progressBar.style.width = '0%';
            percentage.textContent = '0%';
            
            setTimeout(() => {
                progressBar.style.width = '35%';
                percentage.textContent = '35%';
            }, 300);
        }
    }
    
    // Export table data
    function exportTable() {
        // In a real app, this would export the filtered table data
        showFlash('Preparing data for export...', 'info');
        
        setTimeout(() => {
            showFlash('Data exported successfully!', 'success');
        }, 1500);
    }
    
    // Print expenses
    function printExpenses() {
        window.print();
    }
    
    // Edit expense
    function editExpense(expenseId) {
        showFlash(`Editing expense #${expenseId}`, 'info');
        // In a real app, this would open an edit modal
    }
    
    // Delete expense
    function deleteExpense(expenseId) {
        if (confirm('Are you sure you want to delete this expense?')) {
            const row = document.querySelector(`[onclick="deleteExpense(${expenseId})"]`).closest('.table-row');
            row.style.opacity = '0.5';
            row.style.transform = 'translateX(-20px)';
            
            setTimeout(() => {
                row.remove();
                showFlash('Expense deleted successfully', 'success');
                calculateTopCategory();
            }, 300);
        }
    }
    
    // Show add expense modal
    function showAddExpenseModal() {
        const modal = document.getElementById('addExpenseModal');
        modal.style.display = 'flex';
        
        // Add modal styles if not present
        if (!document.querySelector('#modalStyles')) {
            const styles = `
                <style id="modalStyles">
                    .modal-overlay {
                        position: fixed;
                        top: 0;
                        left: 0;
                        width: 100%;
                        height: 100%;
                        background: rgba(0, 0, 0, 0.7);
                        backdrop-filter: blur(5px);
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        z-index: 2000;
                        animation: fadeIn 0.3s ease;
                    }
                    
                    .modal-content {
                        background: var(--card-bg);
                        border-radius: 20px;
                        padding: 2rem;
                        max-width: 500px;
                        width: 90%;
                        max-height: 90vh;
                        overflow-y: auto;
                        border: 1px solid var(--card-border);
                        box-shadow: var(--shadow);
                        animation: slideInUp 0.3s ease;
                    }
                    
                    .modal-header {
                        display: flex;
                        justify-content: space-between;
                        align-items: center;
                        margin-bottom: 1.5rem;
                        padding-bottom: 1rem;
                        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
                    }
                    
                    .modal-header h3 {
                        margin: 0;
                        color: var(--accent-blue);
                        display: flex;
                        align-items: center;
                        gap: 0.75rem;
                    }
                    
                    .modal-close {
                        background: none;
                        border: none;
                        color: var(--text-muted);
                        font-size: 1.5rem;
                        cursor: pointer;
                        padding: 0;
                        line-height: 1;
                    }
                    
                    .modal-close:hover {
                        color: var(--accent-blue);
                    }
                    
                    .modal-body {
                        padding: 1rem 0;
                    }
                    
                    .form-actions {
                        display: flex;
                        justify-content: flex-end;
                        gap: 1rem;
                        margin-top: 2rem;
                        padding-top: 1.5rem;
                        border-top: 1px solid rgba(255, 255, 255, 0.1);
                    }
                    
                    @keyframes slideInUp {
                        from {
                            transform: translateY(30px);
                            opacity: 0;
                        }
                        to {
                            transform: translateY(0);
                            opacity: 1;
                        }
                    }
                </style>
            `;
            document.head.insertAdjacentHTML('beforeend', styles);
        }
    }
    
    // Close modal
    function closeModal() {
        const modal = document.getElementById('addExpenseModal');
        modal.style.animation = 'fadeOut 0.3s ease';
        setTimeout(() => {
            modal.style.display = 'none';
            modal.style.animation = '';
        }, 300);
    }
    
    // Form validation for add expense
    document.getElementById('addExpenseForm')?.addEventListener('submit', function(e) {
        const description = document.getElementById('modalDescription').value.trim();
        const amount = document.getElementById('modalAmount').value;
        
        if (!description) {
            e.preventDefault();
            showError('Please enter a description');
            return;
        }
        
        if (!amount || parseFloat(amount) <= 0) {
            e.preventDefault();
            showError('Please enter a valid amount greater than 0');
            return;
        }
        
        // Format amount to 2 decimal places
        document.getElementById('modalAmount').value = parseFloat(amount).toFixed(2);
        
        showFlash('Adding expense...', 'info');
    });
    
    // Close modal when clicking outside
    document.addEventListener('click', function(e) {
        const modal = document.getElementById('addExpenseModal');
        if (modal && e.target === modal) {
            closeModal();
        }
    });
    
    // Show flash message
    function showFlash(message, type = 'info') {
        // Remove existing flash
        const existingFlash = document.querySelector('.flash-message');
        if (existingFlash) existingFlash.remove();
        
        const flashDiv = document.createElement('div');
        flashDiv.className = `flash-message flash-${type} mb-4`;
        flashDiv.innerHTML = `
            <i class="fas fa-${type === 'success' ? 'check-circle' : 'info-circle'}"></i>
            ${message}
            <button class="flash-close">&times;</button>
        `;
        
        const pageHeader = document.querySelector('.page-header');
        pageHeader.parentNode.insertBefore(flashDiv, pageHeader.nextSibling);
        
        flashDiv.querySelector('.flash-close').addEventListener('click', function() {
            flashDiv.style.opacity = '0';
            flashDiv.style.transform = 'translateY(-10px)';
            setTimeout(() => flashDiv.remove(), 300);
        });
        
        setTimeout(() => {
            if (flashDiv.parentElement) {
                flashDiv.style.opacity = '0';
                flashDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => flashDiv.remove(), 300);
            }
        }, 5000);
    }
    
    // Show error message
    function showError(message) {
        showFlash(message, 'error');
    }
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\room\create.html
=============================
{% extends "layout.html" %}

{% block title %}Create Room - Expensio{% endblock %}

{% block extra_css %}
<style>
    /* Create Room Specific Styles */
    .create-room-container {
        max-width: 800px;
        margin: 2rem auto;
        padding: 0 1.5rem;
        animation: fadeInUp 0.6s ease;
    }
    
    .page-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    
    .page-header h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .page-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
        max-width: 600px;
        margin: 0 auto;
    }
    
    /* Form Styles */
    .create-form {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2.5rem;
        border: 1px solid var(--card-border);
        box-shadow: var(--shadow);
    }
    
    .form-section {
        margin-bottom: 2.5rem;
        padding-bottom: 2rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .form-section:last-child {
        border-bottom: none;
        margin-bottom: 0;
        padding-bottom: 0;
    }
    
    .section-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }
    
    .section-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }
    
    .section-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-blue);
    }
    
    .section-description {
        color: var(--text-muted);
        margin-bottom: 1.5rem;
        font-size: 0.95rem;
        line-height: 1.6;
    }
    
    /* Form Grid */
    .form-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1.5rem;
    }
    
    @media (max-width: 768px) {
        .form-grid {
            grid-template-columns: 1fr;
        }
    }
    
    /* Input Groups */
    .input-group {
        position: relative;
    }
    
    .input-group label {
        display: block;
        margin-bottom: 0.5rem;
        color: var(--text-light);
        font-weight: 500;
        font-size: 0.95rem;
    }
    
    .input-group .form-control {
        width: 100%;
        padding: 12px 16px;
        background: rgba(255, 255, 255, 0.07);
        border: 1px solid var(--card-border);
        border-radius: 12px;
        color: var(--text-light);
        font-size: 1rem;
        transition: var(--transition);
    }
    
    .input-group .form-control:focus {
        outline: none;
        border-color: var(--accent-blue);
        box-shadow: 0 0 0 3px rgba(0, 212, 255, 0.1);
        background: rgba(255, 255, 255, 0.1);
    }
    
    .input-group textarea.form-control {
        min-height: 120px;
        resize: vertical;
        font-family: inherit;
    }
    
    /* Character Counter */
    .char-counter {
        text-align: right;
        font-size: 0.85rem;
        color: var(--text-muted);
        margin-top: 0.25rem;
    }
    
    /* Budget Input */
    .budget-input {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .budget-input .currency {
        padding: 12px 16px;
        background: rgba(138, 43, 226, 0.2);
        border: 1px solid var(--card-border);
        border-radius: 12px 0 0 12px;
        color: var(--accent-purple);
        font-weight: 600;
    }
    
    .budget-input .form-control {
        border-radius: 0 12px 12px 0;
        flex: 1;
    }
    
    /* Room Code Generator */
    .code-generator {
        display: flex;
        gap: 1rem;
        align-items: center;
    }
    
    .code-display {
        flex: 1;
        font-family: monospace;
        font-size: 1.1rem;
        letter-spacing: 1px;
    }
    
    .generate-btn {
        background: var(--secondary-gradient);
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 10px;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 500;
        transition: var(--transition);
    }
    
    .generate-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
    }
    
    /* Form Actions */
    .form-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 2.5rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .form-actions .btn {
        min-width: 150px;
    }
    
    /* Room Preview */
    .room-preview {
        background: rgba(138, 43, 226, 0.1);
        border-radius: 15px;
        padding: 2rem;
        margin-top: 3rem;
        border: 1px solid var(--card-border);
    }
    
    .preview-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }
    
    .preview-header h3 {
        color: var(--accent-blue);
        margin: 0;
    }
    
    .preview-content {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
    }
    
    .preview-item {
        text-align: center;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
    }
    
    .preview-label {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin-bottom: 0.5rem;
    }
    
    .preview-value {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--text-light);
    }
    
    /* Tips Section */
    .tips-section {
        background: rgba(0, 212, 255, 0.05);
        border-radius: 15px;
        padding: 1.5rem;
        margin-top: 2rem;
        border: 1px solid rgba(0, 212, 255, 0.2);
    }
    
    .tips-header {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 1rem;
        color: var(--accent-blue);
    }
    
    .tips-list {
        list-style: none;
        padding: 0;
    }
    
    .tips-list li {
        padding: 0.5rem 0;
        color: var(--text-muted);
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
    }
    
    .tips-list li:before {
        content: '✓';
        color: var(--accent-blue);
        font-weight: bold;
        flex-shrink: 0;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .create-room-container {
            padding: 0 1rem;
        }
        
        .create-form {
            padding: 1.5rem;
        }
        
        .form-actions {
            flex-direction: column;
            gap: 1rem;
        }
        
        .form-actions .btn {
            width: 100%;
        }
        
        .page-header h1 {
            font-size: 2rem;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="create-room-container">
    <!-- Page Header -->
    <div class="page-header">
        <h1>Create New Room</h1>
        <p>Start tracking expenses with friends, family, or colleagues. Create a secure room and invite members to join.</p>
    </div>
    
    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                <div class="flash-message flash-{{ category }} mb-4">
                    <i class="fas fa-{% if category == 'success' %}check-circle{% elif category == 'error' %}exclamation-circle{% else %}info-circle{% endif %}"></i>
                    {{ message }}
                    <button class="flash-close">&times;</button>
                </div>
            {% endfor %}
        {% endif %}
    {% endwith %}
    
    <!-- Create Form -->
    <form method="POST" action="{{ url_for('create_room') }}" class="create-form" id="createRoomForm">
        <!-- Room Details Section -->
        <div class="form-section">
            <div class="section-header">
                <div class="section-icon">
                    <i class="fas fa-door-open"></i>
                </div>
                <h2 class="section-title">Room Details</h2>
            </div>
            
            <p class="section-description">
                Give your room a name and description that helps members understand its purpose.
            </p>
            
            <div class="form-grid">
                <!-- Room Name -->
                <div class="input-group">
                    <label for="roomName">
                        <i class="fas fa-hashtag"></i> Room Name *
                    </label>
                    <input type="text" 
                           id="roomName" 
                           name="name" 
                           class="form-control" 
                           placeholder="e.g., Goa Trip 2024, Office Team Lunch, Family Gathering"
                           required
                           maxlength="100"
                           autofocus>
                    <div class="char-counter">
                        <span id="nameCounter">0</span>/100
                    </div>
                </div>
                
                <!-- Room Code -->
                <div class="input-group">
                    <label for="roomCode">
                        <i class="fas fa-key"></i> Room Code *
                    </label>
                    <div class="code-generator">
                        <input type="text" 
                               id="roomCode" 
                               name="room_code" 
                               class="form-control code-display" 
                               placeholder="Generate a unique code"
                               required
                               readonly>
                        <button type="button" class="generate-btn" id="generateCode">
                            <i class="fas fa-sync-alt"></i>
                            Generate
                        </button>
                    </div>
                    <small style="color: var(--text-muted); margin-top: 0.25rem; display: block;">
                        Share this code with friends to join the room
                    </small>
                </div>
            </div>
            
            <!-- Room Description -->
            <div class="input-group mt-3">
                <label for="roomDescription">
                    <i class="fas fa-align-left"></i> Description (Optional)
                </label>
                <textarea id="roomDescription" 
                          name="description" 
                          class="form-control" 
                          placeholder="Describe the purpose of this room. What expenses will be tracked here?"
                          maxlength="500"></textarea>
                <div class="char-counter">
                    <span id="descCounter">0</span>/500
                </div>
            </div>
        </div>
        
        <!-- Budget Settings Section -->
        <div class="form-section">
            <div class="section-header">
                <div class="section-icon">
                    <i class="fas fa-wallet"></i>
                </div>
                <h2 class="section-title">Budget Settings</h2>
            </div>
            
            <p class="section-description">
                Set an optional budget limit to help track spending against your planned amount.
            </p>
            
            <div class="input-group">
                <label for="roomBudget">
                    <i class="fas fa-chart-line"></i> Total Budget (Optional)
                </label>
                <div class="budget-input">
                    <span class="currency">₹</span>
                    <input type="number" 
                           id="roomBudget" 
                           name="budget" 
                           class="form-control" 
                           placeholder="0.00"
                           min="0"
                           step="0.01"
                           value="0">
                </div>
                <small style="color: var(--text-muted); margin-top: 0.25rem; display: block;">
                    Leave as 0 if you don't want to set a budget limit
                </small>
            </div>
        </div>
        
        <!-- Tips Section -->
        <div class="tips-section">
            <div class="tips-header">
                <i class="fas fa-lightbulb"></i>
                <h3 style="margin: 0; font-size: 1.1rem;">Tips for a Great Room</h3>
            </div>
            <ul class="tips-list">
                <li>Choose a descriptive name that everyone will recognize</li>
                <li>Set a budget to avoid overspending</li>
                <li>Use the room code to easily invite friends</li>
                <li>Add a description to clarify the room's purpose</li>
                <li>Remember to track expenses regularly</li>
            </ul>
        </div>
        
        <!-- Room Preview -->
        <div class="room-preview" id="roomPreview">
            <div class="preview-header">
                <i class="fas fa-eye"></i>
                <h3>Room Preview</h3>
            </div>
            <div class="preview-content">
                <div class="preview-item">
                    <div class="preview-label">Room Name</div>
                    <div class="preview-value" id="previewName">Not set</div>
                </div>
                <div class="preview-item">
                    <div class="preview-label">Room Code</div>
                    <div class="preview-value" id="previewCode">------</div>
                </div>
                <div class="preview-item">
                    <div class="preview-label">Budget</div>
                    <div class="preview-value" id="previewBudget">₹0.00</div>
                </div>
                <div class="preview-item">
                    <div class="preview-label">Members</div>
                    <div class="preview-value">1 (You)</div>
                </div>
            </div>
        </div>
        
        <!-- Form Actions -->
        <div class="form-actions">
            <a href="{{ url_for('dashboard') }}" class="btn btn-outline">
                <i class="fas fa-arrow-left"></i> Back to Dashboard
            </a>
            <button type="submit" class="btn btn-primary" id="createRoomBtn">
                <i class="fas fa-plus-circle"></i> Create Room
            </button>
        </div>
    </form>
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Elements
        const roomNameInput = document.getElementById('roomName');
        const roomCodeInput = document.getElementById('roomCode');
        const roomDescInput = document.getElementById('roomDescription');
        const roomBudgetInput = document.getElementById('roomBudget');
        const generateCodeBtn = document.getElementById('generateCode');
        const createRoomBtn = document.getElementById('createRoomBtn');
        const createRoomForm = document.getElementById('createRoomForm');
        
        // Preview elements
        const previewName = document.getElementById('previewName');
        const previewCode = document.getElementById('previewCode');
        const previewBudget = document.getElementById('previewBudget');
        
        // Character counters
        const nameCounter = document.getElementById('nameCounter');
        const descCounter = document.getElementById('descCounter');
        
        // Generate random room code
        function generateRoomCode() {
            const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
            let code = '';
            for (let i = 0; i < 6; i++) {
                code += chars.charAt(Math.floor(Math.random() * chars.length));
            }
            return code;
        }
        
        // Initialize with a random code
        let initialCode = generateRoomCode();
        roomCodeInput.value = initialCode;
        previewCode.textContent = initialCode;
        
        // Generate new code
        generateCodeBtn.addEventListener('click', function() {
            const newCode = generateRoomCode();
            roomCodeInput.value = newCode;
            previewCode.textContent = newCode;
            
            // Animate the code change
            previewCode.style.transform = 'scale(1.1)';
            previewCode.style.color = 'var(--accent-blue)';
            setTimeout(() => {
                previewCode.style.transform = 'scale(1)';
                previewCode.style.color = 'var(--text-light)';
            }, 300);
        });
        
        // Update character counters
        function updateCharacterCounters() {
            nameCounter.textContent = roomNameInput.value.length;
            descCounter.textContent = roomDescInput.value.length;
        }
        
        // Update preview in real-time
        function updatePreview() {
            // Update name
            if (roomNameInput.value.trim()) {
                previewName.textContent = roomNameInput.value;
                previewName.style.color = 'var(--text-light)';
            } else {
                previewName.textContent = 'Not set';
                previewName.style.color = 'var(--text-muted)';
            }
            
            // Update budget
            const budget = parseFloat(roomBudgetInput.value) || 0;
            previewBudget.textContent = `₹${budget.toFixed(2)}`;
            
            // Change color based on budget amount
            if (budget > 0) {
                previewBudget.style.color = 'var(--success)';
            } else {
                previewBudget.style.color = 'var(--text-light)';
            }
        }
        
        // Event listeners for real-time updates
        roomNameInput.addEventListener('input', function() {
            updateCharacterCounters();
            updatePreview();
        });
        
        roomDescInput.addEventListener('input', updateCharacterCounters);
        roomBudgetInput.addEventListener('input', updatePreview);
        
        // Form validation
        createRoomForm.addEventListener('submit', function(e) {
            const roomName = roomNameInput.value.trim();
            const roomCode = roomCodeInput.value.trim();
            
            if (!roomName) {
                e.preventDefault();
                showError('Please enter a room name');
                roomNameInput.focus();
                return;
            }
            
            if (!roomCode) {
                e.preventDefault();
                showError('Please generate a room code');
                roomCodeInput.focus();
                return;
            }
            
            // Show loading state
            createRoomBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Creating Room...';
            createRoomBtn.disabled = true;
        });
        
        // Format budget input
        roomBudgetInput.addEventListener('blur', function() {
            const value = parseFloat(this.value);
            if (!isNaN(value) && value >= 0) {
                this.value = value.toFixed(2);
            } else {
                this.value = '0.00';
            }
            updatePreview();
        });
        
        // Initialize
        updateCharacterCounters();
        updatePreview();
        
        // Add some visual effects
        const formSections = document.querySelectorAll('.form-section');
        formSections.forEach((section, index) => {
            section.style.animationDelay = `${index * 0.1}s`;
            section.style.animation = 'fadeInUp 0.5s ease forwards';
            section.style.opacity = '0';
            
            setTimeout(() => {
                section.style.opacity = '1';
            }, 300 + (index * 100));
        });
        
        // Show error function
        function showError(message) {
            // Remove existing error
            const existingError = document.querySelector('.flash-error');
            if (existingError) existingError.remove();
            
            // Create error element
            const errorDiv = document.createElement('div');
            errorDiv.className = 'flash-message flash-error mb-4';
            errorDiv.innerHTML = `
                <i class="fas fa-exclamation-circle"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            // Insert after page header
            const pageHeader = document.querySelector('.page-header');
            pageHeader.parentNode.insertBefore(errorDiv, pageHeader.nextSibling);
            
            // Add close functionality
            errorDiv.querySelector('.flash-close').addEventListener('click', function() {
                errorDiv.style.opacity = '0';
                errorDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => errorDiv.remove(), 300);
            });
            
            // Auto remove after 5 seconds
            setTimeout(() => {
                if (errorDiv.parentElement) {
                    errorDiv.style.opacity = '0';
                    errorDiv.style.transform = 'translateY(-10px)';
                    setTimeout(() => errorDiv.remove(), 300);
                }
            }, 5000);
            
            // Scroll to error
            errorDiv.scrollIntoView({ behavior: 'smooth', block: 'center' });
        }
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\room\join.html
=============================
{% extends "layout.html" %}

{% block title %}Join Room - Expensio{% endblock %}

{% block extra_css %}
<style>
    /* Join Room Specific Styles */
    .join-room-container {
        max-width: 800px;
        margin: 2rem auto;
        padding: 0 1.5rem;
        animation: fadeInUp 0.6s ease;
    }
    
    .page-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    
    .page-header h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .page-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
        max-width: 600px;
        margin: 0 auto;
    }
    
    /* Join Methods */
    .join-methods {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-bottom: 3rem;
    }
    
    .method-card {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2.5rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        height: 100%;
    }
    
    .method-card:hover {
        transform: translateY(-5px);
        border-color: var(--accent-purple);
        box-shadow: 0 15px 35px rgba(138, 43, 226, 0.2);
    }
    
    .method-card.active {
        border-color: var(--accent-blue);
        background: rgba(0, 212, 255, 0.1);
    }
    
    .method-icon {
        width: 80px;
        height: 80px;
        border-radius: 20px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        margin-bottom: 1.5rem;
    }
    
    .method-title {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 1rem;
        color: var(--accent-blue);
    }
    
    .method-description {
        color: var(--text-muted);
        margin-bottom: 1.5rem;
        line-height: 1.6;
    }
    
    .method-badge {
        display: inline-block;
        padding: 4px 12px;
        background: rgba(138, 43, 226, 0.2);
        color: var(--accent-purple);
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 500;
        margin-top: auto;
    }
    
    /* Join Form */
    .join-form-container {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2.5rem;
        border: 1px solid var(--card-border);
        box-shadow: var(--shadow);
        display: none;
        animation: fadeIn 0.5s ease;
    }
    
    .join-form-container.active {
        display: block;
    }
    
    .form-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 2rem;
        padding-bottom: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .form-header i {
        font-size: 2rem;
        color: var(--accent-blue);
    }
    
    .form-header h2 {
        font-size: 1.8rem;
        font-weight: 600;
        margin: 0;
    }
    
    /* Code Input Form */
    .code-form {
        max-width: 500px;
        margin: 0 auto;
    }
    
    .input-group {
        margin-bottom: 1.5rem;
    }
    
    .input-group label {
        display: block;
        margin-bottom: 0.5rem;
        color: var(--text-light);
        font-weight: 500;
        font-size: 0.95rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .input-group .form-control {
        width: 100%;
        padding: 14px 16px;
        background: rgba(255, 255, 255, 0.07);
        border: 1px solid var(--card-border);
        border-radius: 12px;
        color: var(--text-light);
        font-size: 1.1rem;
        transition: var(--transition);
        text-align: center;
        letter-spacing: 2px;
        font-family: monospace;
    }
    
    .input-group .form-control:focus {
        outline: none;
        border-color: var(--accent-blue);
        box-shadow: 0 0 0 3px rgba(0, 212, 255, 0.1);
        background: rgba(255, 255, 255, 0.1);
    }
    
    /* Room Info Display */
    .room-info {
        background: rgba(138, 43, 226, 0.1);
        border-radius: 15px;
        padding: 2rem;
        margin-bottom: 2rem;
        border: 1px solid var(--card-border);
        display: none;
        animation: fadeIn 0.5s ease;
    }
    
    .room-info.active {
        display: block;
    }
    
    .info-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }
    
    .info-header i {
        color: var(--accent-purple);
        font-size: 1.5rem;
    }
    
    .info-header h3 {
        margin: 0;
        color: var(--accent-blue);
    }
    
    .room-details {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
    }
    
    .detail-item {
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
    }
    
    .detail-label {
        font-size: 0.9rem;
        color: var(--text-muted);
        margin-bottom: 0.5rem;
    }
    
    .detail-value {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--text-light);
    }
    
    /* Form Actions */
    .form-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-top: 2.5rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .form-actions .btn {
        min-width: 150px;
    }
    
    /* Invitation List */
    .invitations-list {
        margin-top: 3rem;
    }
    
    .invitations-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 1.5rem;
    }
    
    .invitations-header h3 {
        margin: 0;
        color: var(--accent-blue);
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .invitations-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 1.5rem;
    }
    
    .invitation-card {
        background: var(--card-bg);
        border-radius: 15px;
        padding: 1.5rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
    }
    
    .invitation-card:hover {
        transform: translateY(-3px);
        border-color: var(--accent-purple);
    }
    
    .invitation-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 1rem;
    }
    
    .invitation-name {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--accent-blue);
    }
    
    .invitation-code {
        background: rgba(138, 43, 226, 0.2);
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.9rem;
        font-family: monospace;
    }
    
    .invitation-description {
        color: var(--text-muted);
        font-size: 0.95rem;
        margin-bottom: 1rem;
        line-height: 1.5;
    }
    
    .invitation-stats {
        display: flex;
        gap: 1rem;
        font-size: 0.9rem;
        color: var(--text-muted);
    }
    
    .invitation-stat {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    /* Empty State */
    .empty-invitations {
        text-align: center;
        padding: 3rem;
        color: var(--text-muted);
    }
    
    .empty-invitations i {
        font-size: 3rem;
        margin-bottom: 1rem;
        opacity: 0.5;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .join-room-container {
            padding: 0 1rem;
        }
        
        .join-methods {
            grid-template-columns: 1fr;
        }
        
        .method-card {
            padding: 2rem;
        }
        
        .join-form-container {
            padding: 1.5rem;
        }
        
        .form-actions {
            flex-direction: column;
            gap: 1rem;
        }
        
        .form-actions .btn {
            width: 100%;
        }
        
        .invitations-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="join-room-container">
    <!-- Page Header -->
    <div class="page-header">
        <h1>Join a Room</h1>
        <p>Connect with friends and family by joining their expense tracking rooms. Enter a room code or browse available invitations.</p>
    </div>
    
    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                <div class="flash-message flash-{{ category }} mb-4">
                    <i class="fas fa-{% if category == 'success' %}check-circle{% elif category == 'error' %}exclamation-circle{% else %}info-circle{% endif %}"></i>
                    {{ message }}
                    <button class="flash-close">&times;</button>
                </div>
            {% endfor %}
        {% endif %}
    {% endwith %}
    
    <!-- Join Methods -->
    <div class="join-methods">
        <!-- Method 1: Enter Code -->
        <div class="method-card" id="codeMethodCard">
            <div class="method-icon">
                <i class="fas fa-key"></i>
            </div>
            <h3 class="method-title">Enter Room Code</h3>
            <p class="method-description">
                Have a room code from a friend? Enter it here to join their expense tracking room instantly.
            </p>
            <span class="method-badge">Most Common</span>
        </div>
        
        <!-- Method 2: Browse Invitations -->
        <div class="method-card" id="inviteMethodCard">
            <div class="method-icon">
                <i class="fas fa-envelope-open-text"></i>
            </div>
            <h3 class="method-title">Browse Invitations</h3>
            <p class="method-description">
                View rooms you've been invited to and join them with a single click.
            </p>
            <span class="method-badge">Coming Soon</span>
        </div>
    </div>
    
    <!-- Code Input Form -->
    <div class="join-form-container" id="codeForm">
        <div class="form-header">
            <i class="fas fa-key"></i>
            <h2>Enter Room Details</h2>
        </div>
        
        <form method="POST" action="{{ url_for('join_room') }}" class="code-form" id="joinRoomForm">
            <div class="input-group">
                <label for="roomName">
                    <i class="fas fa-hashtag"></i> Room Name
                </label>
                <input type="text" 
                       id="roomName" 
                       name="room_name" 
                       class="form-control" 
                       placeholder="Enter the exact room name"
                       required>
            </div>
            
            <div class="input-group">
                <label for="roomCode">
                    <i class="fas fa-lock"></i> Room Code
                </label>
                <input type="text" 
                       id="roomCode" 
                       name="room_code" 
                       class="form-control" 
                       placeholder="Enter 6-digit room code"
                       required
                       maxlength="6"
                       pattern="[A-Z0-9]{6}"
                       title="Please enter a 6-character room code (letters and numbers only)">
                <small style="color: var(--text-muted); margin-top: 0.25rem; display: block;">
                    Case-sensitive 6-character code (e.g., ABC123)
                </small>
            </div>
            
            <!-- Room Info Preview -->
            <div class="room-info" id="roomInfoPreview">
                <div class="info-header">
                    <i class="fas fa-door-open"></i>
                    <h3>Room Preview</h3>
                </div>
                <div class="room-details">
                    <div class="detail-item">
                        <div class="detail-label">Room Name</div>
                        <div class="detail-value" id="previewRoomName">-</div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Created By</div>
                        <div class="detail-value" id="previewCreator">-</div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Members</div>
                        <div class="detail-value" id="previewMembers">-</div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Status</div>
                        <div class="detail-value" id="previewStatus">-</div>
                    </div>
                </div>
            </div>
            
            <!-- Form Actions -->
            <div class="form-actions">
                <button type="button" class="btn btn-outline" id="backToMethods">
                    <i class="fas fa-arrow-left"></i> Back
                </button>
                <button type="submit" class="btn btn-primary" id="joinRoomBtn">
                    <i class="fas fa-sign-in-alt"></i> Join Room
                </button>
            </div>
        </form>
    </div>
    
    <!-- Invitations List -->
    <div class="invitations-list" id="invitationsList">
        <div class="invitations-header">
            <h3>
                <i class="fas fa-envelope"></i>
                Your Invitations
            </h3>
            <button class="btn btn-outline btn-sm" onclick="refreshInvitations()">
                <i class="fas fa-sync-alt"></i> Refresh
            </button>
        </div>
        
        <!-- Placeholder for dynamic content -->
        <div class="empty-invitations" id="emptyInvitations">
            <i class="fas fa-inbox"></i>
            <h3>No Invitations Yet</h3>
            <p>When friends invite you to rooms, they'll appear here.</p>
            <p class="mt-2" style="font-size: 0.9rem;">
                <i class="fas fa-lightbulb"></i>
                Ask your friends to share their room code with you
            </p>
        </div>
        
        <!-- Sample Invitations (for demo) -->
        <div class="invitations-grid" id="invitationsGrid" style="display: none;">
            <!-- Invitation 1 -->
            <div class="invitation-card">
                <div class="invitation-header">
                    <div class="invitation-name">Goa Trip 2024</div>
                    <div class="invitation-code">GOA2024</div>
                </div>
                <p class="invitation-description">
                    Planning expenses for our annual Goa trip. Let's track flights, accommodation, and activities together.
                </p>
                <div class="invitation-stats">
                    <div class="invitation-stat">
                        <i class="fas fa-users"></i>
                        <span>4 members</span>
                    </div>
                    <div class="invitation-stat">
                        <i class="fas fa-calendar"></i>
                        <span>Created: Jan 15</span>
                    </div>
                </div>
                <button class="btn btn-primary btn-sm w-100 mt-2">
                    <i class="fas fa-check"></i> Accept Invitation
                </button>
            </div>
            
            <!-- Invitation 2 -->
            <div class="invitation-card">
                <div class="invitation-header">
                    <div class="invitation-name">Office Team Lunch</div>
                    <div class="invitation-code">LUNCH01</div>
                </div>
                <p class="invitation-description">
                    Monthly team lunch expenses. Split bills and track who paid for what.
                </p>
                <div class="invitation-stats">
                    <div class="invitation-stat">
                        <i class="fas fa-users"></i>
                        <span>8 members</span>
                    </div>
                    <div class="invitation-stat">
                        <i class="fas fa-calendar"></i>
                        <span>Created: Jan 10</span>
                    </div>
                </div>
                <button class="btn btn-primary btn-sm w-100 mt-2">
                    <i class="fas fa-check"></i> Accept Invitation
                </button>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Elements
        const codeMethodCard = document.getElementById('codeMethodCard');
        const inviteMethodCard = document.getElementById('inviteMethodCard');
        const codeForm = document.getElementById('codeForm');
        const joinRoomForm = document.getElementById('joinRoomForm');
        const roomInfoPreview = document.getElementById('roomInfoPreview');
        const backToMethodsBtn = document.getElementById('backToMethods');
        const joinRoomBtn = document.getElementById('joinRoomBtn');
        const invitationsList = document.getElementById('invitationsList');
        const emptyInvitations = document.getElementById('emptyInvitations');
        const invitationsGrid = document.getElementById('invitationsGrid');
        
        const roomNameInput = document.getElementById('roomName');
        const roomCodeInput = document.getElementById('roomCode');
        
        // Preview elements
        const previewRoomName = document.getElementById('previewRoomName');
        const previewCreator = document.getElementById('previewCreator');
        const previewMembers = document.getElementById('previewMembers');
        const previewStatus = document.getElementById('previewStatus');
        
        // Method selection
        codeMethodCard.addEventListener('click', function() {
            // Show code form
            codeForm.classList.add('active');
            invitationsList.style.display = 'none';
            
            // Update active state
            codeMethodCard.classList.add('active');
            inviteMethodCard.classList.remove('active');
            
            // Focus on first input
            setTimeout(() => {
                roomNameInput.focus();
            }, 300);
        });
        
        inviteMethodCard.addEventListener('click', function() {
            // Show invitations list
            invitationsList.style.display = 'block';
            codeForm.classList.remove('active');
            
            // Update active state
            inviteMethodCard.classList.add('active');
            codeMethodCard.classList.remove('active');
            
            // Show demo invitations
            emptyInvitations.style.display = 'none';
            invitationsGrid.style.display = 'grid';
        });
        
        // Back to methods
        backToMethodsBtn.addEventListener('click', function() {
            codeForm.classList.remove('active');
            invitationsList.style.display = 'none';
            codeMethodCard.classList.remove('active');
            inviteMethodCard.classList.remove('active');
        });
        
        // Auto-format room code
        roomCodeInput.addEventListener('input', function() {
            this.value = this.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
            
            // Check if code is complete
            if (this.value.length === 6) {
                validateRoomCode();
            } else {
                roomInfoPreview.classList.remove('active');
            }
        });
        
        // Validate room code (simulated for demo)
        function validateRoomCode() {
            const roomName = roomNameInput.value.trim();
            const roomCode = roomCodeInput.value;
            
            if (roomName && roomCode.length === 6) {
                // Simulate API call with sample data
                setTimeout(() => {
                    // Sample room data (in real app, this would come from backend)
                    const sampleRooms = {
                        'GOATRIP': {
                            name: 'Goa Trip 2024',
                            creator: 'Alex Johnson',
                            members: 4,
                            status: 'Active'
                        },
                        'OFFICE': {
                            name: 'Office Team Lunch',
                            creator: 'Sarah Williams',
                            members: 8,
                            status: 'Active'
                        },
                        'FAMILY': {
                            name: 'Family Gathering',
                            creator: 'Michael Brown',
                            members: 6,
                            status: 'Active'
                        }
                    };
                    
                    const roomData = sampleRooms[roomCode] || {
                        name: roomName,
                        creator: 'Unknown',
                        members: Math.floor(Math.random() * 10) + 1,
                        status: 'Active'
                    };
                    
                    // Update preview
                    previewRoomName.textContent = roomData.name;
                    previewCreator.textContent = roomData.creator;
                    previewMembers.textContent = `${roomData.members} members`;
                    previewStatus.textContent = roomData.status;
                    
                    // Set status color
                    if (roomData.status === 'Active') {
                        previewStatus.style.color = 'var(--success)';
                    } else {
                        previewStatus.style.color = 'var(--warning)';
                    }
                    
                    // Show preview
                    roomInfoPreview.classList.add('active');
                    
                }, 500);
            }
        }
        
        // Form validation
        joinRoomForm.addEventListener('submit', function(e) {
            const roomName = roomNameInput.value.trim();
            const roomCode = roomCodeInput.value.trim();
            
            if (!roomName) {
                e.preventDefault();
                showError('Please enter the room name');
                roomNameInput.focus();
                return;
            }
            
            if (!roomCode || roomCode.length !== 6) {
                e.preventDefault();
                showError('Please enter a valid 6-character room code');
                roomCodeInput.focus();
                return;
            }
            
            // Show loading state
            joinRoomBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Joining...';
            joinRoomBtn.disabled = true;
        });
        
        // Auto-fill demo data for testing
        function fillDemoData() {
            roomNameInput.value = 'Goa Trip 2024';
            roomCodeInput.value = 'GOATRIP';
            validateRoomCode();
        }
        
        // Quick fill for demo (Ctrl+D)
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === 'd') {
                e.preventDefault();
                fillDemoData();
            }
        });
        
        // Refresh invitations
        window.refreshInvitations = function() {
            const refreshBtn = document.querySelector('[onclick="refreshInvitations()"]');
            const originalHTML = refreshBtn.innerHTML;
            
            refreshBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Refreshing...';
            refreshBtn.disabled = true;
            
            // Simulate API call
            setTimeout(() => {
                refreshBtn.innerHTML = originalHTML;
                refreshBtn.disabled = false;
                
                // Show success message
                showFlash('Invitations refreshed successfully', 'success');
            }, 1500);
        };
        
        // Initialize with code method selected
        codeMethodCard.click();
        
        // Show error function
        function showError(message) {
            // Remove existing error
            const existingError = document.querySelector('.flash-error');
            if (existingError) existingError.remove();
            
            // Create error element
            const errorDiv = document.createElement('div');
            errorDiv.className = 'flash-message flash-error mb-4';
            errorDiv.innerHTML = `
                <i class="fas fa-exclamation-circle"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            // Insert after page header
            const pageHeader = document.querySelector('.page-header');
            pageHeader.parentNode.insertBefore(errorDiv, pageHeader.nextSibling);
            
            // Add close functionality
            errorDiv.querySelector('.flash-close').addEventListener('click', function() {
                errorDiv.style.opacity = '0';
                errorDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => errorDiv.remove(), 300);
            });
            
            // Auto remove after 5 seconds
            setTimeout(() => {
                if (errorDiv.parentElement) {
                    errorDiv.style.opacity = '0';
                    errorDiv.style.transform = 'translateY(-10px)';
                    setTimeout(() => errorDiv.remove(), 300);
                }
            }, 5000);
        }
        
        // Show flash message
        function showFlash(message, type = 'info') {
            const flashDiv = document.createElement('div');
            flashDiv.className = `flash-message flash-${type} mb-4`;
            flashDiv.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : 'info-circle'}"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            const pageHeader = document.querySelector('.page-header');
            pageHeader.parentNode.insertBefore(flashDiv, pageHeader.nextSibling);
            
            flashDiv.querySelector('.flash-close').addEventListener('click', function() {
                flashDiv.style.opacity = '0';
                flashDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => flashDiv.remove(), 300);
            });
            
            setTimeout(() => {
                if (flashDiv.parentElement) {
                    flashDiv.style.opacity = '0';
                    flashDiv.style.transform = 'translateY(-10px)';
                    setTimeout(() => flashDiv.remove(), 300);
                }
            }, 5000);
        }
        
        // Add animation to method cards
        const methodCards = document.querySelectorAll('.method-card');
        methodCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.2}s`;
            card.style.animation = 'fadeInUp 0.6s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 200));
        });
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\room\view.html
=============================
{% extends "layout.html" %}

{% block title %}{{ room.name }} - Expensio{% endblock %}

{% block extra_css %}
<style>
    /* Room View Specific Styles */
    .room-view-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 1.5rem;
        animation: fadeIn 0.5s ease;
    }
    
    /* Room Header */
    .room-header {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 2rem;
        border: 1px solid var(--card-border);
        position: relative;
        overflow: hidden;
    }
    
    .room-header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 4px;
        background: var(--secondary-gradient);
    }
    
    .room-title-section {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 1.5rem;
        flex-wrap: wrap;
        gap: 1rem;
    }
    
    .room-title {
        font-size: 2rem;
        font-weight: 700;
        margin: 0;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .room-code-badge {
        background: rgba(138, 43, 226, 0.2);
        color: var(--accent-purple);
        padding: 8px 16px;
        border-radius: 20px;
        font-family: monospace;
        font-size: 1.1rem;
        letter-spacing: 1px;
        font-weight: 600;
    }
    
    .room-description {
        color: var(--text-muted);
        font-size: 1.1rem;
        line-height: 1.6;
        margin-bottom: 1.5rem;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 10px;
        border-left: 3px solid var(--accent-blue);
    }
    
    /* Room Stats */
    .room-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        margin-bottom: 2rem;
    }
    
    .stat-card {
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
        padding: 1.5rem;
        text-align: center;
        transition: var(--transition);
        border: 1px solid transparent;
    }
    
    .stat-card:hover {
        transform: translateY(-3px);
        border-color: var(--accent-purple);
    }
    
    .stat-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1rem;
        font-size: 1.5rem;
    }
    
    .stat-value {
        font-size: 1.8rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        color: var(--accent-blue);
    }
    
    .stat-label {
        color: var(--text-muted);
        font-size: 0.95rem;
    }
    
    /* Room Actions */
    .room-actions {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        margin-bottom: 2rem;
    }
    
    .action-btn {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 12px 24px;
        border-radius: 12px;
        text-decoration: none;
        font-weight: 500;
        transition: var(--transition);
        border: 2px solid transparent;
    }
    
    .action-btn-primary {
        background: var(--secondary-gradient);
        color: white;
    }
    
    .action-btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 25px rgba(138, 43, 226, 0.4);
    }
    
    .action-btn-secondary {
        background: transparent;
        border-color: var(--accent-purple);
        color: var(--accent-purple);
    }
    
    .action-btn-secondary:hover {
        background: var(--accent-purple);
        color: white;
    }
    
    /* Main Content Grid */
    .room-content-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 2rem;
        margin-bottom: 3rem;
    }
    
    @media (max-width: 1024px) {
        .room-content-grid {
            grid-template-columns: 1fr;
        }
    }
    
    /* Expenses Section */
    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .section-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-blue);
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    /* Expenses Table */
    .expenses-table {
        background: var(--card-bg);
        border-radius: 15px;
        border: 1px solid var(--card-border);
        overflow: hidden;
    }
    
    .table-header {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1fr 1fr;
        padding: 1rem 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        font-weight: 600;
        color: var(--text-light);
        border-bottom: 1px solid var(--card-border);
    }
    
    .expense-row {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1fr 1fr;
        padding: 1rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        transition: var(--transition);
        align-items: center;
    }
    
    .expense-row:last-child {
        border-bottom: none;
    }
    
    .expense-row:hover {
        background: rgba(138, 43, 226, 0.05);
    }
    
    .expense-description {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .expense-category {
        display: inline-block;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 500;
    }
    
    .category-food { background: rgba(0, 255, 157, 0.1); color: var(--success); }
    .category-travel { background: rgba(0, 212, 255, 0.1); color: var(--accent-blue); }
    .category-shopping { background: rgba(138, 43, 226, 0.1); color: var(--accent-purple); }
    .category-other { background: rgba(255, 183, 77, 0.1); color: var(--warning); }
    
    .expense-amount {
        font-weight: 600;
        color: var(--accent-blue);
    }
    
    .expense-date {
        color: var(--text-muted);
        font-size: 0.9rem;
    }
    
    .expense-payer {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .payer-avatar {
        width: 30px;
        height: 30px;
        border-radius: 50%;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.9rem;
        font-weight: 600;
    }
    
    .expense-actions {
        display: flex;
        gap: 0.5rem;
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    
    .expense-row:hover .expense-actions {
        opacity: 1;
    }
    
    .action-icon {
        width: 32px;
        height: 32px;
        border-radius: 8px;
        background: rgba(255, 255, 255, 0.07);
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: var(--transition);
    }
    
    .action-icon:hover {
        background: var(--accent-purple);
        color: white;
    }
    
    /* Empty State */
    .empty-expenses {
        text-align: center;
        padding: 3rem;
        color: var(--text-muted);
    }
    
    .empty-expenses i {
        font-size: 3rem;
        margin-bottom: 1rem;
        opacity: 0.5;
    }
    
    /* Members Section */
    .members-section {
        background: var(--card-bg);
        border-radius: 15px;
        padding: 1.5rem;
        border: 1px solid var(--card-border);
    }
    
    .members-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    
    .member-card {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 12px;
        transition: var(--transition);
    }
    
    .member-card:hover {
        background: rgba(138, 43, 226, 0.1);
        transform: translateX(5px);
    }
    
    .member-avatar {
        width: 45px;
        height: 45px;
        border-radius: 50%;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
        font-size: 1.1rem;
    }
    
    .member-info {
        flex: 1;
    }
    
    .member-name {
        font-weight: 600;
        margin-bottom: 0.25rem;
    }
    
    .member-role {
        font-size: 0.85rem;
        color: var(--text-muted);
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .member-balance {
        text-align: right;
        min-width: 100px;
    }
    
    .balance-positive {
        color: var(--success);
        font-weight: 600;
    }
    
    .balance-negative {
        color: var(--danger);
        font-weight: 600;
    }
    
    .balance-neutral {
        color: var(--text-muted);
    }
    
    /* Settlement Section */
    .settlement-section {
        background: rgba(0, 255, 157, 0.05);
        border-radius: 15px;
        padding: 2rem;
        margin-top: 2rem;
        border: 1px solid rgba(0, 255, 157, 0.2);
    }
    
    .settlement-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1.5rem;
    }
    
    .settlement-header i {
        color: var(--success);
        font-size: 1.5rem;
    }
    
    .settlement-header h3 {
        margin: 0;
        color: var(--success);
    }
    
    .settlement-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    
    .settlement-item {
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 10px;
        margin-bottom: 0.75rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    
    .settlement-item:last-child {
        margin-bottom: 0;
    }
    
    .settlement-text {
        color: var(--text-light);
    }
    
    .settlement-amount {
        font-weight: 600;
        color: var(--accent-blue);
    }
    
    /* Budget Progress */
    .budget-progress {
        margin-top: 2rem;
    }
    
    .progress-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 0.75rem;
    }
    
    .progress-label {
        color: var(--text-light);
        font-weight: 500;
    }
    
    .progress-percentage {
        color: var(--accent-blue);
        font-weight: 600;
    }
    
    .progress-bar-container {
        height: 8px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 4px;
        overflow: hidden;
        margin-bottom: 0.5rem;
    }
    
    .progress-bar {
        height: 100%;
        background: var(--secondary-gradient);
        border-radius: 4px;
        transition: width 1s ease;
    }
    
    .progress-stats {
        display: flex;
        justify-content: space-between;
        font-size: 0.9rem;
        color: var(--text-muted);
    }
    
    /* Quick Stats */
    .quick-stats {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
        margin-top: 1.5rem;
    }
    
    .quick-stat {
        text-align: center;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.03);
        border-radius: 10px;
    }
    
    .quick-stat-value {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--accent-blue);
        margin-bottom: 0.25rem;
    }
    
    .quick-stat-label {
        font-size: 0.85rem;
        color: var(--text-muted);
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .room-view-container {
            padding: 0 1rem;
        }
        
        .room-header {
            padding: 1.5rem;
        }
        
        .room-title-section {
            flex-direction: column;
            align-items: flex-start;
        }
        
        .room-stats {
            grid-template-columns: repeat(2, 1fr);
        }
        
        .room-actions {
            flex-direction: column;
        }
        
        .table-header,
        .expense-row {
            grid-template-columns: 1fr;
            gap: 0.5rem;
        }
        
        .expense-row {
            padding: 1rem;
            position: relative;
        }
        
        .expense-actions {
            position: absolute;
            top: 0.5rem;
            right: 0.5rem;
            opacity: 1;
        }
        
        .quick-stats {
            grid-template-columns: 1fr;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="room-view-container">
    <!-- Room Header -->
    <div class="room-header">
        <div class="room-title-section">
            <div>
                <h1 class="room-title">{{ room.name }}</h1>
                <div class="room-code-badge mt-1">
                    <i class="fas fa-key"></i> {{ room.room_code }}
                </div>
            </div>
            <div class="room-actions">
                <a href="{{ url_for('add_expense', room_id=room.id) }}" class="action-btn action-btn-primary">
                    <i class="fas fa-plus-circle"></i> Add Expense
                </a>
                <a href="{{ url_for('export_room', room_id=room.id) }}" class="action-btn action-btn-secondary">
                    <i class="fas fa-download"></i> Export CSV
                </a>
                {% if is_admin %}
                <button class="action-btn action-btn-secondary" id="shareRoomBtn">
                    <i class="fas fa-share-alt"></i> Share Room
                </button>
                {% endif %}
            </div>
        </div>
        
        {% if room.description %}
        <div class="room-description">
            <i class="fas fa-info-circle"></i> {{ room.description }}
        </div>
        {% endif %}
        
        <!-- Room Stats -->
        <div class="room-stats">
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-users"></i>
                </div>
                <div class="stat-value">{{ members|length }}</div>
                <div class="stat-label">Total Members</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-receipt"></i>
                </div>
                <div class="stat-value">{{ expenses|length }}</div>
                <div class="stat-label">Total Expenses</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-money-bill-wave"></i>
                </div>
                <div class="stat-value">₹{{ "%.2f"|format(expenses|sum(attribute='amount')) }}</div>
                <div class="stat-label">Total Spent</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">
                    <i class="fas fa-calendar"></i>
                </div>
                <div class="stat-value">{{ room.created_at.strftime('%b %d') }}</div>
                <div class="stat-label">Created On</div>
            </div>
        </div>
    </div>
    
    <!-- Main Content Grid -->
    <div class="room-content-grid">
        <!-- Left Column: Expenses -->
        <div class="expenses-section">
            <div class="section-header">
                <h2 class="section-title">
                    <i class="fas fa-receipt"></i> Recent Expenses
                </h2>
                <div class="expense-filters">
                    <select class="form-control" style="width: auto; min-width: 150px;">
                        <option>All Categories</option>
                        <option>Food</option>
                        <option>Travel</option>
                        <option>Shopping</option>
                        <option>Other</option>
                    </select>
                </div>
            </div>
            
            {% if expenses %}
            <div class="expenses-table">
                <!-- Table Header -->
                <div class="table-header">
                    <div>Description</div>
                    <div>Amount</div>
                    <div>Category</div>
                    <div>Paid By</div>
                    <div>Date</div>
                    <div style="width: 60px;"></div>
                </div>
                
                <!-- Expense Rows -->
                {% for expense in expenses %}
                <div class="expense-row">
                    <div class="expense-description">
                        <i class="fas fa-{% if expense.category == 'Food' %}utensils{% elif expense.category == 'Travel' %}car{% elif expense.category == 'Shopping' %}shopping-bag{% else %}receipt{% endif %}"></i>
                        {{ expense.description }}
                    </div>
                    <div class="expense-amount">₹{{ "%.2f"|format(expense.amount) }}</div>
                    <div>
                        <span class="expense-category category-{{ expense.category|lower }}">
                            {{ expense.category }}
                        </span>
                    </div>
                    <div class="expense-payer">
                        <div class="payer-avatar">
                            {{ expense.payer.username[:1].upper() }}
                        </div>
                        {{ expense.payer.username }}
                    </div>
                    <div class="expense-date">
                        {{ expense.date.strftime('%b %d, %I:%M %p') }}
                    </div>
                    <div class="expense-actions">
                        {% if is_admin or expense.paid_by == current_user.id %}
                        <div class="action-icon" title="Edit">
                            <i class="fas fa-edit"></i>
                        </div>
                        <div class="action-icon" title="Delete">
                            <i class="fas fa-trash"></i>
                        </div>
                        {% endif %}
                    </div>
                </div>
                {% endfor %}
            </div>
            {% else %}
            <div class="empty-expenses">
                <i class="fas fa-receipt"></i>
                <h3>No Expenses Yet</h3>
                <p>Start by adding your first expense to this room.</p>
                <a href="{{ url_for('add_expense', room_id=room.id) }}" class="btn btn-primary mt-2">
                    <i class="fas fa-plus-circle"></i> Add First Expense
                </a>
            </div>
            {% endif %}
        </div>
        
        <!-- Right Column: Members & Info -->
        <div class="sidebar-section">
            <!-- Members List -->
            <div class="members-section">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="fas fa-users"></i> Room Members
                    </h2>
                    <span class="badge" style="background: var(--accent-purple); color: white; padding: 4px 12px; border-radius: 20px;">
                        {{ members|length }}
                    </span>
                </div>
                
                <div class="members-list">
                    {% for member in members %}
                    <div class="member-card">
                        <div class="member-avatar" style="background: {% if loop.index % 3 == 0 %}var(--secondary-gradient){% elif loop.index % 3 == 1 %}linear-gradient(135deg, #667eea 0%, #764ba2 100%){% else %}linear-gradient(135deg, #4facfe 0%, #00f2fe 100%){% endif %};">
                            {{ member.user.username[:1].upper() }}
                        </div>
                        <div class="member-info">
                            <div class="member-name">{{ member.user.username }}</div>
                            <div class="member-role">
                                {% if member.is_admin %}
                                <i class="fas fa-crown" style="color: gold;"></i>
                                <span>Admin</span>
                                {% else %}
                                <i class="fas fa-user"></i>
                                <span>Member</span>
                                {% endif %}
                            </div>
                        </div>
                        <div class="member-balance">
                            {% if member.balance > 0 %}
                            <div class="balance-positive">
                                +₹{{ "%.2f"|format(member.balance) }}
                            </div>
                            <small style="color: var(--text-muted); font-size: 0.8rem;">Gets</small>
                            {% elif member.balance < 0 %}
                            <div class="balance-negative">
                                -₹{{ "%.2f"|format(member.balance|abs) }}
                            </div>
                            <small style="color: var(--text-muted); font-size: 0.8rem;">Owes</small>
                            {% else %}
                            <div class="balance-neutral">₹0.00</div>
                            <small style="color: var(--text-muted); font-size: 0.8rem;">Settled</small>
                            {% endif %}
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
            
            <!-- Settlement Summary -->
            <div class="settlement-section">
                <div class="settlement-header">
                    <i class="fas fa-handshake"></i>
                    <h3>Settlement Summary</h3>
                </div>
                
                <ul class="settlement-list">
                    {% for member in members if member.balance != 0 %}
                    {% if member.balance < 0 %}
                    <li class="settlement-item">
                        <div class="settlement-text">
                            {{ member.user.username }} pays to:
                        </div>
                        <div class="settlement-amount">
                            ₹{{ "%.2f"|format(member.balance|abs) }}
                        </div>
                    </li>
                    {% endif %}
                    {% endfor %}
                    
                    {% if members|selectattr('balance', '!=', 0)|list|length == 0 %}
                    <li class="settlement-item">
                        <div class="settlement-text" style="color: var(--text-muted);">
                            All balances are settled
                        </div>
                        <i class="fas fa-check-circle" style="color: var(--success);"></i>
                    </li>
                    {% endif %}
                </ul>
            </div>
            
            <!-- Budget Progress -->
            {% if room.budget > 0 %}
            <div class="budget-progress">
                <div class="progress-header">
                    <div class="progress-label">Budget Progress</div>
                    <div class="progress-percentage">
                        {{ ((expenses|sum(attribute='amount') / room.budget) * 100)|round|int }}%
                    </div>
                </div>
                <div class="progress-bar-container">
                    <div class="progress-bar" style="width: {{ ((expenses|sum(attribute='amount') / room.budget) * 100)|round|int }}%;"></div>
                </div>
                <div class="progress-stats">
                    <div>Spent: ₹{{ "%.2f"|format(expenses|sum(attribute='amount')) }}</div>
                    <div>Budget: ₹{{ "%.2f"|format(room.budget) }}</div>
                </div>
                
                <!-- Quick Stats -->
                <div class="quick-stats">
                    <div class="quick-stat">
                        <div class="quick-stat-value">
                            ₹{{ "%.2f"|format(room.budget - expenses|sum(attribute='amount')) }}
                        </div>
                        <div class="quick-stat-label">Remaining</div>
                    </div>
                    <div class="quick-stat">
                        <div class="quick-stat-value">
                            ₹{{ "%.2f"|format((expenses|sum(attribute='amount') / members|length) if members|length > 0 else 0) }}
                        </div>
                        <div class="quick-stat-label">Per Person</div>
                    </div>
                </div>
            </div>
            {% endif %}
            
            <!-- Room Admin Info -->
            {% if is_admin %}
            <div class="members-section mt-3">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="fas fa-crown"></i> Room Management
                    </h2>
                </div>
                <div class="room-actions" style="flex-direction: column; margin: 0;">
                    <button class="action-btn action-btn-secondary w-100" id="inviteMembersBtn">
                        <i class="fas fa-user-plus"></i> Invite Members
                    </button>
                    <button class="action-btn action-btn-secondary w-100" id="editRoomBtn">
                        <i class="fas fa-cog"></i> Room Settings
                    </button>
                </div>
            </div>
            {% endif %}
        </div>
    </div>
    
    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                <div class="flash-message flash-{{ category }}">
                    <i class="fas fa-{% if category == 'success' %}check-circle{% elif category == 'error' %}exclamation-circle{% else %}info-circle{% endif %}"></i>
                    {{ message }}
                    <button class="flash-close">&times;</button>
                </div>
            {% endfor %}
        {% endif %}
    {% endwith %}
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Elements
        const shareRoomBtn = document.getElementById('shareRoomBtn');
        const inviteMembersBtn = document.getElementById('inviteMembersBtn');
        const editRoomBtn = document.getElementById('editRoomBtn');
        const expenseFilters = document.querySelector('.expense-filters select');
        
        // Share Room Functionality
        if (shareRoomBtn) {
            shareRoomBtn.addEventListener('click', function() {
                const roomCode = '{{ room.room_code }}';
                const roomName = '{{ room.name }}';
                const shareText = `Join my room "${roomName}" on Expensio! Room Code: ${roomCode}`;
                
                if (navigator.share) {
                    navigator.share({
                        title: 'Join my Expensio Room',
                        text: shareText,
                        url: window.location.href
                    });
                } else {
                    // Fallback: Copy to clipboard
                    navigator.clipboard.writeText(shareText).then(() => {
                        showFlash('Room code copied to clipboard! Share it with friends.', 'success');
                    });
                }
            });
        }
        
        // Invite Members
        if (inviteMembersBtn) {
            inviteMembersBtn.addEventListener('click', function() {
                const roomCode = '{{ room.room_code }}';
                const inviteModal = `
                    <div class="modal-overlay" id="inviteModal">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h3><i class="fas fa-user-plus"></i> Invite Members</h3>
                                <button class="modal-close">&times;</button>
                            </div>
                            <div class="modal-body">
                                <div class="input-group mb-3">
                                    <label>Room Code</label>
                                    <div class="code-display" style="font-family: monospace; font-size: 1.2rem; letter-spacing: 2px; padding: 12px; background: rgba(138, 43, 226, 0.1); border-radius: 10px; text-align: center;">
                                        {{ room.room_code }}
                                    </div>
                                    <small style="color: var(--text-muted); margin-top: 0.5rem; display: block;">
                                        Share this code with friends to invite them
                                    </small>
                                </div>
                                
                                <div class="share-buttons">
                                    <button class="btn btn-primary w-100 mb-2" onclick="shareViaWhatsApp()">
                                        <i class="fab fa-whatsapp"></i> Share via WhatsApp
                                    </button>
                                    <button class="btn btn-outline w-100" onclick="copyInviteLink()">
                                        <i class="fas fa-copy"></i> Copy Invite Link
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
                
                document.body.insertAdjacentHTML('beforeend', inviteModal);
                
                // Close modal
                document.querySelector('.modal-close').addEventListener('click', function() {
                    document.getElementById('inviteModal').remove();
                });
                
                document.querySelector('.modal-overlay').addEventListener('click', function(e) {
                    if (e.target === this) {
                        this.remove();
                    }
                });
            });
        }
        
        // Edit Room
        if (editRoomBtn) {
            editRoomBtn.addEventListener('click', function() {
                showFlash('Room settings feature coming soon!', 'info');
            });
        }
        
        // Expense filtering
        if (expenseFilters) {
            expenseFilters.addEventListener('change', function() {
                const category = this.value.toLowerCase();
                const expenseRows = document.querySelectorAll('.expense-row');
                
                expenseRows.forEach(row => {
                    if (category === 'all' || row.querySelector('.expense-category').textContent.toLowerCase() === category) {
                        row.style.display = 'grid';
                    } else {
                        row.style.display = 'none';
                    }
                });
            });
        }
        
        // Animate progress bars
        const progressBars = document.querySelectorAll('.progress-bar');
        progressBars.forEach(bar => {
            const width = bar.style.width;
            bar.style.width = '0';
            setTimeout(() => {
                bar.style.width = width;
            }, 300);
        });
        
        // Add hover effects to stat cards
        const statCards = document.querySelectorAll('.stat-card');
        statCards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px)';
                this.style.boxShadow = '0 10px 25px rgba(138, 43, 226, 0.2)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0)';
                this.style.boxShadow = 'none';
            });
        });
        
        // Add animation to members
        const memberCards = document.querySelectorAll('.member-card');
        memberCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.1}s`;
            card.style.animation = 'fadeIn 0.5s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 100));
        });
        
        // Expense row actions
        const editButtons = document.querySelectorAll('.action-icon:nth-child(1)');
        const deleteButtons = document.querySelectorAll('.action-icon:nth-child(2)');
        
        editButtons.forEach(btn => {
            btn.addEventListener('click', function() {
                const row = this.closest('.expense-row');
                const description = row.querySelector('.expense-description').textContent.trim();
                showFlash(`Editing expense: ${description}`, 'info');
            });
        });
        
        deleteButtons.forEach(btn => {
            btn.addEventListener('click', function() {
                const row = this.closest('.expense-row');
                const description = row.querySelector('.expense-description').textContent.trim();
                
                if (confirm(`Are you sure you want to delete "${description}"?`)) {
                    row.style.opacity = '0.5';
                    setTimeout(() => {
                        row.style.display = 'none';
                        showFlash('Expense deleted successfully', 'success');
                    }, 300);
                }
            });
        });
        
        // Helper functions for sharing
        window.shareViaWhatsApp = function() {
            const text = `Join my room "{{ room.name }}" on Expensio! Room Code: {{ room.room_code }}`;
            const url = `https://wa.me/?text=${encodeURIComponent(text)}`;
            window.open(url, '_blank');
        };
        
        window.copyInviteLink = function() {
            const text = `Join my room "{{ room.name }}" on Expensio!\nRoom Code: {{ room.room_code }}\n\nJoin here: ${window.location.origin}/room/join`;
            navigator.clipboard.writeText(text).then(() => {
                showFlash('Invite link copied to clipboard!', 'success');
            });
        };
        
        // Show flash message
        function showFlash(message, type = 'info') {
            // Remove existing flash
            const existingFlash = document.querySelector('.flash-message');
            if (existingFlash) existingFlash.remove();
            
            const flashDiv = document.createElement('div');
            flashDiv.className = `flash-message flash-${type}`;
            flashDiv.innerHTML = `
                <i class="fas fa-${type === 'success' ? 'check-circle' : 'info-circle'}"></i>
                ${message}
                <button class="flash-close">&times;</button>
            `;
            
            document.querySelector('.room-view-container').appendChild(flashDiv);
            
            flashDiv.querySelector('.flash-close').addEventListener('click', function() {
                flashDiv.style.opacity = '0';
                flashDiv.style.transform = 'translateY(-20px)';
                setTimeout(() => flashDiv.remove(), 300);
            });
            
            setTimeout(() => {
                if (flashDiv.parentElement) {
                    flashDiv.style.opacity = '0';
                    flashDiv.style.transform = 'translateY(-20px)';
                    setTimeout(() => flashDiv.remove(), 300);
                }
            }, 5000);
        }
        
        // Add modal styles
        const modalStyles = `
            <style>
                .modal-overlay {
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 100%;
                    height: 100%;
                    background: rgba(0, 0, 0, 0.7);
                    backdrop-filter: blur(5px);
                    display: flex;
                    align-items: center;
                    justify-content: center;
                    z-index: 2000;
                    animation: fadeIn 0.3s ease;
                }
                
                .modal-content {
                    background: var(--card-bg);
                    border-radius: 20px;
                    padding: 2rem;
                    max-width: 500px;
                    width: 90%;
                    border: 1px solid var(--card-border);
                    box-shadow: var(--shadow);
                    animation: slideInUp 0.3s ease;
                }
                
                .modal-header {
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                    margin-bottom: 1.5rem;
                    padding-bottom: 1rem;
                    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
                }
                
                .modal-header h3 {
                    margin: 0;
                    color: var(--accent-blue);
                    display: flex;
                    align-items: center;
                    gap: 0.75rem;
                }
                
                .modal-close {
                    background: none;
                    border: none;
                    color: var(--text-muted);
                    font-size: 1.5rem;
                    cursor: pointer;
                    padding: 0;
                    line-height: 1;
                }
                
                .modal-close:hover {
                    color: var(--accent-blue);
                }
                
                @keyframes slideInUp {
                    from {
                        transform: translateY(30px);
                        opacity: 0;
                    }
                    to {
                        transform: translateY(0);
                        opacity: 1;
                    }
                }
            </style>
        `;
        
        document.head.insertAdjacentHTML('beforeend', modalStyles);
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\about.html
=============================
{% extends "layout.html" %}

{% block title %}About Expensio - Smart Planning & Budgeting{% endblock %}

{% block extra_css %}
<style>
    /* About Page Specific Styles */
    .about-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1.5rem;
        animation: fadeIn 0.5s ease;
    }
    
    /* Hero Section */
    .hero-section {
        text-align: center;
        padding: 4rem 2rem;
        margin-bottom: 3rem;
        background: linear-gradient(135deg, 
            rgba(15, 12, 41, 0.9) 0%, 
            rgba(48, 43, 99, 0.9) 50%, 
            rgba(36, 36, 62, 0.9) 100%);
        border-radius: 25px;
        position: relative;
        overflow: hidden;
        border: 1px solid var(--card-border);
    }
    
    .hero-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: 
            radial-gradient(circle at 20% 80%, rgba(138, 43, 226, 0.3) 0%, transparent 50%),
            radial-gradient(circle at 80% 20%, rgba(0, 212, 255, 0.3) 0%, transparent 50%);
        z-index: -1;
    }
    
    .hero-logo {
        width: 100px;
        height: 100px;
        border-radius: 25px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 2rem;
        font-size: 3rem;
        box-shadow: 0 15px 35px rgba(138, 43, 226, 0.4);
    }
    
    .hero-title {
        font-size: 3.5rem;
        font-weight: 800;
        margin-bottom: 1rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        line-height: 1.2;
    }
    
    .hero-subtitle {
        font-size: 1.3rem;
        color: var(--text-muted);
        max-width: 600px;
        margin: 0 auto 2rem;
        line-height: 1.6;
    }
    
    .hero-tagline {
        display: inline-block;
        padding: 8px 24px;
        background: rgba(138, 43, 226, 0.2);
        border-radius: 20px;
        font-size: 1.1rem;
        color: var(--accent-purple);
        font-weight: 600;
        letter-spacing: 1px;
    }
    
    /* Mission Section */
    .mission-section {
        text-align: center;
        margin-bottom: 4rem;
    }
    
    .section-title {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        color: var(--accent-blue);
        position: relative;
        display: inline-block;
    }
    
    .section-title::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 4px;
        background: var(--secondary-gradient);
        border-radius: 2px;
    }
    
    .mission-statement {
        font-size: 1.2rem;
        color: var(--text-light);
        max-width: 800px;
        margin: 0 auto 3rem;
        line-height: 1.8;
    }
    
    /* Features Grid */
    .features-section {
        margin-bottom: 4rem;
    }
    
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
    }
    
    .feature-card {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2.5rem 2rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    
    .feature-card:hover {
        transform: translateY(-10px);
        border-color: var(--accent-purple);
        box-shadow: 0 20px 40px rgba(138, 43, 226, 0.3);
    }
    
    .feature-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 5px;
        background: var(--secondary-gradient);
    }
    
    .feature-icon {
        width: 80px;
        height: 80px;
        border-radius: 20px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        font-size: 2rem;
    }
    
    .feature-title {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 1rem;
        color: var(--text-light);
    }
    
    .feature-description {
        color: var(--text-muted);
        line-height: 1.6;
        font-size: 1rem;
    }
    
    /* How It Works */
    .how-it-works {
        margin-bottom: 4rem;
    }
    
    .steps-container {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        max-width: 800px;
        margin: 3rem auto 0;
        position: relative;
    }
    
    .steps-container::before {
        content: '';
        position: absolute;
        left: 40px;
        top: 0;
        bottom: 0;
        width: 2px;
        background: var(--card-border);
        z-index: -1;
    }
    
    .step-card {
        display: flex;
        gap: 2rem;
        align-items: flex-start;
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
        position: relative;
    }
    
    .step-card:hover {
        transform: translateX(10px);
        border-color: var(--accent-purple);
    }
    
    .step-number {
        width: 80px;
        height: 80px;
        border-radius: 20px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        font-weight: 700;
        flex-shrink: 0;
        color: white;
    }
    
    .step-content {
        flex: 1;
    }
    
    .step-title {
        font-size: 1.5rem;
        font-weight: 600;
        margin-bottom: 1rem;
        color: var(--accent-blue);
    }
    
    .step-description {
        color: var(--text-muted);
        line-height: 1.6;
        font-size: 1.05rem;
    }
    
    /* Technology Stack */
    .tech-stack {
        margin-bottom: 4rem;
    }
    
    .tech-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
        margin-top: 3rem;
    }
    
    .tech-card {
        background: var(--card-bg);
        border-radius: 15px;
        padding: 2rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
        text-align: center;
    }
    
    .tech-card:hover {
        transform: translateY(-5px);
        border-color: var(--accent-blue);
    }
    
    .tech-icon {
        font-size: 3rem;
        margin-bottom: 1rem;
        color: var(--accent-blue);
    }
    
    .tech-name {
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: var(--text-light);
    }
    
    .tech-purpose {
        color: var(--text-muted);
        font-size: 0.9rem;
    }
    
    /* Team Section */
    .team-section {
        margin-bottom: 4rem;
    }
    
    .team-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 2rem;
        margin-top: 3rem;
    }
    
    .team-card {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
        text-align: center;
    }
    
    .team-card:hover {
        transform: translateY(-5px);
        border-color: var(--accent-purple);
    }
    
    .team-avatar {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        background: var(--secondary-gradient);
        margin: 0 auto 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2.5rem;
        font-weight: 600;
        color: white;
    }
    
    .team-name {
        font-size: 1.3rem;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: var(--text-light);
    }
    
    .team-role {
        color: var(--accent-blue);
        font-weight: 500;
        margin-bottom: 1rem;
    }
    
    .team-bio {
        color: var(--text-muted);
        font-size: 0.95rem;
        line-height: 1.5;
    }
    
    /* CTA Section */
    .cta-section {
        text-align: center;
        padding: 4rem 2rem;
        background: linear-gradient(135deg, 
            rgba(138, 43, 226, 0.1) 0%, 
            rgba(75, 0, 130, 0.1) 100%);
        border-radius: 25px;
        margin-bottom: 4rem;
        border: 1px solid rgba(138, 43, 226, 0.2);
    }
    
    .cta-title {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 1rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .cta-description {
        font-size: 1.2rem;
        color: var(--text-muted);
        max-width: 600px;
        margin: 0 auto 2rem;
    }
    
    .cta-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }
    
    /* Footer Links */
    .footer-links {
        display: flex;
        justify-content: center;
        gap: 2rem;
        margin-top: 3rem;
        padding-top: 3rem;
        border-top: 1px solid var(--card-border);
        flex-wrap: wrap;
    }
    
    .footer-link {
        color: var(--text-muted);
        text-decoration: none;
        transition: var(--transition);
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .footer-link:hover {
        color: var(--accent-blue);
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .about-container {
            padding: 0 1rem;
        }
        
        .hero-title {
            font-size: 2.5rem;
        }
        
        .hero-subtitle {
            font-size: 1.1rem;
        }
        
        .section-title {
            font-size: 2rem;
        }
        
        .step-card {
            flex-direction: column;
            text-align: center;
        }
        
        .steps-container::before {
            display: none;
        }
        
        .step-number {
            margin: 0 auto;
        }
        
        .cta-title {
            font-size: 2rem;
        }
        
        .cta-buttons {
            flex-direction: column;
            align-items: center;
        }
        
        .cta-buttons .btn {
            width: 100%;
            max-width: 300px;
        }
        
        .features-grid,
        .tech-grid,
        .team-grid {
            grid-template-columns: 1fr;
        }
    }
    
    @media (max-width: 480px) {
        .hero-title {
            font-size: 2rem;
        }
        
        .section-title {
            font-size: 1.8rem;
        }
        
        .feature-card,
        .step-card,
        .tech-card,
        .team-card {
            padding: 1.5rem;
        }
        
        .footer-links {
            flex-direction: column;
            align-items: center;
            gap: 1rem;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="about-container">
    <!-- Hero Section -->
    <div class="hero-section">
        <div class="hero-logo">
            <i class="fas fa-money-bill-wave"></i>
        </div>
        <h1 class="hero-title">EXPENSIO</h1>
        <p class="hero-subtitle">
            Smart Planning & Budgeting for Groups and Individuals. 
            Track, Split, and Manage Expenses with Ease.
        </p>
        <div class="hero-tagline">
            <i class="fas fa-bolt"></i> Simplify Your Finances
        </div>
    </div>
    
    <!-- Mission Statement -->
    <div class="mission-section">
        <h2 class="section-title">Our Mission</h2>
        <p class="mission-statement">
            At Expensio, we believe managing finances should be simple, transparent, and stress-free. 
            Whether you're planning a trip with friends, managing household expenses, or tracking personal 
            spending, our platform provides the tools you need to stay in control of your money.
        </p>
    </div>
    
    <!-- Key Features -->
    <div class="features-section">
        <h2 class="section-title">Why Choose Expensio?</h2>
        
        <div class="features-grid">
            <!-- Feature 1 -->
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h3 class="feature-title">Group Expense Splitting</h3>
                <p class="feature-description">
                    Create rooms for events and automatically split expenses equally among members. 
                    No more manual calculations or confusion about who owes what.
                </p>
            </div>
            
            <!-- Feature 2 -->
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-chart-pie"></i>
                </div>
                <h3 class="feature-title">Visual Insights</h3>
                <p class="feature-description">
                    Get beautiful charts and graphs that show your spending patterns. 
                    Understand where your money goes with intuitive visualizations.
                </p>
            </div>
            
            <!-- Feature 3 -->
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-file-export"></i>
                </div>
                <h3 class="feature-title">Export & Reports</h3>
                <p class="feature-description">
                    Download detailed expense reports as CSV files. 
                    Perfect for record keeping, tax purposes, or financial analysis.
                </p>
            </div>
            
            <!-- Feature 4 -->
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-wallet"></i>
                </div>
                <h3 class="feature-title">Personal Budgeting</h3>
                <p class="feature-description">
                    Set monthly budgets and track your personal expenses. 
                    Get alerts and insights to help you stay within your financial goals.
                </p>
            </div>
            
            <!-- Feature 5 -->
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-mobile-alt"></i>
                </div>
                <h3 class="feature-title">Responsive Design</h3>
                <p class="feature-description">
                    Access Expensio from any device – desktop, tablet, or mobile. 
                    Our responsive design ensures a seamless experience everywhere.
                </p>
            </div>
            
            <!-- Feature 6 -->
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <h3 class="feature-title">Secure & Private</h3>
                <p class="feature-description">
                    Your financial data is safe with us. We use industry-standard 
                    security practices to protect your information and ensure privacy.
                </p>
            </div>
        </div>
    </div>
    
    <!-- How It Works -->
    <div class="how-it-works">
        <h2 class="section-title">How It Works</h2>
        
        <div class="steps-container">
            <!-- Step 1 -->
            <div class="step-card">
                <div class="step-number">1</div>
                <div class="step-content">
                    <h3 class="step-title">Create an Account</h3>
                    <p class="step-description">
                        Sign up for free and create your profile. It takes less than a minute 
                        to get started with Expensio.
                    </p>
                </div>
            </div>
            
            <!-- Step 2 -->
            <div class="step-card">
                <div class="step-number">2</div>
                <div class="step-content">
                    <h3 class="step-title">Create or Join a Room</h3>
                    <p class="step-description">
                        Create a room for your event or join an existing room using a unique 
                        room code shared by friends.
                    </p>
                </div>
            </div>
            
            <!-- Step 3 -->
            <div class="step-card">
                <div class="step-number">3</div>
                <div class="step-content">
                    <h3 class="step-title">Add Expenses</h3>
                    <p class="step-description">
                        Start adding expenses to your room. Include descriptions, amounts, 
                        categories, and upload receipts if needed.
                    </p>
                </div>
            </div>
            
            <!-- Step 4 -->
            <div class="step-card">
                <div class="step-number">4</div>
                <div class="step-content">
                    <h3 class="step-title">Split Automatically</h3>
                    <p class="step-description">
                        Expensio automatically calculates how much each person owes or 
                        should receive, eliminating manual calculations.
                    </p>
                </div>
            </div>
            
            <!-- Step 5 -->
            <div class="step-card">
                <div class="step-number">5</div>
                <div class="step-content">
                    <h3 class="step-title">Track & Analyze</h3>
                    <p class="step-description">
                        View charts, export reports, and gain insights into your spending 
                        patterns. Stay on top of your finances effortlessly.
                    </p>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Technology Stack -->
    <div class="tech-stack">
        <h2 class="section-title">Built With Modern Technology</h2>
        
        <div class="tech-grid">
            <!-- Tech 1 -->
            <div class="tech-card">
                <div class="tech-icon">
                    <i class="fab fa-python"></i>
                </div>
                <h3 class="tech-name">Python Flask</h3>
                <p class="tech-purpose">Backend Framework</p>
            </div>
            
            <!-- Tech 2 -->
            <div class="tech-card">
                <div class="tech-icon">
                    <i class="fas fa-database"></i>
                </div>
                <h3 class="tech-name">SQLite/PostgreSQL</h3>
                <p class="tech-purpose">Database</p>
            </div>
            
            <!-- Tech 3 -->
            <div class="tech-card">
                <div class="tech-icon">
                    <i class="fab fa-js"></i>
                </div>
                <h3 class="tech-name">JavaScript</h3>
                <p class="tech-purpose">Frontend Interactivity</p>
            </div>
            
            <!-- Tech 4 -->
            <div class="tech-card">
                <div class="tech-icon">
                    <i class="fab fa-html5"></i>
                </div>
                <h3 class="tech-name">HTML5 & CSS3</h3>
                <p class="tech-purpose">Modern Web Standards</p>
            </div>
            
            <!-- Tech 5 -->
            <div class="tech-card">
                <div class="tech-icon">
                    <i class="fas fa-chart-bar"></i>
                </div>
                <h3 class="tech-name">Chart.js</h3>
                <p class="tech-purpose">Data Visualization</p>
            </div>
            
            <!-- Tech 6 -->
            <div class="tech-card">
                <div class="tech-icon">
                    <i class="fas fa-palette"></i>
                </div>
                <h3 class="tech-name">Custom CSS</h3>
                <p class="tech-purpose">Aesthetic Design</p>
            </div>
        </div>
    </div>
    
    <!-- Team Section -->
    <div class="team-section">
        <h2 class="section-title">Meet the Creator</h2>
        
        <div class="team-grid">
            <!-- Team Member 1 -->
            <div class="team-card">
                <div class="team-avatar">
                    H
                </div>
                <h3 class="team-name">Harini</h3>
                <div class="team-role">Full Stack Developer</div>
                <p class="team-bio">
                    Passionate about creating beautiful, functional web applications 
                    that solve real-world problems. Believes in clean code and user-centric design.
                </p>
            </div>
            
            <!-- Team Member 2 -->
            <div class="team-card">
                <div class="team-avatar">
                    <i class="fas fa-users"></i>
                </div>
                <h3 class="team-name">Our Community</h3>
                <div class="team-role">Users & Contributors</div>
                <p class="team-bio">
                    Expensio is built for and with feedback from our amazing user community. 
                    Your suggestions help shape the future of our platform.
                </p>
            </div>
        </div>
    </div>
    
    <!-- CTA Section -->
    <div class="cta-section">
        <h2 class="cta-title">Ready to Simplify Your Finances?</h2>
        <p class="cta-description">
            Join thousands of users who are already managing their expenses smarter with Expensio. 
            It's free, easy to use, and packed with powerful features.
        </p>
        <div class="cta-buttons">
            {% if current_user.is_authenticated %}
            <a href="{{ url_for('dashboard') }}" class="btn btn-primary btn-lg">
                <i class="fas fa-rocket"></i> Go to Dashboard
            </a>
            <a href="{{ url_for('create_room') }}" class="btn btn-outline btn-lg">
                <i class="fas fa-plus-circle"></i> Create Your First Room
            </a>
            {% else %}
            <a href="{{ url_for('signup') }}" class="btn btn-primary btn-lg">
                <i class="fas fa-user-plus"></i> Sign Up Free
            </a>
            <a href="{{ url_for('login') }}" class="btn btn-outline btn-lg">
                <i class="fas fa-sign-in-alt"></i> Login to Account
            </a>
            {% endif %}
        </div>
    </div>
    
    <!-- Footer Links -->
    <div class="footer-links">
        <a href="{{ url_for('dashboard') }}" class="footer-link">
            <i class="fas fa-home"></i> Dashboard
        </a>
        <a href="{{ url_for('charts') }}" class="footer-link">
            <i class="fas fa-chart-pie"></i> Analytics
        </a>
        <a href="#" class="footer-link">
            <i class="fas fa-question-circle"></i> Help Center
        </a>
        <a href="#" class="footer-link">
            <i class="fas fa-file-contract"></i> Privacy Policy
        </a>
        <a href="#" class="footer-link">
            <i class="fas fa-envelope"></i> Contact Us
        </a>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Add animations to feature cards
        const featureCards = document.querySelectorAll('.feature-card');
        featureCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.1}s`;
            card.style.animation = 'fadeInUp 0.6s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 100));
        });
        
        // Add animations to step cards
        const stepCards = document.querySelectorAll('.step-card');
        stepCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.15}s`;
            card.style.animation = 'fadeInRight 0.6s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 150));
        });
        
        // Add animations to tech cards
        const techCards = document.querySelectorAll('.tech-card');
        techCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.1}s`;
            card.style.animation = 'fadeInUp 0.5s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 100));
        });
        
        // Add animations to team cards
        const teamCards = document.querySelectorAll('.team-card');
        teamCards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.2}s`;
            card.style.animation = 'fadeInUp 0.6s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 200));
        });
        
        // Add keyframe animations if not present
        if (!document.querySelector('#aboutAnimations')) {
            const animations = `
                <style id="aboutAnimations">
                    @keyframes fadeInRight {
                        from {
                            opacity: 0;
                            transform: translateX(-30px);
                        }
                        to {
                            opacity: 1;
                            transform: translateX(0);
                        }
                    }
                    
                    @keyframes fadeInUp {
                        from {
                            opacity: 0;
                            transform: translateY(30px);
                        }
                        to {
                            opacity: 1;
                            transform: translateY(0);
                        }
                    }
                </style>
            `;
            document.head.insertAdjacentHTML('beforeend', animations);
        }
        
        // Add interactive hover effects
        const cards = document.querySelectorAll('.feature-card, .tech-card, .team-card');
        cards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.zIndex = '10';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.zIndex = '';
            });
        });
        
        // Add scroll animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };
        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.animationPlayState = 'running';
                }
            });
        }, observerOptions);
        
        // Observe all animated elements
        document.querySelectorAll('.feature-card, .step-card, .tech-card, .team-card').forEach(el => {
            observer.observe(el);
        });
        
        // Add smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function(e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                if (targetId !== '#') {
                    const targetElement = document.querySelector(targetId);
                    if (targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                }
            });
        });
        
        // Add typing effect to hero tagline
        const tagline = document.querySelector('.hero-tagline');
        const originalText = tagline.textContent;
        tagline.textContent = '';
        
        let i = 0;
        function typeWriter() {
            if (i < originalText.length) {
                tagline.textContent += originalText.charAt(i);
                i++;
                setTimeout(typeWriter, 50);
            }
        }
        
        // Start typing after a delay
        setTimeout(typeWriter, 1000);
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\charts.html
=============================
{% extends "layout.html" %}

{% block title %}Charts & Insights - Expensio{% endblock %}

{% block extra_css %}
<style>
    /* Charts Page Specific Styles */
    .charts-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 1.5rem;
        animation: fadeIn 0.5s ease;
    }
    
    .page-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    
    .page-header h1 {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .page-header p {
        color: var(--text-muted);
        font-size: 1.1rem;
        max-width: 600px;
        margin: 0 auto;
    }
    
    /* Time Range Selector */
    .time-selector {
        display: flex;
        justify-content: center;
        gap: 1rem;
        margin-bottom: 2rem;
        flex-wrap: wrap;
    }
    
    .time-btn {
        padding: 10px 24px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid var(--card-border);
        border-radius: 10px;
        color: var(--text-muted);
        cursor: pointer;
        transition: var(--transition);
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .time-btn:hover {
        background: rgba(138, 43, 226, 0.1);
        color: var(--accent-purple);
    }
    
    .time-btn.active {
        background: var(--secondary-gradient);
        color: white;
        border-color: transparent;
    }
    
    /* Charts Grid */
    .charts-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
        gap: 2rem;
        margin-bottom: 3rem;
    }
    
    @media (max-width: 768px) {
        .charts-grid {
            grid-template-columns: 1fr;
        }
    }
    
    /* Chart Card */
    .chart-card {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
    }
    
    .chart-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 35px rgba(138, 43, 226, 0.2);
    }
    
    .chart-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .chart-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-blue);
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .chart-actions {
        display: flex;
        gap: 0.5rem;
    }
    
    .chart-action-btn {
        width: 36px;
        height: 36px;
        border-radius: 8px;
        background: rgba(255, 255, 255, 0.07);
        border: none;
        color: var(--text-muted);
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: var(--transition);
    }
    
    .chart-action-btn:hover {
        background: var(--accent-purple);
        color: white;
    }
    
    /* Chart Container */
    .chart-wrapper {
        position: relative;
        height: 300px;
        margin: 0 auto;
    }
    
    /* Insights Grid */
    .insights-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1.5rem;
        margin-bottom: 3rem;
    }
    
    .insight-card {
        background: var(--card-bg);
        border-radius: 15px;
        padding: 1.5rem;
        border: 1px solid var(--card-border);
        transition: var(--transition);
    }
    
    .insight-card:hover {
        transform: translateY(-3px);
        border-color: var(--accent-purple);
    }
    
    .insight-header {
        display: flex;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1rem;
    }
    
    .insight-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        background: var(--secondary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
    }
    
    .insight-title {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--text-light);
        margin: 0;
    }
    
    .insight-value {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .insight-description {
        color: var(--text-muted);
        font-size: 0.95rem;
        line-height: 1.5;
    }
    
    .insight-trend {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        margin-top: 0.5rem;
        font-size: 0.9rem;
    }
    
    .trend-up {
        color: var(--success);
    }
    
    .trend-down {
        color: var(--danger);
    }
    
    /* Category Breakdown */
    .category-breakdown {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 3rem;
        border: 1px solid var(--card-border);
    }
    
    .breakdown-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
    }
    
    .breakdown-title {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-blue);
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
    
    .category-list {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    
    .category-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 12px;
        transition: var(--transition);
    }
    
    .category-item:hover {
        background: rgba(138, 43, 226, 0.1);
        transform: translateX(5px);
    }
    
    .category-color {
        width: 16px;
        height: 16px;
        border-radius: 4px;
        flex-shrink: 0;
    }
    
    .category-info {
        flex: 1;
    }
    
    .category-name {
        font-weight: 500;
        margin-bottom: 0.25rem;
        color: var(--text-light);
    }
    
    .category-stats {
        display: flex;
        justify-content: space-between;
        font-size: 0.9rem;
        color: var(--text-muted);
    }
    
    .progress-bar {
        height: 6px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 3px;
        overflow: hidden;
        margin-top: 0.5rem;
    }
    
    .progress-fill {
        height: 100%;
        border-radius: 3px;
        transition: width 1s ease;
    }
    
    /* Comparison Section */
    .comparison-section {
        background: var(--card-bg);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 3rem;
        border: 1px solid var(--card-border);
    }
    
    .comparison-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
    }
    
    .comparison-card {
        text-align: center;
        padding: 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 15px;
    }
    
    .comparison-period {
        font-size: 1rem;
        color: var(--text-muted);
        margin-bottom: 1rem;
    }
    
    .comparison-amount {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 1rem;
        color: var(--accent-blue);
    }
    
    .comparison-change {
        display: inline-block;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: 500;
    }
    
    .change-positive {
        background: rgba(0, 255, 157, 0.1);
        color: var(--success);
    }
    
    .change-negative {
        background: rgba(255, 107, 139, 0.1);
        color: var(--danger);
    }
    
    /* Export Section */
    .export-section {
        background: rgba(138, 43, 226, 0.05);
        border-radius: 20px;
        padding: 2rem;
        margin-bottom: 3rem;
        border: 1px solid rgba(138, 43, 226, 0.2);
        text-align: center;
    }
    
    .export-header {
        margin-bottom: 1.5rem;
    }
    
    .export-header h2 {
        color: var(--accent-purple);
        margin-bottom: 0.5rem;
    }
    
    .export-header p {
        color: var(--text-muted);
        max-width: 600px;
        margin: 0 auto;
    }
    
    .export-options {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
    }
    
    /* No Data State */
    .no-data-state {
        text-align: center;
        padding: 4rem 2rem;
        color: var(--text-muted);
    }
    
    .no-data-state i {
        font-size: 4rem;
        margin-bottom: 1rem;
        opacity: 0.5;
    }
    
    .no-data-state h3 {
        margin-bottom: 1rem;
        color: var(--text-light);
    }
    
    /* Chart Colors */
    .chart-colors {
        display: flex;
        gap: 1rem;
        margin-top: 1rem;
        flex-wrap: wrap;
    }
    
    .color-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.9rem;
        color: var(--text-muted);
    }
    
    .color-swatch {
        width: 12px;
        height: 12px;
        border-radius: 3px;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .charts-container {
            padding: 0 1rem;
        }
        
        .charts-grid {
            gap: 1.5rem;
        }
        
        .chart-card {
            padding: 1.5rem;
        }
        
        .time-selector {
            gap: 0.5rem;
        }
        
        .time-btn {
            padding: 8px 16px;
            font-size: 0.9rem;
        }
        
        .chart-wrapper {
            height: 250px;
        }
        
        .insights-grid {
            grid-template-columns: 1fr;
        }
        
        .comparison-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="charts-container">
    <!-- Page Header -->
    <div class="page-header">
        <h1>Expense Insights & Analytics</h1>
        <p>Visualize your spending patterns, track trends, and gain valuable insights into your expenses.</p>
    </div>
    
    <!-- Time Range Selector -->
    <div class="time-selector">
        <button class="time-btn active" data-range="week">
            <i class="fas fa-calendar-week"></i> This Week
        </button>
        <button class="time-btn" data-range="month">
            <i class="fas fa-calendar-alt"></i> This Month
        </button>
        <button class="time-btn" data-range="quarter">
            <i class="fas fa-chart-line"></i> Last 3 Months
        </button>
        <button class="time-btn" data-range="year">
            <i class="fas fa-calendar-year"></i> This Year
        </button>
        <button class="time-btn" data-range="all">
            <i class="fas fa-infinity"></i> All Time
        </button>
    </div>
    
    <!-- Flash Messages -->
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                <div class="flash-message flash-{{ category }} mb-4">
                    <i class="fas fa-{% if category == 'success' %}check-circle{% elif category == 'error' %}exclamation-circle{% else %}info-circle{% endif %}"></i>
                    {{ message }}
                    <button class="flash-close">&times;</button>
                </div>
            {% endfor %}
        {% endif %}
    {% endwith %}
    
    {% if categories or monthly_data %}
    <!-- Charts Grid -->
    <div class="charts-grid">
        <!-- Pie Chart Card -->
        <div class="chart-card">
            <div class="chart-header">
                <h2 class="chart-title">
                    <i class="fas fa-chart-pie"></i> Expense Distribution
                </h2>
                <div class="chart-actions">
                    <button class="chart-action-btn" title="Download as PNG" onclick="downloadChart('pieChart', 'expense-distribution.png')">
                        <i class="fas fa-download"></i>
                    </button>
                    <button class="chart-action-btn" title="Refresh Data" onclick="refreshChart('pieChart')">
                        <i class="fas fa-sync-alt"></i>
                    </button>
                </div>
            </div>
            
            <div class="chart-wrapper">
                <canvas id="pieChart"></canvas>
            </div>
            
            <div class="chart-colors" id="pieChartColors">
                <!-- Colors will be populated by JavaScript -->
            </div>
        </div>
        
        <!-- Bar Chart Card -->
        <div class="chart-card">
            <div class="chart-header">
                <h2 class="chart-title">
                    <i class="fas fa-chart-bar"></i> Monthly Spending
                </h2>
                <div class="chart-actions">
                    <button class="chart-action-btn" title="Download as PNG" onclick="downloadChart('barChart', 'monthly-spending.png')">
                        <i class="fas fa-download"></i>
                    </button>
                    <button class="chart-action-btn" title="Refresh Data" onclick="refreshChart('barChart')">
                        <i class="fas fa-sync-alt"></i>
                    </button>
                </div>
            </div>
            
            <div class="chart-wrapper">
                <canvas id="barChart"></canvas>
            </div>
        </div>
    </div>
    
    <!-- Key Insights -->
    <div class="insights-grid">
        <div class="insight-card">
            <div class="insight-header">
                <div class="insight-icon">
                    <i class="fas fa-money-bill-wave"></i>
                </div>
                <h3 class="insight-title">Total Spending</h3>
            </div>
            <div class="insight-value" id="totalSpending">₹0.00</div>
            <p class="insight-description">Total amount spent in selected period</p>
            <div class="insight-trend trend-up">
                <i class="fas fa-arrow-up"></i>
                <span>12% from last period</span>
            </div>
        </div>
        
        <div class="insight-card">
            <div class="insight-header">
                <div class="insight-icon">
                    <i class="fas fa-tags"></i>
                </div>
                <h3 class="insight-title">Top Category</h3>
            </div>
            <div class="insight-value" id="topCategory">Food</div>
            <p class="insight-description">Your most frequent expense category</p>
            <div class="insight-trend trend-up">
                <i class="fas fa-arrow-up"></i>
                <span>₹2,450 this month</span>
            </div>
        </div>
        
        <div class="insight-card">
            <div class="insight-header">
                <div class="insight-icon">
                    <i class="fas fa-calendar-day"></i>
                </div>
                <h3 class="insight-title">Daily Average</h3>
            </div>
            <div class="insight-value" id="dailyAverage">₹0.00</div>
            <p class="insight-description">Average daily spending</p>
            <div class="insight-trend trend-down">
                <i class="fas fa-arrow-down"></i>
                <span>8% lower than last week</span>
            </div>
        </div>
        
        <div class="insight-card">
            <div class="insight-header">
                <div class="insight-icon">
                    <i class="fas fa-bullseye"></i>
                </div>
                <h3 class="insight-title">Budget Status</h3>
            </div>
            <div class="insight-value" id="budgetStatus">75%</div>
            <p class="insight-description">Monthly budget utilization</p>
            <div class="insight-trend">
                <i class="fas fa-info-circle"></i>
                <span>₹2,500 remaining</span>
            </div>
        </div>
    </div>
    
    <!-- Category Breakdown -->
    <div class="category-breakdown">
        <div class="breakdown-header">
            <h2 class="breakdown-title">
                <i class="fas fa-list-ol"></i> Category Breakdown
            </h2>
            <div class="time-btn">
                <i class="fas fa-filter"></i> Filter
            </div>
        </div>
        
        <div class="category-list" id="categoryList">
            <!-- Categories will be populated by JavaScript -->
        </div>
    </div>
    
    <!-- Monthly Comparison -->
    <div class="comparison-section">
        <div class="breakdown-header">
            <h2 class="breakdown-title">
                <i class="fas fa-balance-scale"></i> Monthly Comparison
            </h2>
        </div>
        
        <div class="comparison-grid">
            <div class="comparison-card">
                <div class="comparison-period">Current Month</div>
                <div class="comparison-amount">₹12,450</div>
                <div class="comparison-change change-positive">+8%</div>
            </div>
            
            <div class="comparison-card">
                <div class="comparison-period">Last Month</div>
                <div class="comparison-amount">₹11,520</div>
                <div class="comparison-change change-negative">-5%</div>
            </div>
            
            <div class="comparison-card">
                <div class="comparison-period">3-Month Average</div>
                <div class="comparison-amount">₹10,890</div>
                <div class="comparison-change change-positive">+12%</div>
            </div>
        </div>
    </div>
    
    <!-- Export Section -->
    <div class="export-section">
        <div class="export-header">
            <h2>Export Your Insights</h2>
            <p>Download your expense data and charts for further analysis or record keeping.</p>
        </div>
        <div class="export-options">
            <a href="{{ url_for('export_personal') }}" class="btn btn-primary">
                <i class="fas fa-file-csv"></i> Export as CSV
            </a>
            <button class="btn btn-outline" onclick="exportAllCharts()">
                <i class="fas fa-file-image"></i> Export Charts as PNG
            </button>
            <button class="btn btn-outline" onclick="generateReport()">
                <i class="fas fa-file-pdf"></i> Generate PDF Report
            </button>
        </div>
    </div>
    
    {% else %}
    <!-- No Data State -->
    <div class="no-data-state">
        <i class="fas fa-chart-line"></i>
        <h3>No Data Available Yet</h3>
        <p>Start adding expenses to see beautiful charts and insights about your spending patterns.</p>
        <div style="display: flex; gap: 1rem; justify-content: center; margin-top: 2rem;">
            <a href="{{ url_for('dashboard') }}" class="btn btn-primary">
                <i class="fas fa-plus-circle"></i> Add Your First Expense
            </a>
            <a href="{{ url_for('create_room') }}" class="btn btn-outline">
                <i class="fas fa-users"></i> Create a Room
            </a>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}

{% block extra_js %}
<script>
    // Chart.js Configuration
    Chart.defaults.color = '#E6E6FA';
    Chart.defaults.borderColor = 'rgba(255, 255, 255, 0.1)';
    
    // Define chart colors
    const chartColors = [
        '#8A2BE2', // Purple
        '#00D4FF', // Blue
        '#00FF9D', // Green
        '#FFB74D', // Orange
        '#FF6B8B', // Pink
        '#667EEA', // Light Purple
        '#764BA2', // Dark Purple
        '#4FACFE', // Light Blue
        '#00F2FE', // Cyan
        '#F093FB', // Light Pink
        '#F5576C'  // Red
    ];
    
    // Initialize charts when DOM is loaded
    document.addEventListener('DOMContentLoaded', function() {
        // Time range selector
        const timeButtons = document.querySelectorAll('.time-btn');
        timeButtons.forEach(btn => {
            btn.addEventListener('click', function() {
                timeButtons.forEach(b => b.classList.remove('active'));
                this.classList.add('active');
                
                const range = this.getAttribute('data-range');
                updateCharts(range);
                
                // Add animation
                this.style.transform = 'scale(0.95)';
                setTimeout(() => {
                    this.style.transform = '';
                }, 200);
            });
        });
        
        // Initialize charts if data exists
        {% if categories or monthly_data %}
        initializeCharts();
        updateCategoryList();
        updateInsights();
        {% endif %}
        
        // Add visual effects to cards
        const cards = document.querySelectorAll('.chart-card, .insight-card, .comparison-card');
        cards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.1}s`;
            card.style.animation = 'fadeInUp 0.5s ease forwards';
            card.style.opacity = '0';
            
            setTimeout(() => {
                card.style.opacity = '1';
            }, 300 + (index * 100));
        });
        
        // Initialize tooltips
        initTooltips();
    });
    
    // Initialize Charts
    function initializeCharts() {
        // Prepare data for Pie Chart
        const pieData = preparePieChartData();
        createPieChart(pieData);
        
        // Prepare data for Bar Chart
        const barData = prepareBarChartData();
        createBarChart(barData);
    }
    
    // Prepare Pie Chart Data
    function preparePieChartData() {
        // Sample data - in real app, this would come from backend
        const categories = {
            'Food': 4500,
            'Travel': 3200,
            'Shopping': 2800,
            'Entertainment': 1500,
            'Bills': 1200,
            'Other': 800
        };
        
        return {
            labels: Object.keys(categories),
            data: Object.values(categories),
            colors: chartColors.slice(0, Object.keys(categories).length)
        };
    }
    
    // Create Pie Chart
    function createPieChart(data) {
        const ctx = document.getElementById('pieChart').getContext('2d');
        
        // Clear previous chart if exists
        if (window.pieChartInstance) {
            window.pieChartInstance.destroy();
        }
        
        window.pieChartInstance = new Chart(ctx, {
            type: 'pie',
            data: {
                labels: data.labels,
                datasets: [{
                    data: data.data,
                    backgroundColor: data.colors,
                    borderColor: 'rgba(255, 255, 255, 0.1)',
                    borderWidth: 2,
                    hoverOffset: 15
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'right',
                        labels: {
                            padding: 20,
                            usePointStyle: true,
                            pointStyle: 'circle',
                            font: {
                                size: 12
                            }
                        }
                    },
                    tooltip: {
                        callbacks: {
                            label: function(context) {
                                const label = context.label || '';
                                const value = context.parsed;
                                const total = context.dataset.data.reduce((a, b) => a + b, 0);
                                const percentage = Math.round((value / total) * 100);
                                return `${label}: ₹${value.toLocaleString()} (${percentage}%)`;
                            }
                        }
                    }
                },
                animation: {
                    animateScale: true,
                    animateRotate: true,
                    duration: 1500,
                    easing: 'easeOutQuart'
                }
            }
        });
        
        // Update color legend
        updateColorLegend('pieChartColors', data.labels, data.colors);
    }
    
    // Prepare Bar Chart Data
    function prepareBarChartData() {
        // Sample data - in real app, this would come from backend
        const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
        const monthlyData = [4500, 5200, 4800, 6100, 5800, 7200, 6800, 7500, 6200, 5900, 7100, 8000];
        
        return {
            labels: months.slice(0, 6), // Last 6 months
            data: monthlyData.slice(0, 6),
            color: chartColors[1]
        };
    }
    
    // Create Bar Chart
    function createBarChart(data) {
        const ctx = document.getElementById('barChart').getContext('2d');
        
        // Clear previous chart if exists
        if (window.barChartInstance) {
            window.barChartInstance.destroy();
        }
        
        window.barChartInstance = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: data.labels,
                datasets: [{
                    label: 'Monthly Expenses',
                    data: data.data,
                    backgroundColor: data.color + '80', // 50% opacity
                    borderColor: data.color,
                    borderWidth: 2,
                    borderRadius: 6,
                    hoverBackgroundColor: data.color
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: false
                    },
                    tooltip: {
                        callbacks: {
                            label: function(context) {
                                return `₹${context.parsed.y.toLocaleString()}`;
                            }
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        grid: {
                            color: 'rgba(255, 255, 255, 0.1)'
                        },
                        ticks: {
                            callback: function(value) {
                                return '₹' + value.toLocaleString();
                            }
                        }
                    },
                    x: {
                        grid: {
                            display: false
                        }
                    }
                },
                animation: {
                    duration: 1500,
                    easing: 'easeOutQuart'
                }
            }
        });
    }
    
    // Update charts based on time range
    function updateCharts(range) {
        // Show loading state
        const chartCards = document.querySelectorAll('.chart-card');
        chartCards.forEach(card => {
            card.style.opacity = '0.7';
        });
        
        // Simulate API call
        setTimeout(() => {
            // Update pie chart data based on range
            const pieData = preparePieChartData();
            updatePieChart(pieData);
            
            // Update bar chart data based on range
            const barData = prepareBarChartData();
            updateBarChart(barData);
            
            // Update insights
            updateInsights();
            
            // Update category list
            updateCategoryList();
            
            // Restore opacity
            chartCards.forEach(card => {
                card.style.opacity = '1';
            });
            
            // Show success message
            showFlash(`Charts updated for ${range} period`, 'success');
        }, 500);
    }
    
    // Update Pie Chart
    function updatePieChart(data) {
        if (window.pieChartInstance) {
            window.pieChartInstance.data.labels = data.labels;
            window.pieChartInstance.data.datasets[0].data = data.data;
            window.pieChartInstance.data.datasets[0].backgroundColor = data.colors;
            window.pieChartInstance.update();
            
            // Update color legend
            updateColorLegend('pieChartColors', data.labels, data.colors);
        }
    }
    
    // Update Bar Chart
    function updateBarChart(data) {
        if (window.barChartInstance) {
            window.barChartInstance.data.labels = data.labels;
            window.barChartInstance.data.datasets[0].data = data.data;
            window.barChartInstance.update();
        }
    }
    
    // Update Category List
    function updateCategoryList() {
        const categoryList = document.getElementById('categoryList');
        const pieData = preparePieChartData();
        const total = pieData.data.reduce((a, b) => a + b, 0);
        
        categoryList.innerHTML = '';
        
        pieData.labels.forEach((label, index) => {
            const value = pieData.data[index];
            const percentage = Math.round((value / total) * 100);
            const color = pieData.colors[index];
            
            const categoryItem = document.createElement('div');
            categoryItem.className = 'category-item';
            categoryItem.innerHTML = `
                <div class="category-color" style="background: ${color};"></div>
                <div class="category-info">
                    <div class="category-name">${label}</div>
                    <div class="category-stats">
                        <span>₹${value.toLocaleString()}</span>
                        <span>${percentage}%</span>
                    </div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: ${percentage}%; background: ${color};"></div>
                    </div>
                </div>
            `;
            
            categoryList.appendChild(categoryItem);
            
            // Animate progress bars
            setTimeout(() => {
                const fill = categoryItem.querySelector('.progress-fill');
                fill.style.width = `${percentage}%`;
            }, 100 * index);
        });
    }
    
    // Update Insights
    function updateInsights() {
        const pieData = preparePieChartData();
        const barData = prepareBarChartData();
        
        // Calculate total spending
        const totalSpending = pieData.data.reduce((a, b) => a + b, 0);
        document.getElementById('totalSpending').textContent = `₹${totalSpending.toLocaleString()}`;
        
        // Find top category
        const maxValue = Math.max(...pieData.data);
        const topCategoryIndex = pieData.data.indexOf(maxValue);
        document.getElementById('topCategory').textContent = pieData.labels[topCategoryIndex];
        
        // Calculate daily average (assuming 30 days)
        const dailyAverage = totalSpending / 30;
        document.getElementById('dailyAverage').textContent = `₹${dailyAverage.toFixed(2)}`;
        
        // Update budget status
        const budgetStatus = Math.min(Math.round((totalSpending / 10000) * 100), 100);
        document.getElementById('budgetStatus').textContent = `${budgetStatus}%`;
    }
    
    // Update Color Legend
    function updateColorLegend(elementId, labels, colors) {
        const element = document.getElementById(elementId);
        element.innerHTML = '';
        
        labels.forEach((label, index) => {
            const colorItem = document.createElement('div');
            colorItem.className = 'color-item';
            colorItem.innerHTML = `
                <div class="color-swatch" style="background: ${colors[index]};"></div>
                <span>${label}</span>
            `;
            element.appendChild(colorItem);
        });
    }
    
    // Download Chart as PNG
    function downloadChart(chartId, filename) {
        const chart = document.getElementById(chartId);
        const link = document.createElement('a');
        link.download = filename;
        link.href = chart.toDataURL('image/png');
        link.click();
        
        showFlash(`Chart downloaded as ${filename}`, 'success');
    }
    
    // Refresh Chart
    function refreshChart(chartId) {
        const chartElement = document.getElementById(chartId);
        chartElement.style.opacity = '0.5';
        
        setTimeout(() => {
            if (chartId === 'pieChart') {
                const pieData = preparePieChartData();
                updatePieChart(pieData);
            } else {
                const barData = prepareBarChartData();
                updateBarChart(barData);
            }
            
            chartElement.style.opacity = '1';
            showFlash('Chart data refreshed', 'info');
        }, 300);
    }
    
    // Export All Charts
    function exportAllCharts() {
        // In a real implementation, this would create a ZIP file with all charts
        showFlash('Preparing chart package for download...', 'info');
        
        setTimeout(() => {
            // Simulate download
            showFlash('Charts exported successfully!', 'success');
        }, 1500);
    }
    
    // Generate PDF Report
    function generateReport() {
        showFlash('Generating comprehensive PDF report...', 'info');
        
        setTimeout(() => {
            // Simulate report generation
            const link = document.createElement('a');
            link.download = 'expensio-report.pdf';
            link.href = '#';
            link.click();
            
            showFlash('PDF report downloaded successfully!', 'success');
        }, 2000);
    }
    
    // Initialize Tooltips
    function initTooltips() {
        const tooltipTriggers = document.querySelectorAll('[title]');
        tooltipTriggers.forEach(trigger => {
            trigger.addEventListener('mouseenter', function(e) {
                const tooltip = document.createElement('div');
                tooltip.className = 'tooltip';
                tooltip.textContent = this.getAttribute('title');
                tooltip.style.position = 'absolute';
                tooltip.style.background = 'var(--primary-dark)';
                tooltip.style.color = 'var(--text-light)';
                tooltip.style.padding = '8px 12px';
                tooltip.style.borderRadius = '6px';
                tooltip.style.fontSize = '0.9rem';
                tooltip.style.zIndex = '10000';
                tooltip.style.border = '1px solid var(--card-border)';
                tooltip.style.boxShadow = 'var(--shadow)';
                tooltip.style.maxWidth = '200px';
                tooltip.style.whiteSpace = 'nowrap';
                
                document.body.appendChild(tooltip);
                
                const rect = this.getBoundingClientRect();
                tooltip.style.left = (rect.left + (rect.width / 2) - (tooltip.offsetWidth / 2)) + 'px';
                tooltip.style.top = (rect.top - tooltip.offsetHeight - 10) + 'px';
                
                this._tooltip = tooltip;
            });
            
            trigger.addEventListener('mouseleave', function() {
                if (this._tooltip) {
                    this._tooltip.remove();
                    delete this._tooltip;
                }
            });
        });
    }
    
    // Show Flash Message
    function showFlash(message, type = 'info') {
        // Remove existing flash
        const existingFlash = document.querySelector('.flash-message');
        if (existingFlash) existingFlash.remove();
        
        const flashDiv = document.createElement('div');
        flashDiv.className = `flash-message flash-${type} mb-4`;
        flashDiv.innerHTML = `
            <i class="fas fa-${type === 'success' ? 'check-circle' : 'info-circle'}"></i>
            ${message}
            <button class="flash-close">&times;</button>
        `;
        
        const pageHeader = document.querySelector('.page-header');
        pageHeader.parentNode.insertBefore(flashDiv, pageHeader.nextSibling);
        
        flashDiv.querySelector('.flash-close').addEventListener('click', function() {
            flashDiv.style.opacity = '0';
            flashDiv.style.transform = 'translateY(-10px)';
            setTimeout(() => flashDiv.remove(), 300);
        });
        
        setTimeout(() => {
            if (flashDiv.parentElement) {
                flashDiv.style.opacity = '0';
                flashDiv.style.transform = 'translateY(-10px)';
                setTimeout(() => flashDiv.remove(), 300);
            }
        }, 5000);
    }
    
    // Add animation to time selector buttons
    const timeButtons = document.querySelectorAll('.time-btn');
    timeButtons.forEach((btn, index) => {
        btn.style.animationDelay = `${index * 0.1}s`;
        btn.style.animation = 'fadeInUp 0.5s ease forwards';
        btn.style.opacity = '0';
        
        setTimeout(() => {
            btn.style.opacity = '1';
        }, 300 + (index * 100));
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\dashboard.html
=============================
{% extends "layout.html" %}

{% block title %}Dashboard - Expensio{% endblock %}

{% block extra_css %}
<style>
    /* Dashboard Specific Styles */
    .welcome-section {
        text-align: center;
        padding: 3rem 2rem;
        margin-bottom: 2rem;
        background: linear-gradient(135deg, 
            rgba(138, 43, 226, 0.1) 0%, 
            rgba(75, 0, 130, 0.1) 100%);
        border-radius: 20px;
        border: 1px solid var(--card-border);
    }
    
    .welcome-section h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        background: var(--secondary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
    
    .welcome-section p {
        color: var(--text-muted);
        font-size: 1.1rem;
        max-width: 600px;
        margin: 0 auto;
    }
    
    /* Summary Cards Grid */
    .summary-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
        margin-bottom: 3rem;
    }
    
    .summary-card {
        padding: 2rem;
        border-radius: 15px;
        text-align: center;
        position: relative;
        overflow: hidden;
        transition: var(--transition);
    }
    
    .summary-card:nth-child(1) {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    
    .summary-card:nth-child(2) {
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    }
    
    .summary-card:nth-child(3) {
        background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }
    
    .summary-card i {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        opacity: 0.9;
    }
    
    .summary-card h3 {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
    }
    
    .summary-card p {
        font-size: 0.9rem;
        opacity: 0.9;
    }
    
    /* Quick Actions */
    .quick-actions {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        margin-bottom: 3rem;
    }
    
    .action-btn {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 2rem 1rem;
        border-radius: 15px;
        background: var(--card-bg);
        border: 2px solid var(--card-border);
        transition: var(--transition);
        text-decoration: none;
        color: var(--text-light);
    }
    
    .action-btn:hover {
        transform: translateY(-5px);
        border-color: var(--accent-purple);
        background: rgba(138, 43, 226, 0.1);
    }
    
    .action-btn i {
        font-size: 2rem;
        margin-bottom: 1rem;
        color: var(--accent-blue);
    }
    
    .action-btn span {
        font-weight: 600;
        font-size: 1.1rem;
    }
    
    /* Rooms Section */
    .rooms-section {
        margin-bottom: 3rem;
    }
    
    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
    }
    
    .section-title {
        font-size: 1.8rem;
        font-weight: 600;
    }
    
    .view-all {
        color: var(--accent-blue);
        text-decoration: none;
        font-weight: 500;
    }
    
    .view-all:hover {
        text-decoration: underline;
    }
    
    .rooms-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 1.5rem;
    }
    
    .room-card {
        padding: 1.5rem;
        border-radius: 15px;
        position: relative;
        overflow: hidden;
    }
    
    .room-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, 
            transparent, 
            rgba(255, 255, 255, 0.1), 
            transparent);
        transition: 0.6s;
    }
    
    .room-card:hover::before {
        left: 100%;
    }
    
    .room-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
    }
    
    .room-name {
        font-size: 1.3rem;
        font-weight: 600;
        color: var(--accent-blue);
    }
    
    .room-code {
        background: rgba(138, 43, 226, 0.2);
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.9rem;
        font-family: monospace;
    }
    
    .room-stats {
        display: flex;
        gap: 1rem;
        margin-top: 1rem;
        font-size: 0.9rem;
    }
    
    .room-stat {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .room-stat i {
        color: var(--accent-purple);
    }
    
    /* Recent Activity */
    .activity-list {
        background: var(--card-bg);
        border-radius: 15px;
        padding: 1.5rem;
        border: 1px solid var(--card-border);
    }
    
    .activity-item {
        display: flex;
        align-items: center;
        padding: 1rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .activity-item:last-child {
        border-bottom: none;
    }
    
    .activity-icon {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: rgba(138, 43, 226, 0.2);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-right: 1rem;
    }
    
    .activity-details {
        flex: 1;
    }
    
    .activity-title {
        font-weight: 500;
        margin-bottom: 0.25rem;
    }
    
    .activity-time {
        font-size: 0.85rem;
        color: var(--text-muted);
    }
    
    .activity-amount {
        font-weight: 600;
        color: var(--success);
    }
    
    /* Budget Tracker */
    .budget-tracker {
        background: var(--card-bg);
        border-radius: 15px;
        padding: 2rem;
        border: 1px solid var(--card-border);
        margin-top: 2rem;
    }
    
    .budget-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
    }
    
    .budget-title {
        font-size: 1.3rem;
        font-weight: 600;
    }
    
    .budget-amount {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--accent-blue);
    }
    
    .progress-bar {
        height: 12px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        overflow: hidden;
        margin-bottom: 1rem;
    }
    
    .progress-fill {
        height: 100%;
        background: var(--secondary-gradient);
        border-radius: 10px;
        transition: width 0.5s ease;
    }
    
    .budget-info {
        display: flex;
        justify-content: space-between;
        font-size: 0.9rem;
        color: var(--text-muted);
    }
    
    /* Empty States */
    .empty-state {
        text-align: center;
        padding: 3rem 2rem;
        color: var(--text-muted);
    }
    
    .empty-state i {
        font-size: 3rem;
        margin-bottom: 1rem;
        opacity: 0.5;
    }
    
    /* Responsive */
    @media (max-width: 768px) {
        .summary-grid {
            grid-template-columns: 1fr;
        }
        
        .quick-actions {
            grid-template-columns: repeat(2, 1fr);
        }
        
        .rooms-grid {
            grid-template-columns: 1fr;
        }
        
        .section-header {
            flex-direction: column;
            align-items: flex-start;
            gap: 1rem;
        }
    }
    
    @media (max-width: 480px) {
        .quick-actions {
            grid-template-columns: 1fr;
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="dashboard-container">
    <!-- Welcome Section -->
    <div class="welcome-section">
        <h1>Welcome back, {{ current_user.username }}! 👋</h1>
        <p>Track your expenses, manage group budgets, and stay on top of your finances.</p>
    </div>

    <!-- Summary Cards -->
    <div class="summary-grid">
        <div class="summary-card">
            <i class="fas fa-money-bill-wave"></i>
            <h3>₹{{ "%.2f"|format(total_spent) }}</h3>
            <p>Total You Spent</p>
        </div>
        
        <div class="summary-card">
            <i class="fas fa-hand-holding-usd"></i>
            <h3>₹{{ "%.2f"|format(total_owed) }}</h3>
            <p>Total Owed To You</p>
        </div>
        
        <div class="summary-card">
            <i class="fas fa-balance-scale"></i>
            <h3>₹{{ "%.2f"|format(net_balance) }}</h3>
            <p>Net Balance</p>
        </div>
    </div>

    <!-- Quick Actions -->
    <div class="quick-actions">
        <a href="{{ url_for('create_room') }}" class="action-btn">
            <i class="fas fa-plus-circle"></i>
            <span>Create Room</span>
        </a>
        
        <a href="{{ url_for('join_room') }}" class="action-btn">
            <i class="fas fa-sign-in-alt"></i>
            <span>Join Room</span>
        </a>
        
        <a href="{{ url_for('add_expense', room_id=0) }}" class="action-btn">
            <i class="fas fa-receipt"></i>
            <span>Add Expense</span>
        </a>
        
        <a href="{{ url_for('charts') }}" class="action-btn">
            <i class="fas fa-chart-bar"></i>
            <span>View Charts</span>
        </a>
    </div>

    <!-- Your Rooms -->
    <div class="rooms-section">
        <div class="section-header">
            <h2 class="section-title">Your Rooms</h2>
            <a href="{{ url_for('create_room') }}" class="view-all">
                Create New Room <i class="fas fa-arrow-right"></i>
            </a>
        </div>
        
        {% if rooms %}
        <div class="rooms-grid">
            {% for room in rooms %}
            <div class="card room-card">
                <div class="room-header">
                    <div class="room-name">{{ room.name }}</div>
                    <div class="room-code">{{ room.room_code }}</div>
                </div>
                
                <p class="room-description">{{ room.description or "No description" }}</p>
                
                <div class="room-stats">
                    <div class="room-stat">
                        <i class="fas fa-users"></i>
                        <span>{{ room.members|length }} members</span>
                    </div>
                    <div class="room-stat">
                        <i class="fas fa-calendar"></i>
                        <span>{{ room.created_at.strftime('%b %d') }}</span>
                    </div>
                </div>
                
                <a href="{{ url_for('view_room', room_id=room.id) }}" 
                   class="btn btn-outline" style="width: 100%; margin-top: 1rem;">
                    Enter Room <i class="fas fa-arrow-right"></i>
                </a>
            </div>
            {% endfor %}
        </div>
        {% else %}
        <div class="empty-state">
            <i class="fas fa-door-closed"></i>
            <h3>No Rooms Yet</h3>
            <p>Create or join a room to start tracking group expenses!</p>
            <a href="{{ url_for('create_room') }}" class="btn btn-primary mt-2">
                Create Your First Room
            </a>
        </div>
        {% endif %}
    </div>

    <div class="dashboard-grid">
        <!-- Recent Activity -->
        <div class="card">
            <div class="section-header">
                <h2 class="section-title">Recent Activity</h2>
            </div>
            
            <div class="activity-list">
                {% if personal_expenses %}
                    {% for expense in personal_expenses %}
                    <div class="activity-item">
                        <div class="activity-icon">
                            {% if expense.category == 'Food' %}
                            <i class="fas fa-utensils"></i>
                            {% elif expense.category == 'Travel' %}
                            <i class="fas fa-car"></i>
                            {% elif expense.category == 'Shopping' %}
                            <i class="fas fa-shopping-bag"></i>
                            {% else %}
                            <i class="fas fa-receipt"></i>
                            {% endif %}
                        </div>
                        <div class="activity-details">
                            <div class="activity-title">{{ expense.description }}</div>
                            <div class="activity-time">{{ expense.date.strftime('%b %d, %I:%M %p') }}</div>
                        </div>
                        <div class="activity-amount">₹{{ "%.2f"|format(expense.amount) }}</div>
                    </div>
                    {% endfor %}
                {% else %}
                    <div class="empty-state">
                        <i class="fas fa-history"></i>
                        <p>No recent activity</p>
                    </div>
                {% endif %}
            </div>
        </div>

        <!-- Personal Budget Tracker -->
        <div class="card">
            <div class="budget-tracker">
                <div class="budget-header">
                    <h2 class="budget-title">Monthly Budget</h2>
                    <div class="budget-amount">₹5,000</div>
                </div>
                
                <div class="progress-bar">
                    <div class="progress-fill" style="width: 65%"></div>
                </div>
                
                <div class="budget-info">
                    <span>Spent: ₹3,250</span>
                    <span>Remaining: ₹1,750</span>
                </div>
                
                <div style="margin-top: 1.5rem;">
                    <button class="btn btn-outline" style="width: 100%;">
                        <i class="fas fa-edit"></i> Edit Budget
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block extra_js %}
<script>
    // Dashboard specific JavaScript
    document.addEventListener('DOMContentLoaded', function() {
        // Animate summary cards on load
        const cards = document.querySelectorAll('.summary-card');
        cards.forEach((card, index) => {
            card.style.animationDelay = `${index * 0.1}s`;
            card.classList.add('floating');
        });
        
        // Update progress bar animation
        const progressFill = document.querySelector('.progress-fill');
        setTimeout(() => {
            progressFill.style.transition = 'width 1s ease';
        }, 500);
        
        // Room card hover effect
        const roomCards = document.querySelectorAll('.room-card');
        roomCards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px)';
                this.style.boxShadow = '0 15px 35px rgba(138, 43, 226, 0.2)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0)';
                this.style.boxShadow = 'none';
            });
        });
        
        // Flash message auto dismiss
        const flashMessages = document.querySelectorAll('.flash-message');
        flashMessages.forEach(message => {
            setTimeout(() => {
                message.style.opacity = '0';
                message.style.transform = 'translateY(-20px)';
                setTimeout(() => message.remove(), 300);
            }, 4000);
            
            // Close button
            const closeBtn = message.querySelector('.flash-close');
            if (closeBtn) {
                closeBtn.addEventListener('click', () => {
                    message.style.opacity = '0';
                    message.style.transform = 'translateY(-20px)';
                    setTimeout(() => message.remove(), 300);
                });
            }
        });
    });
</script>
{% endblock %}


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\export.html
=============================


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\index.html
=============================


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\templates\layout.html
=============================
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Expensio - Smart Planning & Budgeting{% endblock %}</title>
    
    <!-- CSS -->
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
    {% block extra_css %}{% endblock %}
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="{{ url_for('static', filename='images/logo.svg') }}">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="container">
            <a href="{{ url_for('dashboard') }}" class="navbar-brand">
                <i class="fas fa-money-bill-wave"></i> EXPENSIO
            </a>
            
            {% if current_user.is_authenticated %}
            <div class="nav-menu">
                <a href="{{ url_for('dashboard') }}" class="nav-link">
                    <i class="fas fa-home"></i> Dashboard
                </a>
                <a href="{{ url_for('create_room') }}" class="nav-link">
                    <i class="fas fa-plus-circle"></i> Create Room
                </a>
                <a href="{{ url_for('join_room') }}" class="nav-link">
                    <i class="fas fa-sign-in-alt"></i> Join Room
                </a>
                <a href="{{ url_for('charts') }}" class="nav-link">
                    <i class="fas fa-chart-pie"></i> Charts
                </a>
                <a href="{{ url_for('about') }}" class="nav-link">
                    <i class="fas fa-info-circle"></i> About
                </a>
                <div class="user-dropdown">
                    <button class="user-btn">
                        <i class="fas fa-user-circle"></i> {{ current_user.username }}
                    </button>
                    <div class="dropdown-menu">
                        <a href="{{ url_for('personal_expense') }}" class="dropdown-item">
                            <i class="fas fa-wallet"></i> Personal Expenses
                        </a>
                        <a href="{{ url_for('logout') }}" class="dropdown-item">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </a>
                    </div>
                </div>
            </div>
            {% endif %}
        </div>
    </nav>

    <!-- Flash Messages -->
    <div class="flash-container">
        {% with messages = get_flashed_messages(with_categories=true) %}
            {% if messages %}
                {% for category, message in messages %}
                    <div class="flash-message flash-{{ category }}">
                        {{ message }}
                        <button class="flash-close">&times;</button>
                    </div>
                {% endfor %}
            {% endif %}
        {% endwith %}
    </div>

    <!-- Main Content -->
    <main class="main-content">
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 Expensio. Smart Planning & Budgeting.</p>
            <p class="footer-links">
                <a href="{{ url_for('about') }}">About</a> |
                <a href="#">Privacy</a> |
                <a href="#">Terms</a>
            </p>
        </div>
    </footer>

    <!-- JavaScript -->
    <script src="{{ url_for('static', filename='js/main.js') }}"></script>
    {% block extra_js %}{% endblock %}
</body>
</html>


=============================
FILE: C:\Users\Harini\OneDrive\Desktop\expensio\app.py
=============================
from flask import Flask, render_template, request, redirect, url_for, flash, jsonify, send_file
from flask_sqlalchemy import SQLAlchemy
from flask_login import LoginManager, UserMixin, login_user, login_required, logout_user, current_user
from werkzeug.security import generate_password_hash, check_password_hash
from datetime import datetime
import csv
import io
import os
from dotenv import load_dotenv

# Load environment variables
load_dotenv()

app = Flask(__name__)
app.config['SECRET_KEY'] = os.getenv('SECRET_KEY', 'dev-secret-key-change-in-production')
app.config['SQLALCHEMY_DATABASE_URI'] = os.getenv('DATABASE_URL', 'sqlite:///expensio.db')
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False

# Initialize extensions
db = SQLAlchemy(app)
login_manager = LoginManager(app)
login_manager.login_view = 'login'
login_manager.login_message_category = 'info'

# ========== MODELS (Define them here to avoid circular imports) ==========
class User(db.Model, UserMixin):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(80), unique=True, nullable=False)
    email = db.Column(db.String(120), unique=True, nullable=False)
    password_hash = db.Column(db.String(200), nullable=False)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    
    # Relationships will be defined after all models

class Room(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), nullable=False)
    description = db.Column(db.Text)
    room_code = db.Column(db.String(50), unique=True, nullable=False)
    budget = db.Column(db.Float, default=0)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    created_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)

class RoomMember(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    room_id = db.Column(db.Integer, db.ForeignKey('room.id'), nullable=False)
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    is_admin = db.Column(db.Boolean, default=False)
    joined_at = db.Column(db.DateTime, default=datetime.utcnow)

class Expense(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    description = db.Column(db.String(200), nullable=False)
    amount = db.Column(db.Float, nullable=False)
    category = db.Column(db.String(50), default='Other')
    room_id = db.Column(db.Integer, db.ForeignKey('room.id'), nullable=False)
    paid_by = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    date = db.Column(db.DateTime, default=datetime.utcnow)
    notes = db.Column(db.Text)

class PersonalExpense(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    description = db.Column(db.String(200), nullable=False)
    amount = db.Column(db.Float, nullable=False)
    category = db.Column(db.String(50), default='Other')
    user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
    date = db.Column(db.DateTime, default=datetime.utcnow)
    budget_month = db.Column(db.String(7))  # YYYY-MM format

# Now define relationships
User.rooms = db.relationship('RoomMember', backref='member', lazy=True)
User.personal_expenses = db.relationship('PersonalExpense', backref='user', lazy=True)
User.expenses_paid = db.relationship('Expense', foreign_keys='Expense.paid_by', backref='payer', lazy=True)
Room.expenses = db.relationship('Expense', backref='room', lazy=True)
Room.members = db.relationship('RoomMember', backref='room', lazy=True)

@login_manager.user_loader
def load_user(user_id):
    return db.session.get(User, int(user_id))  # Fixed: Use db.session.get()

# ========== AUTH ROUTES ==========
@app.route('/')
def index():
    if current_user.is_authenticated:
        return redirect(url_for('dashboard'))
    return redirect(url_for('login'))

@app.route('/login', methods=['GET', 'POST'])
def login():
    if request.method == 'POST':
        username = request.form.get('username')
        password = request.form.get('password')
        
        user = User.query.filter_by(username=username).first()
        
        if user and check_password_hash(user.password_hash, password):
            login_user(user, remember=True)
            flash('Login successful!', 'success')
            return redirect(url_for('dashboard'))
        else:
            flash('Invalid username or password', 'error')
    
    return render_template('auth/login.html')

@app.route('/signup', methods=['GET', 'POST'])
def signup():
    if request.method == 'POST':
        username = request.form.get('username')
        email = request.form.get('email')
        password = request.form.get('password')
        confirm_password = request.form.get('confirm_password')
        
        # Validation
        if password != confirm_password:
            flash('Passwords do not match!', 'error')
            return redirect(url_for('signup'))
        
        if User.query.filter_by(username=username).first():
            flash('Username already exists', 'error')
            return redirect(url_for('signup'))
        
        if User.query.filter_by(email=email).first():
            flash('Email already registered', 'error')
            return redirect(url_for('signup'))
        
        # Create new user
        new_user = User(
            username=username,
            email=email,
            password_hash=generate_password_hash(password)
        )
        
        try:
            db.session.add(new_user)
            db.session.commit()
            flash('Account created successfully! Please login.', 'success')
            return redirect(url_for('login'))
        except Exception as e:
            db.session.rollback()
            flash('Error creating account. Please try again.', 'error')
            print(f"Error: {e}")
    
    return render_template('auth/signup.html')

@app.route('/logout')
@login_required
def logout():
    logout_user()
    flash('Logged out successfully', 'info')
    return redirect(url_for('login'))

# ========== DASHBOARD ROUTES ==========
@app.route('/dashboard')
@login_required
def dashboard():
    try:
        # Calculate user statistics
        user_rooms = RoomMember.query.filter_by(user_id=current_user.id).all()
        room_ids = [room.room_id for room in user_rooms]
        
        # Get all expenses in user's rooms
        expenses = Expense.query.filter(Expense.room_id.in_(room_ids)).all() if room_ids else []
        
        # Calculate totals
        total_spent = sum(exp.amount for exp in expenses if exp.paid_by == current_user.id)
        total_owed = 0  # We'll implement this logic later
        net_balance = total_spent - total_owed
        
        # Get user's rooms
        rooms = Room.query.filter(Room.id.in_(room_ids)).all() if room_ids else []
        
        # Get personal expenses
        personal_expenses = PersonalExpense.query.filter_by(
            user_id=current_user.id
        ).order_by(PersonalExpense.date.desc()).limit(5).all()
        
        return render_template('dashboard.html',
                             total_spent=total_spent,
                             total_owed=total_owed,
                             net_balance=net_balance,
                             rooms=rooms,
                             personal_expenses=personal_expenses)
    except Exception as e:
        print(f"Dashboard error: {e}")
        return render_template('dashboard.html',
                             total_spent=0,
                             total_owed=0,
                             net_balance=0,
                             rooms=[],
                             personal_expenses=[])

# ========== ROOM ROUTES ==========
@app.route('/room/create', methods=['GET', 'POST'])
@login_required
def create_room():
    if request.method == 'POST':
        name = request.form.get('name')
        description = request.form.get('description')
        room_code = request.form.get('room_code')
        budget = float(request.form.get('budget', 0))
        
        new_room = Room(
            name=name,
            description=description,
            room_code=room_code,
            budget=budget,
            created_by=current_user.id
        )
        
        try:
            db.session.add(new_room)
            db.session.commit()
            
            # Add creator as room member
            member = RoomMember(
                room_id=new_room.id,
                user_id=current_user.id,
                is_admin=True
            )
            db.session.add(member)
            db.session.commit()
            
            flash(f'Room "{name}" created successfully!', 'success')
            return redirect(url_for('view_room', room_id=new_room.id))
        except Exception as e:
            db.session.rollback()
            flash(f'Error creating room: {str(e)}', 'error')
    
    return render_template('room/create.html')

@app.route('/room/join', methods=['GET', 'POST'])
@login_required
def join_room():
    if request.method == 'POST':
        room_name = request.form.get('room_name')
        room_code = request.form.get('room_code')
        
        room = Room.query.filter_by(name=room_name, room_code=room_code).first()
        
        if room:
            # Check if already a member
            existing = RoomMember.query.filter_by(
                room_id=room.id,
                user_id=current_user.id
            ).first()
            
            if not existing:
                member = RoomMember(
                    room_id=room.id,
                    user_id=current_user.id,
                    is_admin=False
                )
                db.session.add(member)
                db.session.commit()
                flash(f'Joined room "{room.name}" successfully!', 'success')
            else:
                flash('You are already a member of this room', 'info')
            
            return redirect(url_for('view_room', room_id=room.id))
        else:
            flash('Invalid room name or code', 'error')
    
    return render_template('room/join.html')

@app.route('/room/<int:room_id>')
@login_required
def view_room(room_id):
    try:
        room = Room.query.get_or_404(room_id)
        
        # Check if user is member
        member = RoomMember.query.filter_by(
            room_id=room_id,
            user_id=current_user.id
        ).first()
        
        if not member:
            flash('You are not a member of this room', 'error')
            return redirect(url_for('dashboard'))
        
        # Get room expenses
        expenses = Expense.query.filter_by(room_id=room_id).order_by(
            Expense.date.desc()
        ).all()
        
        # Calculate balances
        members = RoomMember.query.filter_by(room_id=room_id).all()
        member_data = []
        
        for mem in members:
            user = db.session.get(User, mem.user_id)  # Fixed: Use db.session.get()
            paid = sum(exp.amount for exp in expenses if exp.paid_by == user.id)
            share = sum(exp.amount for exp in expenses) / len(members) if members else 0
            balance = share - paid
            
            member_data.append({
                'user': user,
                'paid': paid,
                'share': share,
                'balance': balance
            })
        
        return render_template('room/view.html',
                             room=room,
                             expenses=expenses,
                             members=member_data,
                             is_admin=member.is_admin)
    except Exception as e:
        flash(f'Error loading room: {str(e)}', 'error')
        return redirect(url_for('dashboard'))

# ========== EXPENSE ROUTES ==========
@app.route('/expense/add/<int:room_id>', methods=['GET', 'POST'])
@login_required
def add_expense(room_id):
    room = Room.query.get_or_404(room_id)
    
    if request.method == 'POST':
        description = request.form.get('description')
        amount = float(request.form.get('amount'))
        category = request.form.get('category')
        
        new_expense = Expense(
            description=description,
            amount=amount,
            category=category,
            room_id=room_id,
            paid_by=current_user.id,
            date=datetime.utcnow()
        )
        
        try:
            db.session.add(new_expense)
            db.session.commit()
            flash('Expense added successfully!', 'success')
            return redirect(url_for('view_room', room_id=room_id))
        except Exception as e:
            db.session.rollback()
            flash(f'Error adding expense: {str(e)}', 'error')
    
    # Pass current date to template - FIXED: Use the imported datetime module
    return render_template('expenses/add.html', room=room, now=datetime.utcnow())

@app.route('/expense/personal', methods=['GET', 'POST'])
@login_required
def personal_expense():
    if request.method == 'POST':
        description = request.form.get('description')
        amount = float(request.form.get('amount'))
        category = request.form.get('category')
        date_str = request.form.get('date')
        
        try:
            if date_str:
                date = datetime.strptime(date_str, '%Y-%m-%d')
            else:
                date = datetime.utcnow()
            
            new_expense = PersonalExpense(
                description=description,
                amount=amount,
                category=category,
                user_id=current_user.id,
                date=date
            )
            
            db.session.add(new_expense)
            db.session.commit()
            flash('Personal expense added!', 'success')
            return redirect(url_for('dashboard'))
        except Exception as e:
            db.session.rollback()
            flash(f'Error adding expense: {str(e)}', 'error')
    
    # Get personal expenses
    expenses = PersonalExpense.query.filter_by(
        user_id=current_user.id
    ).order_by(PersonalExpense.date.desc()).all()
    
    # Calculate total
    total = sum(exp.amount for exp in expenses)
    
    return render_template('expenses/personal.html',
                         expenses=expenses,
                         total=total)

# ========== CHART ROUTES ==========
@app.route('/charts')
@login_required
def charts():
    try:
        # Get data for charts
        personal_expenses = PersonalExpense.query.filter_by(
            user_id=current_user.id
        ).all()
        
        # Prepare data for pie chart (categories)
        categories = {}
        for exp in personal_expenses:
            categories[exp.category] = categories.get(exp.category, 0) + exp.amount
        
        # Prepare data for bar chart (monthly expenses)
        monthly_data = {}
        for exp in personal_expenses:
            month = exp.date.strftime('%Y-%m')
            monthly_data[month] = monthly_data.get(month, 0) + exp.amount
        
        return render_template('charts.html',
                             categories=categories,
                             monthly_data=monthly_data)
    except Exception as e:
        print(f"Charts error: {e}")
        return render_template('charts.html',
                             categories={},
                             monthly_data={})

# ========== EXPORT ROUTES ==========
@app.route('/export/<int:room_id>')
@login_required
def export_room(room_id):
    try:
        room = Room.query.get_or_404(room_id)
        expenses = Expense.query.filter_by(room_id=room_id).all()
        
        # Create CSV in memory
        output = io.StringIO()
        writer = csv.writer(output)
        
        # Write header
        writer.writerow(['Description', 'Amount', 'Paid By', 'Category', 'Date'])
        
        # Write data
        for exp in expenses:
            user = db.session.get(User, exp.paid_by)  # Fixed: Use db.session.get()
            writer.writerow([
                exp.description,
                exp.amount,
                user.username if user else 'Unknown',
                exp.category,
                exp.date.strftime('%Y-%m-%d %H:%M:%S')
            ])
        
        # Prepare response
        output.seek(0)
        return send_file(
            io.BytesIO(output.getvalue().encode()),
            mimetype='text/csv',
            as_attachment=True,
            download_name=f'expensio_room_{room.name}.csv'
        )
    except Exception as e:
        flash(f'Error exporting data: {str(e)}', 'error')
        return redirect(url_for('view_room', room_id=room_id))

@app.route('/export/personal')
@login_required
def export_personal():
    try:
        expenses = PersonalExpense.query.filter_by(user_id=current_user.id).all()
        
        output = io.StringIO()
        writer = csv.writer(output)
        
        writer.writerow(['Description', 'Amount', 'Category', 'Date'])
        
        for exp in expenses:
            writer.writerow([
                exp.description,
                exp.amount,
                exp.category,
                exp.date.strftime('%Y-%m-%d')
            ])
        
        output.seek(0)
        return send_file(
            io.BytesIO(output.getvalue().encode()),
            mimetype='text/csv',
            as_attachment=True,
            download_name='expensio_personal_expenses.csv'
        )
    except Exception as e:
        flash(f'Error exporting data: {str(e)}', 'error')
        return redirect(url_for('dashboard'))

# ========== ABOUT PAGE ==========
@app.route('/about')
def about():
    return render_template('about.html')

# ========== ERROR HANDLERS ==========
@app.errorhandler(404)
def page_not_found(e):
    return render_template('404.html'), 404

@app.errorhandler(500)
def internal_error(e):
    db.session.rollback()
    return render_template('500.html'), 500

# ========== CREATE TABLES ==========
def create_tables():
    with app.app_context():
        db.create_all()
        print("✅ Database tables created successfully!")
        
        # Create a sample user if none exists
        if not User.query.first():
            sample_user = User(
                username='demo',
                email='demo@expensio.com',
                password_hash=generate_password_hash('demo123')
            )
            db.session.add(sample_user)
            db.session.commit()
            print("👤 Sample user created: demo / demo123")

if __name__ == '__main__':
    create_tables()  # This will create tables when app runs
    app.run(debug=True, port=5000)


